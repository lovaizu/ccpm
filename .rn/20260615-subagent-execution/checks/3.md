# 3 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence |
|---|---|---|
| Leads with the purpose and frames delegation as payoff-governed, not "the coordinator performs no domain work itself" | OK | Intro now opens with "The loop exists to **keep the coordinator's context light while preserving quality and keeping the user in the loop at every task boundary**" and states "**Delegation is the means, governed by payoff — not an absolute.**" The old "does no domain work itself" sentence is removed; the coordinator bullet now reads "routes work that carries trial-and-error to the expert … and handles trivial edits and bookkeeping itself." |
| Execute phase delegates work carrying real trial-and-error and permits coordinator-direct trivial edits, with the trigger stated | OK | New paragraph: "**Decide: delegate or act directly.** The trigger is whether there is trial-and-error to isolate from the coordinator's context. If the work carries real trial-and-error or exploration — code, research, multi-attempt work — dispatch the implementation expert … If it is a trivial single-step edit with no exploration, the coordinator makes the edit itself and proceeds to Verify." |
| Complete phase performs commit/push as coordinator bookkeeping (no mandatory subagent), keeps the user-review gate and the `complete task #{id}` convention | OK | Step renamed "**Step — Commit and push (coordinator bookkeeping)**": "The coordinator commits and pushes directly — there is no trial-and-error to isolate, so this is its own bookkeeping". User-review step unchanged ("**DO NOT proceed without user approval.**"). Message convention kept verbatim: "`{type}: complete task #{id} — {description}`" and "The exact `complete task #{id}` substring is what `/rn:hi` matches against `git log`". |
| Review-expert independence and neutral-framing safeguards present and unweakened | OK | Verify phase untouched: "Each review expert runs as an independent subagent … That independence is the safeguard against bias — protect it." and the 6-element list incl. element 6 "**Neutral framing** … **Never** pass the self-check file … do not hint at the verdict you expect." |
| `gm` and `hi` still reach task execution solely through `task-workflow.md` | OK | Edits confined to this file; "`gm` and `hi` read this file when they reach task execution" retained. No gm/hi files touched; nothing introduced contradicts them. |

## QA Expert Review (subagent, adversarial)

PASS on all 5 completion criteria with quoted evidence (purpose-first intro; payoff-governed
delegation; Execute delegate-or-direct trigger; Complete commit/push as coordinator bookkeeping with
the user gate and `complete task #{id}` convention; Verify independence + 6-element neutral framing
untouched; gm/hi not contradicted).

One valid finding, triaged and fixed (no escalation — minor, decidable against the bar):
- **Finding B (self-check ownership on the direct-edit path).** The new "coordinator may edit directly"
  path left it unstated who produces the self-check. Fixed directly (trivial edit): the Execute
  "Decide" block now says the coordinator performs the self-check itself (element 5, recorded in the
  check file); for whole-document coherence the Process-selection and Check-file-format paragraphs were
  also softened to name the coordinator on the direct-edit path. Re-reviewed → CLOSED, PASS, no new
  inconsistency or dangling reference.

## Overall Verdict
- Self-check: OK
- QA: OK (PASS, after closing Finding B)
- Ready for user review: Yes
