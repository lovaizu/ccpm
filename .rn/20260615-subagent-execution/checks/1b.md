# 1b Triage subsection rewrite — Self-check

## Criteria

| # | Criterion | OK/NG | Evidence |
|---|---|---|---|
| 1 | Opens with the bar (proper form: correct/clear/consistent with codebase standards); triage against it; don't swallow wholesale; don't bounce a decidable call for lack of a standard | OK | Opening paragraph: "The coordinator holds **the bar**: the artifact should reach the form it ought to take to serve the goal — correct, clear, and consistent with the codebase's standards (its **proper form**)... don't swallow review feedback wholesale, and **don't bounce a decidable call to the user for lack of a standard**." |
| 2 | Assess each finding: factually correct + moves artifact toward its proper form for the goal | OK | "is it factually correct, and does acting on it move the artifact toward its proper form for the goal?" |
| 3 | Valid → instructions + re-dispatch impl expert (coordinator does not edit), re-run reviewer; includes minor improvements (apply, don't ask); max 3, then record + escalate | OK | Valid bucket: re-dispatches implementation expert; "**This includes minor improvements: if the fix is correct and makes the artifact better, just apply it — do not ask the user.**"; "Max 3 iterations... record them and escalate to user review" |
| 4 | Invalid → reject with evidence; only on factual error or out-of-scope per Completion criteria; cite fact/criterion; never accept just because raised | OK | Invalid bucket unchanged in substance: "Invalid **only** when it rests on a factual error, or falls outside a scope boundary written in the task's Completion criteria — cite the specific fact or criterion. Never accept a finding just because an expert raised it." |
| 5 | User's call → escalate only when genuinely theirs (expand scope / change direction or taste / effort-vs-benefit only user can weigh); "minor so I'll ask" is NOT a reason — decide against the bar | OK | User's call bucket: "escalate to the user *only* when the decision is genuinely theirs: it would expand scope, change the agreed direction or a matter of taste, or trade effort against benefit... \"It's minor, so I'll just ask\" is **not** a reason — decide it against the bar." |
| 6 | Closing: every finding ends in fix / evidence-cited rejection / user-owned decision; never silently dropped, never blindly accepted, never bounced for lack of a standard | OK | "Every finding ends in an explicit fix, an evidence-cited rejection, or a user-owned decision — never silently dropped, never blindly accepted, never bounced for lack of a standard." |
| 7 | Recording line after buckets kept | OK | "Record the review verdicts into `{steering_dir}/checks/{task-id}.md`..." line is intact and immediately follows the subsection. |
| 8 | Only the triage subsection changed in task-workflow.md; no other file touched by this task | OK | `git diff rn/references/task-workflow.md` shows changes confined to lines under "Step — Triage and re-instruct". (rn/README.md shows as modified but that change pre-existed in the worktree, not made by this task.) |

## QA Expert Review

PASS (adversarial, loophole-hunt). All four "bar to clear" points present; Valid / Invalid / User's
call partition is disjoint and jointly exhaustive (no over-/under-asking escape hatch — buckets keyed
on kind-of-decision, not magnitude; "minor → I'll just ask" explicitly forbidden; "agreed direction"
discriminates taste-changes from pure improvements). Safeguards intact: Phase: Complete user-review
gate untouched and even fed by the post-cap escalation; Invalid still needs evidence-cited rejection;
"never silently dropped / blindly accepted" holds across the three buckets. No dangling reference.
Residual grey zone (clarity = proper form vs taste) is inherent and adequately bounded.

## Overall Verdict

- Self-check: OK — all 6 required intent points present, recording line preserved, scope confined.
- QA: OK (PASS)
- Ready for user review: Yes
