# 2 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence |
|---|---|---|
| `gm`/`hi`/`bb` SKILL.md, `rn/README.md`, `steering-template.md` contain no statement contradicting the new division (incl. README bb-commit example and the "without involving an expert" line) | OK | README.md:52 "without involving an expert" line removed; README.md:61 now `"test: add reproduction test for payment failure"` (no `complete task #` marker); bb SKILL.md forbids the marker (bb/SKILL.md:31); hi/SKILL.md:37 matches `complete task #{id}` per new model; gm has no commit-ownership claim (gm commits only its own steering ledger, gm/SKILL.md:66). grep across rn/ confirms no stale phrases remain |
| Neither `steering-template.md` nor push-and-review.md states "1 task = 1 commit"; each states "commit and push every change; one completion marker per task" | OK | steering-template.md:29 `- commit and push every change; one completion marker per task`; push-and-review.md:8 "Commit and push every change; one completion marker per task." grep for "1 task = 1 commit"/"one commit" returns nothing |
| `bb` SKILL.md explicitly forbids `complete task #{id}` in a suspend-time commit and references `steering-template.md` for `State`/`paused` semantics | OK | bb/SKILL.md:31 "this is a plain commit — its message must not contain `complete task #{id}`..."; bb/SKILL.md State step now "per the `State` field semantics in `steering-template.md`"; template State placeholder (steering-template.md:67-69) now documents the `paused`/`not suspended` semantics the reference points to |
| `gm` SKILL.md does not re-teach content owned by `steering-template.md`/`task-workflow.md`; no behavioral directive lost | OK | Step 3 (gm/SKILL.md:48-50) replaced the re-taught two-axes/exhaustive/facts-vs-assumptions prose with "follow its per-section guidance" hand-off (template owns it at lines 18-25). Step 4 (gm/SKILL.md:55-57) replaced the field re-listing with "follow the template's `Tasks` structure, inline `Completion criteria` rules, and `Task definition requirements` table in full". KEPT: the goal-restatement (gm's own core interaction) and the "work backwards from Acceptance criteria" decomposition method (load-bearing, not in template). No behavioral directive lost — each removed line is owned verbatim by the template, still reached via the read+follow hand-off |
| `plugin.json` `description` reflects coordinator/experts execution model; `version` is `0.3.0` | OK | description now "...execute each task via a coordinator that delegates hands-on work to domain experts and reviews it before approval..."; `"version": "0.3.0"` unchanged (grep count 1); JSON parses |
| `CHANGELOG.md` has a new line under `## [Unreleased]` in user terms | OK | New `### Changed` line under `## [Unreleased]`: deliverable now written/committed by a dedicated implementation expert while the assistant stays on planning/review/approval; plain commits with one completion marker per task |
| `claude plugin validate ./rn --strict` and `claude plugin validate . --strict` both report success | OK | Both `✔ Validation passed`, exit 0 |

## Validation

- `claude plugin validate ./rn --strict`: ✔ Validation passed (exit 0)
- `claude plugin validate . --strict`: ✔ Validation passed (exit 0)

## QA Expert Review

| Aspect | Verdict | Evidence |
|---|---|---|
| Verifications meaningful to purpose (each edit removes its target stale-model statement; none missed) | OK | All 7 criteria re-verified independently against files on disk; grep confirms no "without involving an expert" / "1 task = 1 commit" remain; README mermaid/prose already consistent with the new model |
| Edge cases / integration (no lost directive in gm trim; bb reference resolves; README example consistent; CHANGELOG accurate) | OK | gm's trimmed directives all survive in template (`:18-25`, `:44-48`); bb's `steering-template.md` State reference resolves to real content (`:67-69`); README bb-commit example carries no marker and fits the simulated console; CHANGELOG line in user terms |
| Cross-file consistency across all 7 files + task-workflow.md | OK | README / gm / hi / bb / task-workflow / steering-template / push-and-review all agree on coordinator-delegates + one-marker-per-task |

## Overall Verdict

- Self-check: OK
- QA: OK (PASS — all 7 criteria OK, no additional defects)
- Ready for user review: Yes
