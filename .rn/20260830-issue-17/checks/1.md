# 1 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| every normative statement in the 13 files carries a judgement with grounds; a reader can take any line of any of those files and find it accounted for | OK | **Round 2, built on the round-1 file rather than regenerated.** 254 rows, each with a `file:line`, a verdict and grounds. Coverage re-derived by script instead of asserted: all 254 citations resolve inside their files (0 out of range), and every range's first and last line lands on content (0 endpoints on a blank line or heading, where round 1 had 15 over-runs). Walking the complement — every line of the 13 files no row's range covers — leaves only non-normative material: YAML frontmatter delimiters and `name:` fields, each file's descriptive lead paragraph, the fenced example blocks at `status-display.md:56-83` and the placeholder lines inside `steering-template.md`'s template block, table header and separator rows, `---` rules, and lead-in lines whose rules are cited on the lines below them (`steering-template.md:13`, `task-verify-workflow.md:161`, `pr-feedback-workflow.md:93`). `task-verify-workflow.md:6-136` is covered by rows 3-26, cited on `task-execute-workflow.md` because `diff <(sed -n '6,136p' …) <(sed -n '6,136p' …)` still returns no output — 131 lines. Per-file counts unchanged and still summing to 254 (41/32/36/37/13/11/16/26 + 4/14/11/7/6). Grounds quality: every cell was swept for the round-1 residue in which a replacement sentence was appended without deleting the sentence it replaced — 21 cells stated the same finding twice and now keep only the half naming the failed leg, and row 154, which restated its own rule back at itself, has real grounds. | NG | Coverage is dense and the row-range/file-range alignment holds for all 13 files, but the coverage *argument* is false: lines 4-135 are not byte-identical (true range 6-136, 131 lines; coordinator re-derived), so `task-verify-workflow.md:5` ("this one **second**") is accounted for nowhere. Four further statements carry no row: `task-execute-workflow.md:179`, `planning-workflow.md:19`, `:26-28`, and the prescribed criteria/steps content of `:36-37`. 15 grounds cells are bare negations that restate the verdict instead of explaining it. |
| the unenforced set is separated from the enforced set, so #2 has a definite list to work from | OK | **Enforced 89** — 13 `refuses` + 76 `recognized` — across four mechanism groups: A harness 5, B external command 8, C named reader 44 (13 coordinator / 18 user-at-gate / 13 PR reviewer), D later `rn` step 32; 5+8+44+32 = 89. **Unenforced 165** in nine groups: U1 33, U2 15, U3 10, U4 14, U5 14, U6 10, U7 26, U8 8, U9 35 = 165. Both partitions re-derived by script after every move (parse each row's verdict from the tables, parse each group's membership, compare sets, check each heading's declared count): no duplicates, no strays, nothing missing, 89 + 165 = 254. The boundary was applied uniformly this round rather than to the five rows the review named: all 80 `recognized` rows were re-tested against the three legs and 4 failed one — row 4 (its own grounds conceded "nothing compares them", and its fallback rested on row 39, itself `unseen`), row 42 (the coordinator is the breaching party, so leg (1) has no reader other than it; row 41, the adjacent bullet of the same step, was already `unseen` on that fact), and rows 147 and 238 (no reader named at all; row 213 is structurally identical and `unseen`). They move to U1, U7, U9 and U9. Row 2 keeps `recognized` on a mechanism that exists — the second task's unchecked box, read by `task-verify-workflow.md:215` and `status-display.md:19-22` — instead of "the PR the user reviews", which is the ground U9 exists to reject, and moves from group C to group D. Nine further rows that passed but stated their grounds loosely (16, 89, 104, 107, 121, 220, 221, 240, 252) now name all three legs, and the two applications of leg (2) that decide these cases are stated where the vocabulary is defined. Usability for #2: each group's paragraph of bare integers is now a table of id, `file:line` and what the rule says, so a worker starting on U1 reads 33 rules instead of chasing 33 numbers across 13 tables; all 165 citations bounds-checked. The one unenforced item outside the 165 — the missing sync rule for the shared span — is stated as a rule to write rather than a rule to rebuild, with the reason it has no row. | NG | The partition is exact and independently reproduced (U1-U8 = the 104 `neither` rows, no duplicates, no strays), but the boundary feeding it is wrong. 28 rows verdicted `visible` rest on nothing but "the user reads the message", which the document itself concedes after the `status-display.md` table; 10 rows verdicted `stops` rest on the assistant's turn ending, which happens only if the agent complies by asking. Row 145 sits in both the enforced set and U7. Row 169's grounds end "Nobody is asked to check." under a `visible` verdict. Six further `stops` verdicts (231, 217, 220, 224, 248, 159) were re-derived as wrong. The list the next task works from is short by roughly 35 rows. |
| every unconditionally mandated step has an answer to "what does this catch that the rest of the structure does not" — including the ones that turn out not to earn their place | OK | The section defined its scope as every step run "on every task or every session of its kind" and then inventoried 23 of them. It now inventories **61**, in five tables whose sizes the prose states and matches: 15 bind every build task (4 reviews + 11 others), 8 are planning's, 24 belong to the five command skills, 12 are the PR-feedback loop's, 2 run across everything. Coverage checked by script against `grep -nE '^[0-9]+\. \*\*' rn/skills/*/SKILL.md rn/references/planning-workflow.md rn/references/pr-feedback-workflow.md`: all **47** numbered mandated steps in those files now resolve to a citation inside the section (0 uncited), where before `dn` had 4 of 9 answered, `up` 1 of 8, `ty` 1 of 5, `gm` 1 of 5, planning 2 of 5 and `pr-feedback-workflow.md` 0 of 12. `up` step 4 — which runs on every resume, is `rn`'s only consumer of the completion marker, and is the step this document itself proves defective — now has its own entry with both halves of the answer. The scope definition is restated as *no discretion about the size of the change*, which is the question `steering.md` asks, so the per-medium Craft and Verification and the structure-conditional Design sit inside it without the contradiction the review found. The steps judged not to earn their place were scattered across six tables and never collected; the thirteen are now in one list with the row ids behind each, and the section closes on what the cost column adds up to rather than on a table row. | NG | The 17 analysed steps each carry a real "what only this catches" plus a cost judgement, and the non-earners are named honestly. But the set is asserted, not derived: the prose says thirteen per build task plus five per session (18) against 17 table rows, with no mapping back to the 249 rows, so a reader cannot check it. Unconditional steps absent from both tables: `task-execute-workflow.md:142` (Scope) and `:143-147` (Method), `dn/SKILL.md:35-39` and `:50-54`, the always-open escalation channel at `task-verify-workflow.md:194-200`, and migration's three reconciliation steps. Two "only this catches" answers are contradicted by the lines they cite: every expert receives the verbatim completion criteria, and Execute element 5 also reads criterion against artifact. |
| no row asserts a mechanism that does not exist — each named mechanism is quoted from the file that implements it | OK | Every claim was verified against source as it was drafted, and the round-1 residue swept. **Quotations**: all 494 quoted fragments were matched against the 13 source files, the 8 session directories and the 3 commit bodies they cite — all resolve. Four were altered and are fixed: row 197 said "resolve" where `pr-feedback-workflow.md:144-145` says "resolves"; row 59's `Round 3 (final, cap reached)` was lower-cased against `.rn/20260705-improve-design-template/checks/4.md:23`; row 141 and the triage entry had dropped the source's `**` emphasis that their neighbours keep; and row 36 now carries `893de07`'s body verbatim, including the parenthesis the earlier splice dropped. The sweep found three more — row 86's elision cut into a path token, row 210's "can never actually fire" quoted without the clause `on/SKILL.md:15` actually carries, row 236's paraphrase set in quote marks — all corrected. The two typographic conventions applied to every quotation (rejoining a fragment broken across source lines, setting a bare identifier in backticks) are stated where the method is. **By command, re-run this round**: `gh api repos/lovaizu/ccpm/rules/branches/main` returns a `pull_request` rule with `required_approving_review_count: 1` plus `non_fast_forward` and `deletion`, so row 98 rests on evidence and its 403 caveat is deleted (`git push --dry-run origin HEAD:main` reports success, so the dry run is not the test); the 40 past check files re-counted (39 `## Overall Verdict`, 16 `Ready to check off` so 24 lack it, 38 QA, 11 Craft, 5 Verification, 8 Design); Craft present in 5 of 14 `rn-lean` files and 6 of 6 in `20260705-improve-design-template`, which also writes a Craft step into 6 of its 7 tasks — so "ran neither in any of its 7 tasks" was false and U2 now separates the two axes; `grep -c '^### #.*DONE'` on `rn-lean` returns 15 against 16 task headings with exactly one `- [x]` at `:241`, correcting row 141 and reconciling it with row 112; unchecked boxes counted per session (5/3/2/1/1 across five of the seven past sessions), completing row 124; one Design sign-off task in 8 sessions and three Evaluation sign-off tasks; `Rn version:` in 2 of 8; `- **Status**: paused` only at `.rn/20260615-subagent-execution/steering.md:340`; `git log --all --grep='complete task #1' --oneline` = 16 and `chore: start session` = 11; `find rn -type f` = 17 with 0 executable; the old group B re-derived from `git show fb8413d` (17 rows, 10 removed, 1 added = 8, and 231 was never in it); the shared span is 131 lines, not 132. All ten false or self-contradicting statements the review named are corrected at every occurrence, the opening line included: it now says **self-enforces**, which is what the document's own definitions support. **Scope not covered**: the round-1 absence searches ("looked for a reader / an artifact / a trace, there is none") were re-read for consistency with the tightened legs but not re-executed as searches; and the two mechanisms whose absence was proven by running them (`gh browse <sha> -n`, `migration-workflow.md:47-48`'s claimed gate) were not re-run, their round-1 results standing. | NG | Three claims presented as verified are false or irreproducible, each repeated across the document: the byte-identity claim (4 occurrences, labelled "verified by `diff`"; true range 6-136); "24 of 40 check files have no `## Overall Verdict` at all" (7 occurrences; 39 of 40 carry the heading, 24 lack the `Ready to check off` line); and "`complete task #1` matches 10 commits" (3 occurrences; no scoping reproduces 10). Also wrong: "5 of 8 sessions have no Design sign-off task" (true value 7 of 8, the 5-of-8 figure belongs to the Evaluation sign-off), `migration-workflow.md:4` for a phrase on line 5 (3 occurrences), and several altered quotations. The `migration-workflow.md:46-48` phantom-mechanism finding is correct and was independently confirmed. |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | NG | **Round 3.** The skeleton re-derives exactly — 269 ids with no gaps, verdicts 12/49/208, both partitions exact, all 13 per-file counts, the 71-step split, and the `:65` claim that 27 rows moved (checked against `f62035f~1`). The failure is in the interface task #2 consumes. Four verdicts rest on mechanisms their cited files do not contain: row 188 (`refuses`) — coordinator re-ran the file's own query with a fabricated but existing number, `-F pr=1` exits 0 with real thread data, only out-of-range errors, so the loop proceeds on the wrong PR undeclined; row 99 (`refuses`) borrows row 72's mechanism, which declines a re-run, not a skipped or wrongly-titled PR; rows 195/201 (`recognized`) name the PR diff, but `pr-feedback-workflow.md:144-145` makes the reviewer open the reply, not the diff. Group C's largest bullet (18 rows, 9 more downstream) asserts the gate "exists to make them read `steering.md`", which only the plan gate does — the Evaluation gate's prescribed artifact is "the Acceptance criteria run result". Row 224's grounds state "which is why row 220 is recognized" while row 220 is `unseen`. Row 141 sits in U10 but its reader is read by no one, so U10's repair would not enforce it. Coverage gaps remain: `design-template.md:38` and `:35-36` uncited; `steering-template.md:87` unrowed while `:88-91` each have rows; granularity uneven (a 62-line template block yields 23 rows, a 50-line check-file block yields one). Step tables omit `gm` 3/4, the review prompt's elements 1–5, the 3-iteration cap, and session close. |

## Expert Reviews (axes the task needs)

### Craft Expert (writing)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | NG | **Round 3.** Register, structure and cell discipline improved; the defects are now stale figures left by round 3's own re-derivation, all in load-bearing summary sentences. The opening says U9 is "Thirty-five rows" (37) and U1 "Thirty-three more" (35) while its own total 72 = 37+35; `:1036` says "U1's 33"; `:826` says "the 27 rows of U10" (30), names nine ids while calling them eight, and only its "34" is right; `:967` says "these five steps" against `:955`'s "Six reader steps carry all 30"; `:1069` says "thirteen real refusals" (12); the closing section heads "Eighteen of the seventy-one" over 19 bullets and repeats 16/18 in three more places. `:535-537` puts row 63 in U10; the index puts it in U1. Repetition persists: the status-display closing claim, the U9/U10 antithesis, the `up/SKILL.md:26` grep defect (4 places) and the `20260705` Verification omission (5) each restated. Nineteen grounds cells open "Looked for …", which `.claude/rules/output.md` bans by name. The stated `…` convention contradicts itself and 129 of 208 index Rule cells do not follow it. Two lines run 169 and 175 chars against the document's ~100 wrap. |
| Consistency with existing style | NG | **Round 3.** Spelling and citation form now match the sources (`judgment` 25×, `judgement` 0×). Two problems remain. Register drifts back into aphorism and superlative — "the least recoverable rules in the plugin", "the plugin's single most load-bearing step", "It is the right step running on the wrong test", and a bolded maxim at `:1011`. And the document narrates its own revision history to a reader who never saw the earlier draft: "not a reader in this group any more" (`:532`), "Six consumers this document used to credit are gone" (`:549`), "until this pass" (`:1031`), "often cited here" (`:215`). It also crosses into task #2's side of the line, which `steering.md:135-136` assigns to #2 — prescribing devices at `:822-823`, `:966-969`, `:913-917`, `:485`, `:690`. |

### Verification Expert (fact-check)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | NG | **Round 3.** 500 quoted fragments machine-compared, 4 defects; 189 citations bounds-checked; ~35 numeric claims re-derived; 10 commands executed. The lead defect is that round 3's evidence-scoping fix introduced a new error of the class it was fixing: row 86 and the U6 note attribute the `Rn version:` rule to `2ddcafa` (2026-07-01) and call `rn-lean` "the one post-rule breach", but coordinator confirmed `git show 2ddcafa \| grep -c 'Rn version'` returns 0 — the rule entered `3b9603e` at 2026-07-05 23:16, after rn-lean's work, so post-rule breaches are 0. The document prescribes the command that returns the right answer. Row 124 claims each named task "carries a `complete task #{id}` marker commit all the same"; `git log --all --grep='complete task #15'` returns nothing and both `complete task #7` commits belong to other sessions — its own U2 says the opposite. Row 264 says rn-lean's steering "runs to 350 lines"; `wc -l` returns 383. Row 265 quotes `### #N: <task name>` at `steering-template.md:57`, which reads `### #1:`. Rows 93/261 cite `.rn/20260625-rn-lean/steering.md:78` as that session's fold-in record; coordinator confirmed the line is a general statement of the doc-division rule and rn-lean carries a `Design:` line — this citation came from the coordinator's own work-order and was wrong. |
| Coverage (edge cases / claims / steps) | NG | **Round 3.** Source coverage is near-complete and the partitions are exact — 269 rows, 12/49/208, A5+B7+C39+D10 = 61, U1–U10 = 208, 207 of 208 index citations matching their table cells, the 71-step split reconciling. Two source gaps: `design-template.md:38` carries a normative statement with no row and no citation, and granularity is uneven across the skills — the `:9` role line gets a row in `dn` (213) and `ty` (238) but not in `on`, `up` or `gm`, though `gm/SKILL.md:9` states the argument-routing rule row 184 attributes to that file. The remaining failures are internal-consistency ones: eight mutually inconsistent counts, which the last commit ("account for the new rows in each group summary") attempted and did not complete. |

## Overall Verdict

- Self-check: OK
- QA: NG
- Craft expert: NG
- Verification expert: NG
- Ready to check off: No — third fix round complete, the workflow's 3-iteration cap is reached, and all three reviews still return NG. Escalated to the user with the unresolved items per `task-verify-workflow.md:182-183`. The document's skeleton is sound and independently re-derived by all three reviewers; the residue is (a) ~12 boundary rows whose verdict rests on a mechanism its cited file does not contain, (b) eight stale counts left by round 3's own re-derivation, (c) one new rule-era misattribution introduced by round 3's evidence-scoping fix, and (d) writing defects — repetition, revision-history narration, and prescriptions that belong to task #2.
