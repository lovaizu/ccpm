# Task #9 self-check — dogfood findings → SKILL.md minimal additions

Method: `grep -F` each addition's distinctive phrase in `hposal/skills/up/SKILL.md`; confirm phase
structure unchanged; run `claude plugin validate` (plugin + marketplace).

## Completion criteria

1. **All 12 findings (+#19 SKILL side) added, zero misses** — OK.
   - #1 ⑥ SEO/301 standard topic / #2 subdomain-source 301 feasibility / #3 toC legal pages /
     #5 small-engagement omit-or-stage / #7 template≠cheap trap / #8 contract form is human's call /
     #9 03 two-aggregation machine reconcile (completion criteria) / #14 denominator≠new-site size /
     #15 fill-marker `（…）`/`※記入` convention / #17 sourcing words are internal /
     #18 platform-reality gate / #19 common-effort-once + per-basis branch / #20 .com vs .org gate.
   - All 13 grepped `OK`.
2. **Structure/章立て unchanged (additions only)** — OK. `grep -c '^## Phase'` = 4 (unchanged);
   edits are all inside existing rules / ⚠️ blocks / completion criteria, no phases or headers
   added/removed.
3. **CHANGELOG `[Unreleased]` has the line; `validate --strict` (plugin + marketplace) passes** —
   CHANGELOG: OK (added under `### Changed`). validate: see task log.

## Notes
- #4 and #10–#13, #21, and the #19 *template*枠 are out of scope here (template改修 = task #10).
