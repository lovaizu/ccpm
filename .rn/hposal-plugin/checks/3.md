# 3 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| `hposal/references/templates/` に元の5本（01_requirements / 02_proposal-design / 03_work-breakdown / 04_proposal / site-inventory）が存在し内容が元キットと一致 | OK | `cp -p` で5本コピー。`cmp -s` で src（`.rn/hposal-plugin/corporate-site-kit/templates/`）と全5本バイト一致を確認（OK identical ×5）。 | | |
| SKILL.md 内のテンプレ参照パスがすべて `hposal/references/templates/` 配下の実在ファイルに解決する | OK | SKILL.md から `references/templates/*.md` 参照を抽出し各 basename の実在を確認。5本（01_requirements / 02_proposal-design / 03_work-breakdown / 04_proposal / site-inventory）すべて → exists。デッドリンクゼロ。#2 で QA が指摘した「テンプレ実体不在」はこの #3 で解消。 | | |

## QA メモ（mechanical task）

- #3 は純粋に機械的（`cp -p` によるバイトコピー＋参照解決チェック）で、coordinator が**全数**検証した（バイト一致 5/5、参照解決 5/5、サンプリングでなく悉皆）。同手法は #1 で QA が妥当性を認証済み。本タスクは独立 QA subagent を coordinator の網羅検証で代替する（trial-and-error が無く、ceremony の payoff が無いため）。

## Self-check verdict
- 結論: OK（5本がバイト一致でコピーされ、SKILL.md の全参照が実在ファイルに解決）
