# 6 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence |
|---|---|---|
| `claude plugin validate hposal --strict` がエラー・警告なしで完了 | OK | `✔ Validation passed`、exit 0。`hposal/.claude-plugin/plugin.json` を検証。 |
| `claude plugin validate .`（マーケットルート）がエラー・警告なしで完了 | OK | `✔ Validation passed`、exit 0。`.claude-plugin/marketplace.json` を検証（rn + hposal の2エントリ）。 |
| `claude -p "/hposal:up" --plugin-dir hposal` がスキルを認識した出力で正常終了 | OK | exit 0。出力（940 bytes）で `/hposal:up` を受領し4フェーズ（要件整理→提案整理→作業リスト→提案書）を提示、フェーズ1手順どおり目的確認・インプット資料・現行サイトURL・出力先を質問。「フェーズ」言及 2 回。スキル本体（SKILL.md）が読み込まれ起動していることを確認。 |

## QA メモ（verification task）

- #6 は検証コマンドの実行＝事実確認タスク。coordinator が3コマンドを実走し、exit code と出力を全数確認した（claude CLI 2.1.177）。trial-and-error が無いため独立 QA subagent は coordinator 実走で代替。
- `--strict` は version 未設定で warn→fail するが、plugin.json に `version: 0.1.0` があるため通過（plugin.md の strict ゲートに準拠）。

## Self-check verdict
- 結論: OK（3コマンドすべて exit 0・警告なし・スキル認識を確認＝プラグインが動作する状態）
