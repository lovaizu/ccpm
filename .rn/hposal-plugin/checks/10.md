# Task #10 self-check — proposal template → outline + parts (1 part = 1 slide / D-9)

Method: list `parts/`; build a representative assembly (single site + 2-basis, 3-layer) in the
scratchpad; run the export gate, headless-Chrome 16:9 export, `pdfinfo`, and PNG eyeball; `diff` the
CSS against the git monolith; `grep` for stale `04_proposal.html` refs; `claude plugin validate`.

## Completion criteria (from steering.md task #10)

1. **`parts/` holds 1-slide-per-part with the dogfood variants selectable; CSS shared once,
   byte-identical to the monolith.** — OK.
   - 26 slide parts present + `_head.html` + `_foot.html`. Variants: as-is-to-be `single`/`multi`;
     screen `service-cta`/`search-filter`/`tree-nav`; estimate `total`/`range`/`compare`; 3rd-layer
     2-column `compare-basis`/`compare-build`/`compare-operation`. `work-detail.html` added (26th part).
   - CSS byte-identical: `diff <(git show 4f65909:…04_proposal.html | sed -n '6,374p') <(sed -n
     '6,374p' parts/_head.html)` = **empty** (DIFF EMPTY).

2. **`04_proposal.md` is an outline (3 layers, slot order, variant selection, layer→layout,
   assembly-time numbering).** — OK. Rewritten: 背骨 3-layer table (1列/2列 by layer); slot-order
   tables per layer with the variant for each slot; explicit 2-layer-skips-3rd-layer rule;
   `.pg` assigned at assembly; cross-refs are `{{頁}}` placeholders; 必須/任意 reframed as part
   selection (no slide-delete + renumber).

3. **SKILL.md Phase 4 drives "pick variant → concatenate → fill → export"; Phase 2 has the #4
   basis-comparison block.** — OK.
   - Phase 4 intro + step 1 rewritten to the parts/outline model; skeleton paths → `parts/`;
     line-159 ⚠️ reworded (why-us mandatory, post-launch-care/contact optional = don't concatenate,
     `.pg` at assembly); residue grep gate, fill-marker, headless-hang, rasterize, single-file,
     value-first, through-line, sample-labels, jargon, burn-down, amount/contract all preserved.
   - Phase 2 step 1 adds the #4 ⚠️ (undecided basis → 2–3 candidates on same axes: initial / running /
     who-updates / extensibility / maintenance hands-off → client chooses at ★; cross-refs the
     estimate-side common-effort+per-basis ⚠️, no duplication).
   - Templates-mapping table phase-4 row updated to the parts/outline model.

4. **Representative assembly → single HTML → headless-Chrome 16:9 export not broken, export gate
   clean.** — OK.
   - Assembly (20 parts: single + 3-layer compare + estimate.compare + appendix incl. work-detail)
     = 20 `<section class="slide">`.
   - Export gate on the FILLED throwaway: `grep -c '{{'` = **0**, `grep -c '<!--[^>]*例'` = **0**.
   - `pdfinfo`: **Pages: 20**, **Page size: 960 x 540 pts** (exactly 16:9).
   - PNG eyeball (service-cta, compare-basis/build/operation, estimate.compare, work-detail):
     no breakage — 2-column tables intact, footers correct (4/20 … 20/20), `（提案者）` human-marker
     preserved, `.idc` ID column hidden in print. Minor: estimate.compare's bottom note sits close to
     the footmark with my fuller-than-real sample text — same absolute-position behavior as the old
     monolith (CSS byte-identical), not a parts regression; tuned at fill time.

5. **CHANGELOG `[Unreleased]` has the line; `validate --strict` (plugin + marketplace) passes.** — OK.
   - CHANGELOG: line added under `### Changed` (deck assembled from selectable parts; variants/
     comparison chosen not hand-edited; pages auto-numbered; stylesheet unchanged).
   - `claude plugin validate hposal --strict` → ✔ passed (exit 0).
   - `claude plugin validate . --strict` → ✔ passed (exit 0).

## Fix round (QA found two MED defects)
- **#13 now covered** — added `⚠️ 内容5〜6行＋リード2行が収まる目安。超えるならリード短縮 or 表を分割（#13）`
  to the `╔═ PART:` header of BOTH `03-as-is-to-be.single.html` and `03-as-is-to-be.multi.html`
  (`grep -l '#13'` matches both). Closes the `.dense` table footer-overflow gap.
- **CHANGELOG `[Unreleased]` self-contradiction fixed** — the two Added lines that described the
  template in the old monolith model are rewritten to the parts-based deck as it actually ships
  (assembled from selectable parts, variant per slot, comparison across a layer, mandatory why-us +
  optional post-launch-care/contact only in scope, auto page numbering, consistent stylesheet); the
  redundant task-#10 Changed line ("now assembled … instead of editing one big template") removed
  (a never-released plugin has nothing to change *from*). The dogfood-guardrails Changed line kept.
- Re-validate: `validate hposal --strict` ✔, `validate . --strict` ✔.

## Coordinator review (verdict)

- **QA expert review** — **PASS-with-notes** (initial deliverable `b70f9a3`). Two MED findings: (a)
  dogfood #13 (`.dense` as-is/to-be footer-overflow note) missed; (b) CHANGELOG `[Unreleased]`
  self-contradiction (monolith Added lines vs parts Changed line). Both fixed in the fix round
  (`87787be`); QA confirmed all other completion criteria, dogfood #4/#10/#11/#12/#19/#21 addressed,
  CSS byte-identical, 11/11 Phase-4 ⚠️ preserved, no PII, validate ✔✔.
- **Independent coordinator spot-check (after fix round)** — CSS diff (monolith 6–374 vs `_head.html`)
  empty ✓ / monolith deleted, no source-template refs (only assembled-output filename) ✓ / 26 parts,
  4 Phases intact ✓ / #13 note present in BOTH as-is/to-be parts ✓ / CHANGELOG `[Unreleased]` reads as
  one net parts-based state ✓ / `validate hposal --strict` ✔ / `validate . --strict` ✔.
- **Overall verdict**: Self-check OK · QA PASS (after fixes) · **user review pending on PR #8** (final
  gate, per push-and-review). The `complete task #10` marker rides on the post-approval check-off.

## Notes
- Old monolith `04_proposal.html` `git rm`'d. Remaining `04_proposal.html` mentions in SKILL.md
  (lines 11, 25, 167) refer to the **assembled output file name** (the deliverable), not the deleted
  template — intentional. CHANGELOG line 12 is a historical [Unreleased] entry — left untouched.
- Hardcoded `P.N` strings in `_head.html` are CSS comments (byte-identical to monolith); not touched.
  `premises.html` cross-ref already uses `{{頁}}`; `02-read-guide.html` uses `P.{{頁}}` — both
  assembly-time placeholders, no hardcoded cross-page numbers in any part body.
