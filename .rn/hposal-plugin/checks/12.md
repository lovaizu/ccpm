# Task #12 (round 1 = apply followups) self-check — reflection-plan followups + minor items applied

Round-1 scope: apply the dogfood reflection-plan `followup` items plus the adjacent `minor` items
(F1, F2, F4, F5, F6, F7, F8, F9, F11, F12, F13, F14) to SKILL / templates / parts / outline,
additive-only. Re-dogfood and the convergence loop are later steps of #12, out of this round's scope.

Method: edit each target file; grep-verify each finding; `diff`-confirm `_head.html` CSS unchanged;
assemble `_head` + each new part + `_foot` as a smoke test; run both `claude plugin validate --strict`.

## Completion criteria

| # | Criterion | Self-check | Evidence | QA | QA-Evidence |
|---|---|---|---|---|---|
| 1 | Reflection-plan followups (F1,F2,F4,F5,F6,F7,F8,F9,F11,F12,F13,F14) applied across SKILL / templates / parts / outline | OK | grep on each: F1 `No stated count is the normal case`=1, F2 `fan out into more than one q`=1, F4 `Anchor-type single-page sites are the sharp case`=1, F5 `managed-SaaS basis's monthly price varies by billing terms`=1, F7 `not double-counted … per-row`=1, F9 `progressively dropping / staging rows`=1 (all in `SKILL.md`). F6 `移行 標準作業チェックリスト`=1 + `プラットフォーム実態ゲート`=1 (`02_proposal-design.md`). F8 `共通1回＋基盤分岐＋基盤ごと総額`=1 + `役割別突合表`=1 (`03_work-breakdown.md`). F11 `work-detail.common.html` + `work-detail.branch.html` created (pre-split pair mirroring `estimate.*`), original `work-detail.html` kept. F13 `高さ予算（#13）`=1 (`estimate.compare.html`). F12 `組み立て時に埋める`=1 + F14 `一括置換を1回`=1 + F11-slot20 `共通フェーズ1回`=1 (`04_proposal.md`). | | |
| 2 | Existing phase structure, chapter ordering, existing parts unchanged (additive only); `_head.html` CSS byte-identical | OK | `git status` shows `_head.html` NOT modified (CSS untouched → byte-identical). `^## Phase `=4 in SKILL.md (4 phases intact). Outline slot rows unchanged (slot 20 still present, only its variant cell expanded). No existing part deleted/renamed; only 2 new parts added + estimate.compare header comment extended. All edits are additions to existing structure. | | |
| 3 | `claude plugin validate hposal --strict` passes; `claude plugin validate . --strict` passes | OK | `validate hposal --strict` → `✔ Validation passed` (exit 0). `validate . --strict` → `✔ Validation passed` (exit 0). Both run live. | | |
| 4 | CHANGELOG `[Unreleased]` has the corresponding lines | OK | `[Unreleased]` Added: 3 new lines (proposal-design checklist+gate slots, work-breakdown multi-candidate+role-recon slots, `work-detail.common`/`.branch` part pair). Changed: 2 new lines (sharpened edge-case guidance; title/page-count/height-budget notes). Keep-a-Changelog format, user-benefit phrasing matching existing style. | | |

## Smoke test (new parts)

Assembled `_head.html` + `work-detail.common.html` + `work-detail.branch.html` + `_foot.html` into one
file: `<section>`/`</section>`=2/2, `<html>`/`</html>`=1/1, `<body>`/`</body>`=1/1, exactly one `<style>`
block with `:root{` and `.slide{` (shared CSS referenced, not redefined). The new parts use only
existing classes (`.slide`, `.t`, `.estd-t`, `.apx`, `.catrow`, `.idc`, `.brandbar`, `.eyebrow`, `.ttl`,
`.lead`, `.body`, `.footmark`, `.pg`) all defined in `_head.html` — no new CSS introduced. `xmllint
--html` flags only "Tag section invalid" (HTML4 DTD doesn't know HTML5 `<section>`; not a defect; the
shipped parts have always used `<section>`). Well-formed assembly confirmed.

## Overall verdict

- **Self-check**: OK — all 12 findings applied and grep-verified; additive-only confirmed (`_head.html`
  CSS untouched, 4 phases intact, no existing part deleted); both `validate --strict` pass; CHANGELOG
  `[Unreleased]` updated; new parts assemble well-formed against the shared CSS.
- **QA**: (pending)
- **Design expert**: (pending)
- **Craft expert**: (pending)
- **Verification expert**: (pending)
- **Ready to check off**: No — criteria reconciled (rn 0.8.0 migration, 2026-09-05), self-check/review
  must re-run. This round-1 self-check was written against the pre-migration Completion criteria; the
  reconciled criteria in `steering.md` #12 (per-finding closure traceability, phase 1→4 completion of the
  final round, regression absence, PII absence, reachability intact) are not covered by the table above.

## Round 2 (apply-round-2) self-check

Round-2 scope: apply the round-2 dogfood findings (G1, G2, G3, G4+H3, H1, H2, H4, J1, J2, J3, J4) to
SKILL / templates. G5 deliberately not applied (judged low value). `parts/` and `04_proposal.md` are
deferred (the finding round did not reach Phase 4). Re-dogfood is a later step of #12, out of scope.

Method (section 4, applied as written): each SKILL-mirroring slot was drafted **after** reading the
mandating SKILL text — G1 against `SKILL.md` Phase 1 "Standard topics to confirm with the human before
★" (①–⑥) plus "Internal estimate inputs (gather once, before phase 2)" (day rate / direction % /
subcontractor tax / staffing); H1 against `04_proposal.md` slot 8 `process` (必須) + Phase 4's
mandatory/optional part rule; J1 against `SKILL.md` Phase 3 step 1's responsibility-matrix ⚠️ (rows
〔301 map / tests / QC / extract & load〕 × 〔subcontractor / us / client〕, person-days per cell, blank
cell = red flag, QC not double-counted). Before each removal/replacement the whole `hposal/` tree was
grepped for other references (`要件化`, `w4`–`w7`, `年額`, `公開後`, `TBD`).

| # | Criterion | Self-check | Evidence |
|---|---|---|---|
| 1 | All 10 followups present in their named target file and grep-findable | OK | `SKILL.md`: H2 `separate numbering`=1 (Phase 4 step 1 ⚠️), J4 `scope band`/`presentation band`=3 hits (Phase 3 step 3, two bullets + the new ⚠️). `01_requirements.md`: G1 `★ 人間レビュー＝ゲート`=1 (new final section: ①–⑥ table + internal-premise table + ★判定), G2 `要件化`=**0** (column gone), G4+H3 `母数（件数の正）`=1 (4 separately-labelled rows: 現行/移行/転送(301)/新サイト). G3: the `移行` enum lives **only** in `site-inventory.md:15` (`grep -rn TBD hposal/` → 1 hit, not in `01_requirements.md`), so `作り直し` was added there and its counting rule mirrored in 01's 母数 note (`grep -c 作り直し` = 1 in each). `02_proposal-design.md`: H1 `進め方・期間（スケジュール）`=1 (new `## pN` slot in the story-slot shape 実現方法/根拠), H4 `ランニング費の正`=1 (sub-bullet under the 年額・運用費 bullet). `03_work-breakdown.md`: J1 `移行の責任分担マトリクス`=1, J2 detail rows now `w4 問い合わせフォーム（フォーム1つ＝1行）` + `w5 一覧の検索・絞り込み` (rest renumbered w6–w8), J3 `標準項目チェックリスト`=1, J4 `スコープ段階バンド`/`提示バンド`=4 hits. |
| 2 | Existing structure preserved (no phase, section, ⚠️ or ★ lost); additive or the named in-place replacements only | OK | `grep -c '^## Phase' SKILL.md`=4 and `★ Human review = gate`=5 — unchanged. `⚠️` count 37 → 39 (two added, none removed). `git diff SKILL.md` deletes exactly 2 lines, both the J4-named in-place bullet replacements (the small-engagement range bullet and the built-up-vs-presented rounding bullet), each re-emitted with all prior content intact. Heading `diff` per template (`git show HEAD:<f> | grep '^#'` vs current) shows **additions only**: 01 +母数 +★ゲート, 02 +pN process slot, 03 +標準項目チェックリスト +責任分担マトリクス +レンジのバンド. The only other in-place changes are the two named ones: 01's `要件化` column removal and 03's bundled sample row split. |
| 3 | De-duplication findings (G2, H4) remove the duplicate rather than adding a third statement | OK | G2: the `要件化` column is **deleted** from the i-ledger header and both sample rows; `grep -rn 要件化 hposal/` → **0 hits** repo-wide, so nothing still instructs filling it (checked in `SKILL.md`, `README.md`, all templates and parts). The i↔r link is now one-directional in r's `出自`, stated once in 01's ledger intro. H4: `grep -rn 公開後\|年額 hposal/` (outside `parts/`) confirms the running-cost category is stated **only** in 02's 見積モデル `年額・運用費` bullet — the templates carried no restatement to delete, so the fix is a cross-reference rule attached to the owning bullet ("elsewhere write the amount only, reference this field, don't restate the category"), i.e. one home plus a pointer, not a second statement. |
| 4 | Both `claude plugin validate --strict` runs pass | OK | `claude plugin validate hposal --strict` → `✔ Validation passed`, exit 0. `claude plugin validate . --strict` → `✔ Validation passed`, exit 0. Both run live in the worktree after the edits. |
| 5 | CHANGELOG `[Unreleased]` carries user-facing lines for the user-visible changes | OK | 4 lines added under `Added` (phase-1 review-gate slot; per-denominator fields; schedule story slot; work-breakdown matrix + standard-item checklist + two-band table) and 4 under `Changed` (duplicate link column dropped; "rebuilt" migrate value; the two range bands named apart + story vs slide numbering; sample rows split). Keep-a-Changelog format, `<what changed> — <why it helps the user>`, no version bump in `plugin.json`. |

Consistency touches made deliberately (derived from the findings, recorded rather than silent):
`02_proposal-design.md` line 6's `p_n` definition said "（提案書の章と同番号）" — the exact collision H2
names — so it now points at the SKILL's Phase 4 rule instead of asserting the same number; and G3's enum
fix landed in `site-inventory.md` (where the enum actually is) rather than `01_requirements.md`, with the
counting consequence written into 01's new 母数 note.

- **Self-check**: OK — 10/10 followups landed and grep-verified in their named files (G3 in the file
  that actually holds the enum, cross-referenced from 01); structure additive except the two named
  in-place replacements plus the two named removals/splits; G2 removes the duplicate column outright
  and H4 leaves a single owner plus a pointer; both `validate --strict` pass; CHANGELOG updated with
  8 user-facing lines and no version bump.
