# 2 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence |
|---|---|---|
| SKILL.md が存在し name/description の有効な frontmatter を持つ | OK | `hposal/skills/up/SKILL.md` を新規作成。frontmatter は `name: up`／`description`（日本語・D-2準拠で SKILL.md は日本語維持）／`disable-model-invocation: true`（rn 3スキルに倣い副作用ワークフローのため自動起動を抑止）。description は hi/SKILL.md と同粒度の一文で「何を＋いつ（/hposal:up 経由）＋★ゲート＋副作用」を網羅。 |
| 4フェーズ・各完了条件・全⚠️・★ゲートが漏れゼロ | OK | 原本 workflow.md と機械突き合わせ。フェーズ 4/4（F1〜F4 見出し OK）。★ゲート 5/5（フェーズ末4 + 「フェーズの回し方」step3 内の定義1。coordinator の「4/4」は末尾ゲートのみの数え方で、定義行を含めると5）。共通ルール 9/9（`- **` 箇条を awk で計数）。⚠️ 27/27〔F1=6, F2=5, F3=6, F4=10〕。完了条件 4/4（diff IDENTICAL）。`diff` で ⚠️＋★ゲート行・完了条件行は原本と完全一致。「たどれるようにする（1枚絵）」1/1、「フェーズの回し方」「出力言語」注記、3行目「考え方・落とし穴は別ファイルにせず」方針、F1 末「★前に必ず人間へ確認する定番論点リスト」もすべて保持。 |
| テンプレ参照が references/templates 配下の実在予定パスを指す | OK | 5本すべて `${CLAUDE_PLUGIN_ROOT}/references/templates/` 配下：01_requirements.md / 02_proposal-design.md / 03_work-breakdown.md / 04_proposal.md（原本 04_proposal.html の骨格）/ site-inventory.md（原本 inventory/<site>.md）。各フェーズの「出力」行に「骨格＝…をコピーして記入」を付し、依存図の直後に「## 記入用テンプレート」表で5本の対応を列挙（中身の説明・要約なし＝ポインタのみ）。 |

## ⚠️ 件数の検証メモ（原本を自分で数え直し）

- 原本 `workflow.md` の ⚠️ グリフ総数 = **27**（coordinator 値 26 と1件差）。
- 内訳：F1=6, F2=5, F3=6, **F4=10**（coordinator 値 F4=9 と1件差）。
- 差の原因：F4 の line 119「燃やし切りチェック」本文中の `✅カバー済／⚠️一部／❌欠落` の凡例グリフ ⚠️ を、coordinator は「落とし穴の⚠️」として数えず除外（凡例として9）。真の落とし穴コールアウト ⚠️ は F4=9（lines 113-117, 120, 122-124）、凡例グリフを足すと10。
- 同様に ★ゲート：coordinator「4/4」は各フェーズ末の4ゲートのみ。「フェーズの回し方」step3 内のゲート定義行（`3. **★人間レビュー＝ゲート**。…`）を含めると総出現は5。
- 方針：いずれも原文の文言・グリフを1件も削らず逐語保存しているため、どちらの数え方でも漏れゼロ。本チェックでは全グリフ（⚠️27 / ★5）を保存済みとして報告する。

## QA Expert Review（subagent・敵対的）

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Meaningful tests/verification（目的＝逐語の忠実性に即した検証か） | OK | 両ファイル全行を読み、構造要素を grep/awk で実数カウント、本文一致を difflib でバイト比較、テンプレ実在を filesystem 確認、frontmatter を構造検証。検証手法が目的（無漏れ・無ドリフト）に即している。 |
| Edge case coverage（境界・抜け） | OK | frontmatter の壊れ YAML・全角コロン・閉じ忘れを否定。⚠️/★/完了条件/共通ルールの実数を原本と突き合わせ全一致（⚠️27〔F1=6,F2=5,F3=6,F4=10〕／★5／完了条件4／共通ルール9）。本文はバイト一致。README 本文混入・テンプレ中身要約の混入なしを確認。 |

### QA 指摘とトリアージ（coordinator がバーに照らして判定）

- **[QA: 致命] テンプレ実体不在 → 全テンプレ参照がデッドリンク（基準3 を NG と判定）** → **トリアージ: スコープ外（#3 の成果物）。#2 の欠陥として却下。**
  - 根拠: テンプレ5本の `hposal/references/templates/` への配置と「SKILL.md の参照がすべて**実在ファイルに解決する**」検証は、タスク **#3** の Steps と完了基準に明記。#3 は #2 を prerequisite に持つため、#2 時点でテンプレ実体は物理的に存在し得ない。#2 の基準3「参照が…配下の実在パスを**指している**」は参照文字列が正準の場所を正しく形成していることを指し、QA 自身がチェックリスト4で「パス対応・綴り・誤対応ゼロ」を OK 判定済み。よって #2 のバーは満たす。QA の事実観察（未配置）は正しく #3 に実作業が残ることの裏づけだが、タスク境界を渡していないための見え方であり #2 の NG ではない。
- **[QA: 軽微] パスが「記入用テンプレート」表と各フェーズ出力行で二重露出（DRY）** → **トリアージ: 維持（却下）。**
  - 根拠: 実行時に上から読む skill では各フェーズ行に直接パスがある利点があり、原本も依存図＋各フェーズ入出力を併記する流儀。表と本文のパスは一致し実害なし。意図的・低ドリフトリスク。
- **[QA: 観察] `disable-model-invocation: true` の追加／description の `/hposal:up` 案内** → 基準違反でない（rn 3スキルに倣う妥当な追加。コマンド定義の有無は本タスク範囲外）。

## Self-check verdict
- 結論: OK（全 criterion クリア。⚠️ は coordinator 値 26/F4=9 に対し実数 27/F4=10 だが、これは F4 line 119 の凡例グリフ ⚠️ を数えるかどうかの差であり、文言は原本と完全一致で漏れはない）

## Overall Verdict
- Self-check: OK
- QA: OK（致命指摘1件はトリアージで #3 スコープと判定し却下。本文バイト一致・無漏れ・無ドリフトを QA も確認）
- Ready for user review: Yes
