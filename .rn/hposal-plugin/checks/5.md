# 5 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence |
|---|---|---|
| `.claude-plugin/marketplace.json` に `source: ./hposal` を持つ `hposal` エントリが存在し JSON 妥当 | OK | `python3 -m json.tool` パース成功。`plugins` 内に `{name: hposal, description:（英語）, source: ./hposal, category: development}` を確認（version は記載せず＝plugin.md「version は plugin.json の1か所だけ」に準拠）。 |
| root `README.md` の Plugins 一覧に `hposal` が `./hposal/README.md` リンク付きで載る | OK | README.md 8行目に `- **[hposal](./hposal/README.md)** — *HP proposal.* …` を確認。rn と同じ箇条体裁。 |

## QA メモ（mechanical task）

- #5 は marketplace.json への構造化エントリ追加と root README の1行追加で、trial-and-error が無い。coordinator が JSON パース・エントリ実体・README リンクを全数確認した。marketplace.md（2か所登録・同期）に準拠：marketplace.json と root README に同一変更内で hposal を追加。category は rn と同じ `development`（実証済みで validate を通る値）。
- 構造検証（`claude plugin validate`）は #6 で実施。

## Self-check verdict
- 結論: OK（2か所に hposal を同期登録。marketplace.json は JSON 妥当・source ./hposal、root README はリンク付き）
