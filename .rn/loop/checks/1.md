# 1 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence |
|---|---|---|
| State file exists with ctx% matching display, non-empty surface id + repo cwd | OK | `~/.rn/ctx/poc-evidence.json` written by spike. Display = `C:25% 5h:42% 7d:8% \| O4.8/h/200k \| loop@worktree-loop`; file `context_pct: 25` equals the displayed `C:25%`. `surface_id: "poc-evidence"` (non-empty), `repo_cwd: "/Users/kiyo/work/lovaizu/ccpm/.claude/worktrees/loop"` (non-empty). Also records `raw_token_total: 50000` (= 12000+3000+5000+30000), `context_window_size: 200000`, `workspace_id: "ws1"`, `ts: 1781501444`. JSON validated by `jq -e .`. |
| Display byte-for-byte identical to original | OK | `cmp /tmp/orig.out /tmp/spike.out` for same fixture+env → no output (IDENTICAL). Both emit `C:25% 5h:42% 7d:8% \| O4.8/h/200k \| loop@worktree-loop`. Write block is appended strictly AFTER the display `printf`. |

## Live A1 proof (coordinator step — real Claude Code render)

Pointed the live `~/.claude/settings.json` `statusLine.command` at `loop/statusline.sh`, let a real CC render fire, then reverted. Result: `~/.rn/ctx/F73F5BA5-6A6E-445C-B7F7-394B15D4A65F.json` (this session's actual `CMUX_SURFACE_ID`) was written by the CC-spawned statusline with `context_pct: 30`, `raw_token_total: 59634`, `workspace_id: 2D9B0BFD-...` (real), `repo_cwd: /Users/kiyo/work/lovaizu/ccpm/.claude/worktrees/loop`. This proves **A1**: the statusline spawned by Claude Code inherits `CMUX_SURFACE_ID` and can stamp the target surface. Bonus: 5 distinct per-surface files appeared, one per live cmux pane — confirms per-surface isolation under the global setting. Settings.json restored to the original afterward.

## Review fixes applied (producer-of-a-poller-contract hardening)

The write block is the producer side of a poller contract: an external watcher repeatedly reads `~/.rn/ctx/<surface>.json`, so it must be concurrency-safe and never emit a misleading value. Each fix below is mapped to its evidence (actual output).

| Fix | Self-check | How addressed + evidence |
|---|---|---|
| 1. Atomic write | OK | Build into `~/.rn/ctx/.<id>.json.tmp.$$`, then `mv -f` into place (atomic rename on one fs); temp removed if build or mv fails. Evidence: test (e) asserts no `*.tmp.*` left behind (`tmpleft=0`); manual sweep `ls -a ~/.rn/ctx | grep -c tmp → 0`. |
| 2. Never write a misleading value | OK | Write skipped unless `c_int` is a non-empty integer (`case "$c_int" in '' / *[!0-9]*) return 0`). Evidence: test (d) empty stdin → display `C:% \| ...`, **no file written**, exit 0 — was the fail-before case (old script wrote `context_pct:0`). |
| 3. Crash-proof JSON build | OK | Every `--argjson` is coerced twice: pre-coerced via `(... \| tonumber?) // 0` and again inside `jq -n` (`tonumber? // 0`). Evidence: `jq -n --argjson x '"big"' '{c:($x\|tonumber?//0)}'` → `{"cws":0}` (no abort), vs bare `//0` passing `"big"` through. Test (e) feeds `"context_window_size":"big"`: exit 0, no crash, prior good file preserved + valid JSON. |
| 4. raw_token_total honesty | OK | jq emits `null` when `current_usage` absent, real sum when present; `context_pct` still reuses `$c_int`. Evidence: test (b) happy path `raw_token_total=50000`; test (g) fallback fixture (`used_percentage`, no `current_usage`) → `context_pct=32` (real), `raw_token_total=null`. |
| 5. Sanitize surface id | OK | `case` rejects ids matching `*[!A-Za-z0-9_-]*` (UUIDs pass; `/`, `..` rejected). Evidence: ids `a/b`, `../x` → exit 0, **no file**, no `~/.rn/ctx/a` escape dir; `ok-UUID_123` → file written. Test (f) asserts no leak in or outside ctx dir. |
| 6. Preserve exit status | OK | Write runs in `write_state()` ending `return 0`, called as `write_state \|\| true`; placed after the display `printf`. Evidence: test (h) forced failure (`HOME=` unwritable `chmod 000` dir) → **exit 0**, display byte-identical; (d)/(e)/(f) all exit 0. |
| 7. Document the schema | OK | Comment block above `write_state` lists each field with type/meaning (incl. `raw_token_total number\|null`); explicitly no `schema_version` / ms timestamps. |

Branches/paths the test now exercises: happy path (write), degradation (unset id), malformed/empty input (skip), non-numeric size (coerce + atomic preserve), invalid id × slash/dotdot (skip), older-client fallback (`used_percentage` branch → null raw). Display computation unchanged (lines 1–83 byte-for-byte == live original, verified by `od -c` cmp on two fixtures).

## Review fixes — iteration 2 (residual Low/Info findings from the re-review)

All three experts PASSed iteration 1; the items below were the remaining Low/Info findings, triaged and resolved.

| Fix | Self-check | How addressed + evidence |
|---|---|---|
| Empty `repo_cwd` no longer written (convergent: QA+Lang+SWE) | OK | `[ -n "$repo_cwd" ] \|\| return 0` mirrors the surface_id/c_int guards. Criterion 1's "non-empty repo cwd" is now enforced, not just satisfied by luck. Evidence: test **(i)** — input with no `workspace.current_dir` → no file written, display == original, exit 0. |
| stderr leak on unwritable ctx dir (QA F1) | OK | The `jq … > "$tmp"` build runs in a `( … ) 2>/dev/null` subshell so a failing `>` redirect is as silent as the original. Evidence: test **(j)** — ctx dir `chmod 555` → stderr empty, exit 0, display identical. |
| `ts` not coerced before `--argjson` (Lang F2) | OK | `ts` is computed and `case`-coerced to a bare integer before use, so the "every `--argjson` value is guarded" guarantee now holds for all seven fields. |
| `-`-leading surface ids rely on path prefix (Lang F3) | OK | `mv`/`rm` now use `--` end-of-options so a `-`-leading id can never be parsed as a flag — safety is local, not incidental. |

Rejected/deferred (with reason): confident `context_pct: 0` when `context_window` is wholly absent — display also shows `C:0%` so file and display *agree* (no divergence), a 0 never triggers the watcher (0 < 70%), and absence-vs-fresh is #4's `ts` stale-guard job (AC12); `schema_version` / ms-or-monotonic `ts` — gold-plating for a same-session PoC; scientific-notation `raw_token_total` at ~1e18 tokens — valid JSON, unreachable magnitude.

Test now runs 10 cases (a)–(j), ALL PASS; `dash -n` parses both scripts; lines 1–83 still byte-identical to the live original.

## Expert reviews (independent adversarial subagents)

Two review rounds were run (after fix iteration 1 and fix iteration 2); all three experts returned PASS on the final artifact, both completion criteria OK/OK.

### QA expert — PASS
| Aspect | Verdict | Evidence |
|---|---|---|
| Meaningful tests/verification | OK | Suite asserts display↔file ctx% equality, atomicity, no-misleading-write, degradation, path safety — not just "exit 0". |
| Edge case coverage | OK | Adversarial probes: 50 writes / 1148 concurrent reads → 0 truncated reads; 20 concurrent same-surface writes → 0 temp leaks; path traversal (`a/b`, `../x`) rejected; flag-like id stays inside ctx dir; unwritable HOME/ctx → exit 0, no stderr leak. |

### Language expert — PASS
| Aspect | Verdict | Evidence |
|---|---|---|
| Best practices | OK | All 4 `--argjson` values guaranteed numeric/`null`; subshell stderr suppression correct without touching stdout/exit; temp cleaned on every failure path; empty-`repo_cwd` guard correctly placed. |
| Codebase style consistency | OK | Same `echo "$input" \| jq` idiom, comment-banner and `${VAR:-}` guard style as the original. |
| POSIX / no bashisms | OK | `dash -n` parses; no `local`/`[[ ]]`/arrays/`function`. |

### Software-engineering expert — PASS
| Aspect | Verdict | Evidence |
|---|---|---|
| Separation of concerns | OK | Display byte-identical and runs to completion before `write_state`; write cannot alter/block it; exit status preserved. |
| System integrity | OK | Atomic temp+`mv` (no truncated read, no clobber); path-injection-safe surface id; non-empty `repo_cwd` now guaranteed when a file exists; documented schema + consumer notes; `ts` present. |
| Maintainability | OK (1 accepted constraint) | `raw_token_total` re-derives the `c_int` numerator — **forced by the append-only / display-unchanged rule** (cannot compute it once in the frozen display block); mitigated by identical formula + comment, and the field is off the watcher's trigger path. |

Residual Low/cosmetic findings (triaged, not blocking): leading-`-` surface ids accepted (safe via `--`; real ids are UUIDs); sci-notation `raw_token_total` at ~1e18 tokens (valid JSON, unreachable); `~/.rn/ctx` grows unbounded (by design — staleness/cleanup is the consumer's job, documented).

## Overall Verdict
- Self-check: OK
- QA: OK
- Language expert: OK
- Software-engineering expert: OK
- Live A1 proof: OK (real CC render wrote this session's surface file)
- Ready for user review: Yes

## How to run
`sh loop/test-statusline.sh` — feeds fixtures to both original and spike, asserts (a) byte-identical display, (b) state file written + valid JSON + ctx% == display + non-empty surface/cwd + real raw total, (c) degradation path, plus review-fix guards (d) malformed→no file, (e) non-numeric size→no crash/no clobber, (f) invalid id→no escape, (g) fallback→real %+null raw, (h) write failure→exit 0. Result: **ALL PASS** (exit 0). Test FAILS before the fixes (case (d): old script writes `context_pct:0`), PASSES after.

## Notes / paths exercised
- Test uses a throwaway surface id (`poc-test-$$`) so no real session state file is clobbered; cleans up after itself.
- Branches exercised by the test:
  - Happy path: `CMUX_SURFACE_ID` set → state file written, ctx% matches display.
  - Degradation path (criterion-protecting): `CMUX_SURFACE_ID` unset → display still prints, identical to original, NO file written, no error (exit 0). Verified in test step (c).
- jq path for raw token total mirrors the original's `c_int` numerator exactly (input + output + cache_creation + cache_read), so the recorded total is consistent with the displayed percentage rather than recomputed divergently.
- `c_int` is reused directly for `context_pct` (no recomputation), guaranteeing the file matches the display for any input.
- Not exercised: a literal `jq -n` write failure (e.g. unwritable `~/.rn`) — guarded by `mkdir -p ... && ... 2>/dev/null` and the post-printf placement, so a failure cannot affect the already-emitted display.
