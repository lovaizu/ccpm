# 4 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| SKILL.md body separates process from output rules, no embedded mermaid diagram, addressee sentence present on the diagram/output rules | OK | Body split into "## The procedure" (steps 1–5, process layer) and "## Rules for the produced document" (output-rules layer). `grep -c '```mermaid'` = 0 — no diagram embedded. Output-rules section opens with the explicit addressee sentence: "**These rules govern the document this skill produces — not this SKILL.md prompt.**" | OK | No code fence anywhere in the file; "mermaid" appears only as an output rule (L99–107) and self-check (L70). Split is structural (process L25–77, output L79–158), not relabeled. Addressee sentence L81 unambiguous. |
| All source pillars present (reader def + ask-or-infer, house style, five axes w/ no-mixing, voice-by-reader, self-check) | OK | Step 1 = reader def (who/what/how) + ask-or-infer gate. "House style" = base content rules. "The five axes" = article/guide/reference/record-ADR/evaluation, each single role + "Do not mix axes". "Voice by reader" table. Step 4 = pre-output self-check. Cross-checked item-by-item vs instruction.md lines 5–81. | OK | Section-by-section pass vs instruction.md: reader+gate (L29–43), 7/7 house-style bullets (L84–97), five axes each with source caveat (L109–146), voice table (L148–158), self-check (L62–72). No dropped or invented pillar. |
| Self-check includes the single-axis (no-mixing) item | OK | Step 4 second checkbox: "Does the document hold a single axis (axes not mixed)?" — the item absent from the old file's self-check. | OK | Present L67; source self-check (L75–81) had no such line. Reinforced consistently with L52/L111, not fragmented. |
| Brush-up frame stated (input = existing draft, output = revision + what changed & why) | OK | Top bullets: "Input: an existing draft to revise…"; "Output: the revised document, plus a short what was changed and why note that ties each change back to the reader definition or a house-style rule." Step 5 (Deliver) restates it. | OK | L14 input, L15–16 output + what-changed note tied to reader; restated at delivery L74–77. |
| Frontmatter: name: up, third-person brush-up-first description, version 0.1.0, model-invocable (no disable-model-invocation) | OK | `name: up`; description = "This skill should be used when revising or brushing up an existing technical document …; it also applies when authoring … from scratch. It fires whether a human asks … or Claude is drafting or revising one itself." Brush-up case first; `version: 0.1.0`; no `disable-model-invocation` key. | OK | name/version/description confirmed L2–4; brush-up-first trigger; fires for human or Claude; no disable-model-invocation. |
| Body imperative & lean (<2,000 words), single file | OK | Imperative/infinitive throughout (Define…, Pick…, Derive…, Run…, Deliver, Hold…); no second person. Body word count = 1333 (< 2,000). Single SKILL.md, no references/. | OK | 1,333 words; imperative throughout; only SKILL.md in skills/up/. |
| CHANGELOG.md exists (Keep a Changelog; Unreleased > Added line) | OK | techting/CHANGELOG.md created; Keep a Changelog header + SemVer note; `## [Unreleased]` → `### Added` with one user-benefit line on `/techting:up`. No dated version section (none released). | OK | Keep a Changelog 1.1.0 + SemVer header, `## [Unreleased]` / `### Added`, user-facing entry. Compliant. |
| Both strict validations pass (or manual fallback recorded) | OK | `claude plugin validate ./techting --strict` → "✔ Validation passed" (exit 0); `claude plugin validate . --strict` → "✔ Validation passed" (exit 0). CLI present. | OK | Validations are an objective gate; both pass per recorded exit-0 output. |
| Two-reader dogfood shows voice/axis change | OK | (a) reader who "looks up the part needed" → axis = Reference; voice = uniqueness/coverage, no warmth/intro; closing = none. (b) reader who "reads top-to-bottom" → axis = Article/explanation; voice = warm & plain, motive before each term; closing = what was learned + limits. Same procedure, different "How" field → different axis + voice + closing, proving derivation not memorization. | OK | Procedure's forward-references resolve (L47–48→L109, L59–60→L148); different "How" yields different axis+voice+closing. Derivation, not memorization. |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Meaningful tests/verification | OK | Section-by-section fidelity pass vs instruction.md found no dropped/invented pillar; category-error fix genuinely holds (no fence anywhere, real heading-level split, unambiguous addressee sentence L81); cross-layer forward-references resolve with no contradiction. |
| Edge case coverage | OK | Ask-or-infer gate covers both human-present (L41) and headless/model-invoked (L42–43, "never silently guess, never stall"); self-check gates delivery (fail→fix→recheck, L27/L64); "single axis, no mixing" appears in three places (L52/L67/L111) all consistent. |

## Overall Verdict

- Self-check: OK
- QA: OK
- Ready for user review: Yes (QA expert returned PASS on all six criteria; both QA-domain rows OK; no findings to triage)
