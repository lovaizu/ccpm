# Tries — rn at 096c639 and after, on real material

Headless runs (`claude -p … --plugin-dir rn --model opus`), each command in its own invocation, a
fresh conversation for every `/rn:up`. Scratch clones only; removed afterwards.

## 1. `/rn:up` on the 0.8.0 issue-18 session (push and `gh` cut off)

Migrated with nothing run by hand: frontmatter lifted from the header and `State`, the slug renamed
from the ticket to the work (`issue-18` → `task-log-retrospective`) with the evidence file's own
paths following, `checks/` removed, 24 check-offs carried and 3 more that its commits had done.
The Plan evaluation found the design written for rn 0.8.0 while the session now runs 0.9.0, and it
stopped at #3 Design sign-off with that choice. Holds the first Completion criterion.

## 2. `ja-char-count` on lovaizu/rn-try#7 — stopped mid-way

`/rn:on` drew the goal out one point at a time (what, why, done, approach) and planned to a Design
sign-off. Under `/rn:up`, task #2 was built and sent back five times (evaluations 03–07); each
verdict named a new miscount of the hand-written Markdown stripping, each fix patched the instances
named. The conductor stopped only after the fifth, with a narrow question. After the fix at
`368370c` the same conversation put the approach itself to the user, with alternatives. The plan
rewritten on the chosen approach then went through five evaluations (08–12), each on the same kind
of fault. Stopped here: the rest was work on the subject, not on rn.

## 3. `deadline-countdown` on lovaizu/rn-try#8 — start to close

`/rn:on` → `/rn:ty` → `/rn:up` (#2 built, passed first time) → Design sign-off → a PR review summary
→ plain `/rn:gm` (recorded word for word) → `/rn:up` (the revision became task #6, evaluated, the
sign-off presented again) → `/rn:ty` → `/rn:up` (#4 built) → Evaluation sign-off → `/rn:ty`: PR
ready, `evaluations/` removed, the tree left with `steering.md`, the design document and
`deadline.sh`. Holds the second Completion criterion. Seen on the way:

- The Design sign-off asked for a choice and told the user to send `/rn:ty D, …`, which `/rn:ty`
  did not take.
- The plan was evaluated six times for a three-task goal; three of those came after a verdict that
  had already passed, because folding its smaller points counted as a change.
