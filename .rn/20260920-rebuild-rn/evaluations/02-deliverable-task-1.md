## Round 1 — fix step, fourth cycle — third-party evaluation (rn at `479addb`, try at `479addb`)

**Scope checked / not checked.** Checked, by reading or running it myself: the whole prompt text at `479addb` — 5 `skills/*/SKILL.md` (200 lines: `up` 48, `on` 45, `dn` 39, `gm` 38, `ty` 30) and 4 `references/*.md` (328: `steering` 171, `evaluate` 84, `task` 54, `conductor` 19) = **528 lines / 4,829 words**, end to end in one pass; `git diff 923be54..479addb -- rn` and `git diff 4c424cf..479addb -- rn/references/{steering,evaluate}.md` in full; `rn/docs/design.md` (70 lines), `plugin.json`, `CHANGELOG.md` lines 1–30; both `--strict` validations run here (both pass); the session contract `.rn/20260920-issue-31/steering.md`; the third evaluation (5762935137) and the try-8 record (5765362236), fetched from the API. **Try 8**: all 17 session commits in the `try8` clone with timestamps and file lists, and the `State`/`Pending` block at **every one of the 17**, by script; all **7 `Agent` dispatches and the 1 `SendMessage` in all three main sessions, every prompt read in full**; all 7 subagent `.meta.json` and every `.message.model` on the wire; **all 4 PR #5 comments diffed against the returning evaluator's final message**, character-level and whitespace-normalised word-level; every `gh`, `curl`, `WebFetch` and `rm` call in all 7 subagent transcripts; all 61 conductor Bash/Read/Edit calls across the three main sessions with timestamps; the mtime and byte content of `scratchpad/rn-try8/` against `479addb` (identical, mtime 00:22 JST, before the run); all six `t8-*.out`; PR #5 state and body; a residue sweep of `/tmp` and `/private/tmp`. **Not checked**: the 0.8.0 migration was **not re-run** and I did not re-verify it (last evidence is try 5b at `4c424cf`); the Design kind was not re-run (last run try 7 at `616e94d`); the four evaluators' internal tool-by-tool work beyond their `gh`/`rm`/network calls, verdict-file writes and final messages; `tools/rev.sh` as an artifact in its own right (it is the try's subject, not `rn`); `rn/README.md` (task #3 owns it); tries 1–7 except where a comparison mattered, taken from the four earlier comments.

**Read-through.** One uninterrupted pass over the 528 lines, no re-reads before forming the A1 judgment (I re-read specific lines later for sections D and E). 47 numbered steps (`on` 7, `dn` 6, `up` 8, `ty` 3, `gm` 4, `task.md` 6, `evaluate.md` 5, `steering.md` 8); 34 open with an explicit "…, so …" (counted by script), the other 13 state the point another way — five of those are the `SKILL.md` step-1 preambles. **There is no step whose purpose I could not state.** For a human reader, 4,829 words at this density is roughly 20–25 minutes; my own pass took about 6. Two steps state a purpose their instruction only partly delivers: `evaluate.md`:10–14 (the round-on-record guarantee has no resume branch for an ordinary task — E4) and `up`:41–45 (the artifact test excludes exactly the plan gate's artifact — E1).

---

### A. Acceptance criteria

- **A1 (readable in one sitting; every step's purpose statable) — OK.** 528 lines / 4,829 words, one pass, ~20–25 min for a human; all 47 purposes statable. `479addb` adds 3 net lines to `923be54`; the deduplication the last two cycles asked for still holds, with one new duplicate introduced (the three-NG sentence, D lens 1).
- **A2 (every result judged by a third party against named questions) — OK, and clean for the first time.** 4 evaluator dispatches in try 8, every one a fresh `Agent`, `model: opus`, no conversation history, **no `SendMessage` ever sent to an evaluator** (the one continuation goes to the implementer). Questions handed verbatim every round: P1–P5 ×1, E1–E3 ×2, S1–S3 ×1. **No prompt carries an earlier verdict, an exclusion list, or the builder's account**, and **no evaluator made a single `gh`, `curl` or `WebFetch` call** — the two-cycle-old comment leak did not recur. Two deviations from the text, neither affecting a verdict: 3 of 4 evaluators were told to answer in Japanese where `evaluate.md`:29 says the language `steering.md` is written in (English — the plan verdict is in English and the other three are not, so PR #5's four comments are in two languages); and the Deliverable round-1 prompt omits the "only thing to read on the PR" clause, though it also never names the PR, which is safer than the clause asks. See E7.
- **A3 (judgment and execution on different models, shown by a real session's record) — OK.** 4 evaluator subagents all `claude-opus-5` on the wire, 3 implementers all `claude-sonnet-5`; `model` explicit in all 7 `.meta.json` and in all 7 `Agent` calls. None defaulted.
- **A4 (start → suspend → resume → close, stops only at plan/design/evaluation; leaves `steering.md` + deliverable; evaluation on the PR) — OK.** Six invocations and nothing else typed (`on` → `ty`(cut) → `up` → `dn` → `up` → `ty`); the draft PR body is byte-identical to `on`:36; `gh pr ready` at close, `isDraft=false`; the final tree is `README.md` (pre-existing), `tools/rev.sh`, `.rn/20260922-rev-sh/steering.md` and nothing else; **no residue anywhere in `/tmp` or `/private/tmp`** (verified here, after the fact). Every one of the four rounds is on the PR, the NG included, and `Notes` at the close says nothing untrue. The only extra stop was the usage limit, not a stop `rn` asked for.
- **A5 (a 0.8.0 session resumes under 0.9.0 with nothing run by hand) — not re-verified at this text, and now with a text-level reason to doubt it.** It rests on try 5b's evidence at `4c424cf`. Since then the Migration text changed in four ways; three are cosmetic or equivalent (step 2's carry clause reorganised, step 4's `checks/` generalised, `Rn version` template shortened), but one **could have broken it**: `evaluate.md`:47–49's Migration question now reads "allowing for what the Migration section of `steering.md` drops and adds by design" — and `evaluate.md`:21–29 hands the evaluator **the session's `steering.md`**, which has no Migration section, never the plugin's `references/steering.md`. At `4c424cf` the question spelled the rule out inline and was self-contained. Also gone is Migration step 6's "Read it as the conductor against the old file first" (now only implicit via Judging the plan's self-read). See E3.
- **A6 (released per `plugin.md`) — NG, as planned.** `plugin.json` = `0.9.0`; `CHANGELOG.md` opens `## [Unreleased]`; no tag, no Release; `rn/README.md` untouched. Both `claude plugin validate rn --strict` and `claude plugin validate . --strict` → `✔ Validation passed`, run here. Tasks #3/#4 own this.

### B. Task #1's three Completion criteria

- **C1 (issue-18 resumes hand-free, keeping every task, check-off and `State` fact) — carried, not re-shown, on evidence one cycle older than last time.** Held at `4c424cf`; the Migration text has changed since and was not re-run. This remains the one criterion with no evidence at the current text, and E3 gives a concrete reason the current text might fail it.
- **C2 (a real goal goes start → suspend → resume → close, user stopped only at the three gates) — OK, and this is the strongest run of the eight.** Two `✅`, exactly two `complete task #` commits and no other commit carrying that string, `Status: closed`, PR ready, clean tree, no residue. Both halves that broke in earlier tries worked: the recovery after the usage-limit cut (E5) and the resume-at-a-judged-gate without a new round (F, below).
- **C3 (the third party's evaluation and the user's verdict on round 1 are on the PR) — half.** Four evaluations and four try records are on PR #32 and this one is postable unchanged; the user's verdict is outstanding by construction.

### C. The third evaluation's three "fix first" items, and its E1–E11

**The three "fix first" items.**

1. **Make `up`'s gate test one the bookkeeping cannot falsify — CLOSED in text and shown in behaviour, but the test excludes the plan gate's own artifact.** `up`:42–43 is now `git diff --quiet <judged> HEAD -- . ':!.rn'`. Session `cae76acf` ran it **verbatim** at 18:17:07Z (`… && echo "NO_CHANGE_OUTSIDE_RN"`), got the pass, presented the judged gate, and dispatched nothing — the previous cycle's dead branch fires. The dead-HEAD defect is gone. But `.rn/` is where the plan gate's artifact lives, so at that one gate the test compares everything except the thing judged. See E1.
2. **Make a round closable only by its post — CLOSED in text, shown in behaviour for the URL, but the entry is still rewritable in the field that matters.** `evaluate.md`:13–14 adds the clause; try 8 opened an entry before **all four** dispatches (`80d4430`, `e518ebb`, `e732b7c`, `10db00e`) and closed each by a posted URL (`7509f65`, `cb6d495`, `4f4da17`, `e968561`), verified at every one of the 17 commits — no round overwritten, no verdict file deleted unposted, no correction commit. **But at the Session round the entry's *judged commit* was silently changed** from `4f4da17` to `10db00e` when the URL was written. See E2.
3. **Take the URL from the value the post printed, and say where a gate-time scope decision goes — CLOSED in text for both halves; the URL half is not shown, and the scope half was not exercised.** `evaluate.md`:82–83 now says to write the printed URL into the entry; `task.md`:40–41 routes a gate-time limit into Assumptions. In try 8 **all four `Pending` URLs were hand-typed literals inside a python heredoc**, two of them from a comment id the conductor had already retyped into a follow-up `gh api` call; all four happen to be correct (I resolved all four). And the scope decision the record credits to `task.md`:40–41 was in `Assumptions` at the session's **first** commit, written by `/rn:on` — the Assumptions block never changed during the session, so the new clause never ran. See E6, E9.

**E1–E11.**

- **E1** (dead HEAD test) — **CLOSED and shown.** `up`:42–43, run verbatim in `cae76acf`. Replaced by a narrower defect at the plan gate (new E1).
- **E2** (five of eleven rounds unposted, entries overwritten) — **CLOSED and shown.** 4 of 4 posted, including the NG; every entry closed by its URL.
- **E3** (`Pending` URL written by hand) — **CLOSED in text only.** All four URLs are literals typed into a heredoc (new E6).
- **E4** (posted file differs from the evaluator's returned text) — **OPEN, unchanged, in 3 of 4 rounds.** New E5.
- **E5** (26 of 36 commits touch only `steering.md`) — **OPEN, proportionally worse; partly by construction.** 15 of 17 now. New E10.
- **E6** (evaluators read earlier verdicts off the PR) — **CLOSED and shown.** Zero network calls by any of the four; two prompts say "Do not read the PR's comments", one never names the PR.
- **E7** (gate-time scope decisions handed as an exclusion list) — **CLOSED in text, not exercised.** No exclusion list in any of the four prompts; but no gate-time limit arose, so `task.md`:40–41 is untested (new E9).
- **E8** (implementer's account posted on the PR and handed to the evaluator) — **CLOSED and shown.** All four PR comments are evaluator verdicts; no implementer posted anything.
- **E9** (`conductor.md` contradicts itself) — **CLOSED.** `conductor.md`:9 no longer says "on the thing itself"; only line 15's "on a copy" remains, and the conductor did copy (`cp -R … $SCRATCH`, 18:03:55Z, removed in the same command).
- **E10** (three-NG stop shows the conductor's analysis, not the verdict) — **CLOSED in text, not exercised.** `task.md`:34–35 and `steering.md`:112–113 now say "showing the last verdict; the count starts again from the answer". No task reached three NGs in try 8. The fix was written into **two** places (D lens 1).
- **E11** (`dn` wrote `chore:` where the text requires `wip:`) — **CLOSED and shown.** At `d71190f` both of task #2's steps were `[x]`, so `chore: suspend — rev-sh` is what `dn`:32–33 requires.

### D. The 528 lines on the same four lenses

**(1) Is a principle still stated in more than one place?** Three, one of them new this cycle.

> `task.md`:33–35 — "After three NG rounds on one task, stop and ask the user, showing the last verdict; the count starts again from the answer."
> `steering.md`:112–113 — "after three NG rounds stop and ask the user, showing the last verdict; the count starts again from the answer."

The cycle fixed E10 by writing the same new clause into both homes. It belongs in one — `evaluate.md`'s header already says "What follows an NG — a fix, a retry, a stop — and its limit are the caller's", which is exactly the sentence that should carry it once. Unchanged from last cycle: "a change to the Goal, the Success criteria or an approved design is the user's" still has two homes (`up`:34–35, `task.md`:42), and the model split has two (`task.md`:10–11 as a Roles index, `evaluate.md`:17 — defensible). The five `SKILL.md` step-1 preambles remain. **Net: the `Pending`-writing triplication the last cycle named is genuinely gone** — `steering.md`:111–113 and `task.md`:50–53 both dropped their copies and `evaluate.md`:10–14/78–84 is now the single home. That is the cleanest structural win in the diff.

**(2) Slogans or compressed phrasing.** None that needs a decode. `up`:42–43's test is now stated with the command inline, which is the right way to write it. The one line that reads as a guarantee and is not one:

> `evaluate.md`:13–14 — "A round's entry is replaced only by the URL of its posted verdict, never by the next round — an unposted round stays on record until step 5 closes it."

"Stays on record until step 5 closes it" implies something will reach step 5. For an ordinary task's round after a lost conversation, nothing does (E4).

**(3) Prohibitions with no mechanism.** `evaluate.md`:18 ("not the builder's account, not what to excuse, not an earlier verdict") is still a bare prohibition — it held in all four dispatches this time, which is the first cycle it has. `dn`:27 and `gm`:37 likewise held/were not exercised. The deleted rule the last cycle flagged — "The plan the user sees is the plan that was judged: a change after the verdict is judged again" — is **still absent**, and its absence is now load-bearing: `steering.md`:108–110 lists `/rn:on`, Migration and `/rn:gm` as the triggers for Judging the plan and does not list `/rn:up`, while `up`:31–35 explicitly permits `/rn:up` to change Assumptions and remove tasks. Combined with E1 that is a reachable path to an unjudged plan.

**(4) How-instead-of-what-for, or a missing purpose.** Better than last cycle. `evaluate.md`:78–84 (step 5) still carries one purpose sentence over five obligations — post every round, refuse an evidence-free verdict, post from the file, fetch-and-compare, write the URL into `Pending`, plus the PR-less fallback — and it is still the densest step in the text; this time every obligation was met, so the density is no longer disproved, only unproven at scale. `task.md`:40–42 is where this cycle's patching shows: the new Assumption clause was spliced into the middle of the fold-back list and the sentence after it was left unwrapped at **139 characters** (`ty`:29 is 130, `steering.md`:100 is 149, `on`:44 is 114) — four lines in the file that are visibly edits rather than writing.

**Is the volume earned?** Yes. 528 lines is +3 on last cycle and the three added lines are the three fixes. The nameable redundancy is about 11 lines (the duplicated three-NG clause 2, `conductor.md`'s Purpose paragraph 4, the second home of "a change to the Goal…" 2, three of the five step-1 preambles 3), putting the floor near **515 lines**. **The text is at the right size; what is left is correctness, and this cycle it is correctness in paths no try has run.**

### E. New defects, worst first

1. **`up`'s artifact test excludes `.rn/`, which is exactly where the plan gate's artifact lives — so a plan changed after it was judged is presented as judged.** `up`:42–43: "present it only when nothing outside `.rn/` has changed since the commit `Pending` names as judged (`git diff --quiet <judged> HEAD -- . ':!.rn'`)". For a code deliverable this is right and try 8 proves it. For the plan gate — the gate `/rn:on` always ends at, and the gate Migration ends at — the artifact **is** `.rn/{session}/steering.md`, and the test is blind to it by construction. The path is short and each leg is in the text: `/rn:on` stops at the plan gate (`on`:44) → `/rn:dn` may "add any task the work uncovered" (`dn`:17–19) → `/rn:up` step 6 may correct an Assumption or remove a task (`up`:31–35) → `up`:42 finds nothing changed outside `.rn/` → the user approves a plan no evaluator read. `steering.md`:108–110 does not list `/rn:up` among Judging the plan's triggers, so nothing else catches it either. **Never exercised** — no try has suspended and resumed at the plan gate. A structural form: compare the artifact the round names rather than a fixed path filter — for a gate whose artifact is `steering.md`, exclude only the `# State` block; for any other, exclude `.rn/` as now.
2. **A round's on-record judged commit can still be rewritten, and try 8 rewrote it at the one round where the resume then depended on it.** At `10db00e` the conductor wrote `Pending: #2 Evaluation sign-off judged at 4f4da17` (`H=$(git rev-parse --short HEAD)`, 18:11:38Z) and then dispatched the evaluator at 18:12:00Z saying "judged at commit `10db00e`". At 18:15:04Z its own update asserted the entry said `10db00e`, the assert failed, and at 18:15:28Z it ran `git diff --quiet 4f4da17 10db00e -- . ':!.rn'` to satisfy itself and rewrote the field. So the entry `up`:43 keys the whole gate decision on was changed by something other than the posted URL, one commit after it was written. The cause is in the text: `evaluate.md`:12's "at `HEAD`" does not say whether `HEAD` means before or after the record commit this same sentence creates, and try 8 read it **both ways in one session** — the plan round named `f4f6c3c` (HEAD before) and told the evaluator so explicitly, the Session round named `4f4da17` and then judged `10db00e` (HEAD after). The fix is one word: name the commit the record-keeping commit itself will be, or name its parent, and say which.
3. **The Migration question now points the evaluator at a section of a file it is never handed.** `evaluate.md`:47–49: "Is every task, check-off, `State` fact and pending item of the old file present in the new one, allowing for what the Migration section of `steering.md` drops and adds by design?" The evaluator is handed "`steering.md` in full" (`evaluate.md`:21) — the *session's* plan file, which has no Migration section. `references/steering.md` is not in the hand-over list, and `evaluate.md`:18 forbids handing anything not on it. At `4c424cf`, the commit A5/C1's only evidence comes from, the question carried the rule inline ("unchecked steps that only ran the old `rn`'s review and its process files are dropped, and a check-off the commit log proves is added"). This is the one change since `4c424cf` that can plausibly have broken the migration criterion, and it bears on the one criterion with no current evidence.
4. **An ordinary task's unposted round has no resume branch, so `evaluate.md`:13–14's guarantee ends in a dead end there.** All four sentences of `up`:41–46 sit under "A gate named in `Pending` →". A conversation cut between `evaluate.md` step 1 and step 5 on a **Deliverable** round leaves an entry naming a task, not a gate; on resume `up` falls through to "No gate pending → run the next unchecked task", which re-enters `task.md` at step 1 and never closes the entry or notices the verdict file. Only `up`:32–33's "a blocker in `Pending` is investigated" covers it, loosely. Not exercised — try 8's cut fell before any round was open (`Pending` read "none — the user approved the plan").
5. **The posted verdict still differs from what the evaluator returned, in 3 of 4 rounds, and the read-back structurally cannot see it.** Every evaluator wrote its verdict file with a `cat > … <<'EOF'` heredoc and then typed its final message separately, so file and return are two independently produced texts. Deliverable round 1 (`5765205420` / `a5df4a95`) differs in **substance**: the post says the run of `-v` with no `echo "exit=$?"`/`exit=0` line the return has, says 「`tools/` は新規追加された唯一のディレクトリで」 where the return does not, and abbreviates a path the return gives in full — 6 differing spans, word-ratio 0.982. Round 2 (`5765260273`) differs by a trailing parenthetical present only in the return. The Session verdict (`5765309089`) differs only in punctuation width (full-width `（）` in the return, half-width in the post) — a systematic transformation, i.e. two typings again. Only the Plan verdict is word-identical. `evaluate.md`:75–76 requires the file and the return to be the same text; the read-back at `evaluate.md`:81–82 compares the **post to the file**, which is the pair that cannot diverge. Same defect as the third cycle's E4, now in three rounds instead of one.
6. **All four `Pending` URLs were typed by hand, in a heredoc, next to a shell variable that held the right one.** Rounds 1 and 2: `gh pr comment` in one call, then a *separate* `gh api repos/lovaizu/rn-try/issues/comments/5762997370` / `…5765205420` with the id retyped, then the URL retyped again as a literal in a python heredoc. Rounds 3 and 4: `URL=$(gh pr comment …) && ID=${URL##*issuecomment-} && …` — the capture exists — and the `Pending` write still used a literal. All four are correct this time; the mechanism the last cycle asked for is not in the text (`evaluate.md`:82 says "write that printed URL", which a retype satisfies) and was not in the run. The try-8 record's "no URL was typed by hand" is false.
7. **Three of four evaluators were told to answer in Japanese, against `evaluate.md`:29.** The text says "the language to answer in — the one `steering.md` is written in"; try 8's `steering.md` is in English and the plan evaluator was correctly told English, while the two Deliverable and the Session dispatches say "Answer in Japanese (the language of the conversation this session is held in)". PR #5 therefore carries four verdicts in two languages under one PR. Either the run is wrong or the rule is — `evaluate.md`:29 should say which reader it is serving (the PR reader or the artifact) and be obeyed.
8. **Nothing records a miss the conductor itself sends back, so the cut lost one and bought an extra opus round.** In session `2826e7ae` the conductor read the implementer's result (`task.md`:24–27), found the leading-dash defect itself, and sent it back by `SendMessage` at 15:28:42Z with reproduction and exit statuses — then hit the usage limit. `evaluate.md`:10–14 puts an evaluator round on record before it starts; a conductor send-back has no such place, so the finding died with the conversation. The recovery conductor re-checked the script at 18:03:55Z and its dash case was `./tools/rev.sh -- -e` — the one variant that passes — so it missed what the previous conversation had already established, and the defect was re-found by a fresh opus evaluator at 18:06 (round 1 NG). The cost is one full Deliverable round; the shape is the same hole the cycle just closed one instance of.
9. **`task.md`:40–41's new clause did not run, and the record says it did.** The try-8 record states "A gate-time limit went into Assumptions: the 'one code point = one character' scope decision is recorded there." That Assumption is present at `9dd9a19`, the session's **first** commit, written by `/rn:on` before any gate; `git diff 9dd9a19 295899a -- .rn/…/steering.md` shows **no change to the Assumptions block at any point in the session**. The user accepted no limit at a gate — they typed `/rn:ty`. The clause is CLOSED in text and untested.
10. **15 of 17 session commits touch only `steering.md`.** Four open a round, three close one (round 2's close rode with the check-off `4f4da17`, showing it can be done), plus start, plan self-fix, approval, two resumes, suspend, two check-offs. Two commits carry the deliverable (`1682f0c`, `58f184d`). Against try 7's 26-of-36 this is proportionally worse (88% vs 72%) on a session a quarter the size; against the per-round cost it is 1.75 bookkeeping commits per round vs try 7's ~1.5. The opening commit is the price of E2's guarantee and I would pay it; the three separate closing commits are not — `4f4da17` proves the close can ride with the commit that follows it. `design.md`:61–62 says "history lives in git", and `git log --oneline` on this session still cannot be read as a story of the work.
11. **The status block broke its one hard rule twice, and stopped opening the message twice.** `steering.md`:130–131 and `design.md`:56–57: "The Goal's first sentence and the task names are quoted as `steering.md` writes them." `t8-up2.out` and `t8-ty2.out` both truncate it to "…reverses text character by character ──", cutting the whole "the strings given as arguments…" clause. `steering.md`:117 says the block "Opens every message that stops for the user"; in `t8-up2.out` and `t8-ty2.out` a prose sentence comes first. And `t8-ty2.out` omits the `({outlook})` line, which `steering.md`:121–127 shows as part of the block and which only the ⬜ line is licensed to drop (`steering.md`:134). Three cosmetic misses, all in the two sessions after the first — consistent with the block being re-derived from the text rather than carried.
12. **`steering.md`:57–58's `Pending` description is now narrower than what writes into it.** The template says "<the gate awaiting a verdict, with the commit judged and where the verdict is; open questions; …>". `evaluate.md`:10–14 now writes an entry for **every** round including an ordinary task's, and while a round is running "where the verdict is" is a local file path, not a place the user can read. Try 8's four entries are all of the form the text does not describe. Small, but `steering.md`'s template is the one place a cold reader learns what the field means.
13. **Four lines exceed the files' own wrap** and read as splices rather than writing: `task.md`:42 (139 chars, created by this cycle's edit), `steering.md`:100 (149), `ty`:29 (130), `on`:44 (114).

### F. The three specific checks

1. **Every evaluator dispatch — fresh, `model: opus`, no earlier verdict, no exclusion list: 4 of 4. Told the artifact is the only thing to read: 3 of 4 explicitly, the fourth safer than the clause. No evaluator read the PR's comments.** All four are distinct `Agent` calls with `model: opus` in the call and `claude-opus-5` on the wire, `spawnDepth: 1`, no conversation history, and the session's single `SendMessage` goes to an implementer. I read all four prompts in full: none contains an earlier verdict's text, none contains the builder's narrative or its URL, none carries anything to excuse. Plan — "**The artifact — the only thing to read.**"; Deliverable r2 — "Do not read the PR's comments."; Session — "Do not read the PR's comments — judge the artifacts."; Deliverable r1 — says "That script is the only thing to judge" and **never names the PR at all**, which removes the leak path rather than forbidding it. Across all four subagent transcripts there is **not one `gh`, `curl` or `WebFetch` call**. The round-2 prompt contains no trace of round 1 beyond its verdict-file name; what the NG taught reached it the right way, as a new Success criterion folded into the contract by `task.md`:37–42.
2. **Every evaluation comment on PR #5 against the evaluator's returned text — 1 of 4 word-identical, 3 differ, and every difference is the evaluator's own.** I diffed all four character-level and then whitespace-normalised word-level, and I read how each verdict file was produced. Plan (`5762997370`) is identical once the evaluator's 132-character cover sentence is set aside. Deliverable r1 (`5765205420`) differs by 6 spans including dropped exit-status evidence and an added claim (E5). Deliverable r2 (`5765260273`) differs by one trailing parenthetical present only in the return. Session (`5765309089`) differs only in parenthesis width. The **verdict files themselves are visible in the transcripts** as `cat > … <<'EOF'` heredocs, and each posted comment matches its file byte for byte (the conductor's `diff` printed `IDENTICAL` / status 0 on all four) — so the conductor edited nothing; the evaluators wrote themselves twice. One-to-one mapping: 4 posts ↔ 4 distinct evaluators, none missing, none double-posted, no fifth comment on the PR.
3. **The NG round was posted before the fix was dispatched, and the fix's judge was a fresh dispatch.** `gh pr comment 5 --body-file /tmp/rn-verdict-rev-sh-task1-r1.md` at **18:06:50Z**; read-back and `IDENTICAL` at 18:06:56Z; `Pending` closed with the URL and the new criterion folded into `steering.md` at 18:07:13Z (`cb6d495`); the fix implementer dispatched at **18:07:34Z**; `58f184d` at 18:08:12Z; the round-2 `Pending` entry opened at 18:08:50Z (`e732b7c`); and the round-2 evaluator dispatched at **18:09:12Z** as `agent-a7385f68`, a different subagent from round 1's `agent-a5df4a95`, `model: opus`, no history, no round-1 text. The order is exactly what `evaluate.md`:16–18 and 78–84 ask for.

### G. Verdict on round 1

**Every one of the three fixes this cycle was asked for fired, and for the first time no fix was disproved by the run.** `up`'s gate test ran verbatim and presented a judged gate with no new round; every round was on record before its dispatch and closed by its posted URL, with no entry overwritten and no verdict file deleted unposted; the NG was posted before the fix; no evaluator was contaminated, by prompt or by its own reading; residue is zero; the `Pending` triplication is genuinely gone. A1, A2, A3, A4 and C2 hold. A6 is NG by design. A5/C1 rest on try 5b at `4c424cf` and are now one more cycle stale.

**What is left is different in kind from the last three cycles.** There is no finding here of a headline fix that cannot fire. There are three text-level defects on paths no try has run, and they matter because two of them live inside the mechanism this cycle just built.

**Three things I would fix before the user's verdict** — all three are one-line changes to text written in this cycle or the last, and none needs a new try to justify:

1. **Make the gate test see the plan gate's artifact** (E1). `.rn/` is the right exclusion for a code deliverable and exactly the wrong one for the plan, and `/rn:up` is permitted to change the plan on the way to presenting it. Exclude the `# State` block, not the directory, when the gate's artifact is `steering.md` — or restore the deleted "a plan changed after the verdict is judged again" and add `/rn:up` to `steering.md`:108–110's trigger list.
2. **Say which commit "at `HEAD`" means** (E2). One session read `evaluate.md`:12 both ways and silently rewrote the field `up`:43 depends on. Naming the parent, or naming the record commit itself, settles it in a word.
3. **Make the Migration question self-contained again** (E3). It currently sends the evaluator to a file `evaluate.md`:18 forbids handing it, and it is the question guarding the one criterion with no current evidence. Restoring the inline rule from `4c424cf` costs two lines.

**What a verdict can carry into round 2.** The file-vs-return divergence and the read-back that cannot see it (E5) — real, three rounds, no outcome moved, and the right fix (have the evaluator write the file and return "written to <path>", or diff the return against the file) is a round-2 shape question. The hand-typed URLs (E6) — all correct this time, and the fix is the same shape. The unposted-ordinary-round resume hole (E4) — untested, and its trigger is rare. The conductor send-back with no record (E8) — it cost one opus round and nothing else. The language rule (E7), the status-block misses (E11), the `Pending` template description (E12), the duplicated three-NG clause and the second home of "a change to the Goal…" (D lens 1), the commit noise (E10), the four unwrapped lines (E13), `rn/README.md` (task #3). And the paths still untried at this text: the Design kind, the 0.8.0 migration, a Session NG becoming a task, the PR-less fallback, the three-NG stop, and a resume at the plan gate.

**Is round 1 ready for the verdict now?** Not quite — but for the first time the gap is three one-line edits rather than a rebuilt mechanism, and none of them requires another try to close. Make the three changes above, re-read them against the two paths they govern, and round 1 is ready. Everything else on this list is round-2 work, and I would not hold the verdict for any of it.

*Working trees verified clean before and after (`rebuild-rn` at `479addb`, `try8` at `295899a`); nothing checked out, nothing modified, nothing posted anywhere. My scratch `/tmp/rn-eval-r1fix4` removed and verified gone.*

