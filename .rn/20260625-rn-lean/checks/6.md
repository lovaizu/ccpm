# 6 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| User consulted only at plan/design/evaluation, not per task; no per-task user gate survives | OK | `grep -rn "user review\|User review" rn/` → NONE. `task-workflow.md` Phase: Complete has no user-review step (goes straight to check-off after Verify); Process selection rows end `… → coordinator review → check-off`; `steering-template.md` Steps block has no `- [ ] user review`, Flat-tasks row drops it. Exactly three named gates (plan/design/evaluation) appear consistently in `on` (Step 5 plan, Step 5 design), `up` (Step 7 evaluation), `task-workflow.md` (Review gates subsection), `design.md` (Approach + Flow). | OK | Round 1 + round 2 (subagent): no surviving per-task gate in any runtime file; the README (round-2 fix `de1f835`) also no longer depicts per-task approval. Three gates named consistently across `on`/`up`/`task-workflow`/`design.md`/README. |
| No change to agreed plan/design proceeds without reaching user mid-flight via escalation, a separate always-open channel (not a gate's exception) | OK | `task-workflow.md:128` "Escalation is an always-open channel, not a triage step … raised to the user immediately, wherever it surfaces … distinct from the three scheduled gates … counts as none of them." Triage bullet renamed `User's call`→`Escalation`; states a normal in-scope finding is decided Valid/Invalid against the bar, not escalated. `design.md:58` "Escalation is a channel, not a gate" with rationale (would-ship-unseen). | OK | Round 2: double-sense reconciled — channel is "not confined to triage"; the Triage outcome is "one place it fires — during Verify", same channel also fires in Execute/anywhere. "counts as none of them; a change to the agreed plan or design cannot ship unseen" intact. |
| Per-task quality still caught by self-check + QA/expert + coordinator review | OK | `task-workflow.md` Process selection retains Self-check → QA (→ language → SWE for code) → coordinator review → check-off; Verify phase keeps the coordinator's independent diff review (Verify step 1) and all expert reviews (step 2) intact. `steering-template.md` Flat-tasks row: "each task's own verify steps (QA / expert review + the coordinator's review) are its gate". | OK | Round 1: quality net intact after the user gate's removal — nothing dropped with it; coordinator's independent diff review pre-existed Verify and remains. |
| README depicts the three-gate model in scenario voice — no surviving per-task user-approval depiction | OK | `grep -in "approv" rn/README.md` → 3 hits, all the up-front plan gate: L20 mermaid `P([You approve the plan on the draft PR])`, L50 console "approve to start #1", L53 "Read the plan on the PR, approve … without stopping to ask you again until the goal is met". Old per-task loop removed: mermaid `Y([You approve]) -->|added to the same PR| W` gone; replaced by `X -->|cleared, added to the same PR| W` (reviewers clear, not user), dotted `W -.->|only when the call is yours| C([You weigh in])` (escalation), bold `W ==>|all tasks done| D([You confirm the goal is met])` (evaluation). Prose L27 rewritten: "sign off on the plan up front, then … without stopping to ask you for each one … pulls you back in only when a call is genuinely yours, and at the very end you confirm the goal is actually met." Old "nothing lands until you approve" / "each approved task added to the same PR" both gone. Gates match runtime files (plan up front, escalation mid-flight, evaluation at end) in plain scenario language, no mechanical gate-label list. | OK | Round 2: per-task approve loop gone from mermaid; escalation drawn as a *dotted* channel distinct from the solid gate edges and outside the task loop; evaluation as `You confirm the goal is met`. Scenario voice, no mechanical gate-label list. No surviving per-task approval anywhere in README. |
| "Escalation" double-sense in task-workflow.md resolved — no "not a triage step" contradiction | OK | `grep -n "not a triage step\|not confined to triage" rn/references/task-workflow.md` → only `:128 not confined to triage`; "not a triage step" returns NONE. Reworded so the Triage outcome (still named "Escalation", L120) is "**one place** it fires — during Verify", while "the same channel is always open" and also fires in Execute/anywhere a plan-or-design-changing discovery surfaces. "counts as none of them; a change to the agreed plan or design cannot ship unseen" retained — the mid-flight-cannot-ship-unseen content is not weakened. | OK | Round 2: two senses now one channel with multiple firing points, not a contradiction; load-bearing content preserved and strengthened. |
| Exactly three scheduled gates — no fourth, none missing; escalation never counted as a gate | OK | Gates enumerated as exactly plan/design/evaluation in all four files. Design gate folds into plan gate when settled at plan time, separate stop otherwise — explicitly "one of the three, never a fourth" (`design.md:55`). Escalation explicitly counts as none of the gates (`task-workflow.md:131`, `design.md:63`). | OK | Round 1: exactly three, named identically across all files; design-fold-vs-standalone path consistent and non-contradictory; evaluation reachable from both `up` (resume) and `task-workflow` Advance (same-session). No fourth, none missing. |
| Runtime files carry no rationale prose; rationale only in design.md (task #2 constraint) | OK | Runtime edits in `task-workflow.md`/`on`/`up`/`steering-template.md` are procedural (named gates as facts, imperative escalation steps). Rationale phrases ("Chosen over", "ceremony", "unseen", "would ship before the next gate") appear only in `design.md` — grep for those terms returns design.md only. | OK | Round 1: runtime files procedural; `design.md` holds rationale ("Chosen over…") without going per-step. Altitude clean. |

## QA Expert Review

Round 1 (adversarial subagent, on `5b00aa9`): **NG** — gate model correct across the four changed
files, but found one **blocking** cross-doc defect (`rn/README.md` still depicted the removed per-task
user gate — mermaid loop + "nothing lands until you approve" + "each approved task added to the same
PR") and one **nit** (the word "Escalation" used as both a Triage outcome and "not a triage step",
inviting misreading). Both fixed in `de1f835` (README reworked to the three-gate model in scenario
voice; escalation reworded to "not confined to triage"). Round 2 (same expert, on `de1f835`):
**PASS** — both resolved, no new defect or regression. (QA also noted no `## [Unreleased]` CHANGELOG
entry exists for this change — out of scope here; **task #5** owns CHANGELOG reconciliation.)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Meaningful tests/verification | OK | Verified the three named failure modes absent (surviving per-task gate / fourth-or-missing gate / escalation-as-exception), each by grep + file:line; the blocking README contradiction was caught and fixed, then re-verified. |
| Edge case coverage | OK | Probed: design gate fold-vs-standalone; evaluation gate reachable from both `up` and `task-workflow` Advance; completion-marker "one per task" still holds with the user gate gone; README (user-facing entry) vs runtime consistency — the round-1 blocking defect surfaced here and was fixed in `de1f835`. |

## Overall Verdict

- Self-check: OK (deliverable + README/escalation fixes)
- QA: Round 1 NG (1 blocking README defect + 1 nit) → both fixed in `de1f835`; Round 2 PASS, no new defects
- Coordinator independent review: OK (own read of `6bdfc46..de1f835`; three gates consistent, escalation a channel, rationale only in `design.md`)
- Ready to check off: Yes — Verify cleared (self-check + QA round-2 PASS + coordinator review); completion marker held for the consolidated PR review per user direction. CHANGELOG `[Unreleased]` entry deferred to #5.
