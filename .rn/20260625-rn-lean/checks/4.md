# 4 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| C1: steering is a sufficient, minimal forward contract; Governs/collapse/retire machinery gone because heavy content is never stored | OK | `rn/references/steering-template.md`: `# Decisions` section + `## D-N` format fully removed (grep `# Decisions\|## D-\|Governs` over `rn/skills rn/references` → 0 hits); `# Tasks` SHIPPED-collapse parenthetical removed (line 46-48 now just `# Tasks` → `### #1`); Step 2 placeholder line lists only `Tasks`, `State` (line 8); `# State` Notes forward-pointer rewritten to "open questions / pending decisions not yet captured in `design.md`" (line ~83). New `## Doc-division rule` (lines 11-21) states requirements/criteria → steering, structure/decisions → design.md, UX → README, and "a decision lands in a task, in `design.md`, or in a rule; deliberation and history live in git + the PR — never in steering." `rn/skills/up/SKILL.md`: Step 6 now "Clean up State" (reset placeholder + commit only — retire/collapse bullets gone); Step 7 SHIPPED skip sentence removed. Heavy content (rationale, completed-task bodies, narration) is never written to steering, so nothing exists to prune/retire/collapse. | OK | Round 1 + round 2 (subagent): machinery removal verified — `# Decisions`/`Governs`/`D-N`/SHIPPED-collapse absent from runtime files; `up/SKILL.md` never reads the removed content, so a no-design session resumes unblocked. No "resume blocked by missing context" path. |
| C2: design lives at the steering-named path as a whole-structure design-template instance; doc-division rule in steering-template, applied by /rn:on; design.md conforms with every reference resolving | OK | `rn/references/steering-template.md:26` adds top `Design: <path…>` line; doc-division rule lives here (lines 11-19); line 21 notes a no-design session omits the `Design:` line entirely while keeping the block copyable. `rn/skills/on/SKILL.md`: Step 2 decides design.md location with user (default `.rn/{slug}/design.md`); Step 3 writes the path into the `Design:` line, reads doc-division + `references/design-template.md`, allocates content at planning, drops `Decisions` from placeholder line (Tasks+State only), and forces no empty `design.md` and no dangling `Design:` line (no design → no file, no pointer; design gate folds into plan gate). `rn/skills/dn/SKILL.md:18` Step 2 adds discovered *tasks* only (no "or decisions" — steering has no decisions home). New `rn/references/design-template.md` — copyable 5-section template (Context & constraints / Approach / Structure with actor→responsibility table + diagram / Flow as numbered sequence / Open questions); rationale confined to Approach, Structure/Flow describe what-is, no preamble guard. `rn/docs/design.md` fully rewritten to those 5 sections, whole-structure (no per-step memos; the retire/collapse/Governs memos are gone); header still says "Not read at runtime"; all file refs resolve (task-workflow.md / steering.md / design.md / README / checks/{task-id}.md / rn/docs all exist — verified by ls). `rn/skills/dn/SKILL.md:23` dangling `D-N` ref replaced with "not yet captured in `design.md`". README unchanged — it does not claim design lives in steering, no contradiction. | OK | Round 1 + round 2 (subagent): `design.md` conforms to the 5-section design-template, all refs resolve; doc-division rule consistent across `on`/`steering-template`/README (no contradiction); no-design handling consistent across `on:26` + template lines 21/26. No dangling/empty `Design:` failure mode. |

## QA Expert Review

Round 1 (adversarial subagent, on commit `17e2cfe`): **PASS** on both C1 and C2, with two minor
consistency leftovers — both fixed in `30f8fc1`. Round 2 (same expert, on `30f8fc1`): **PASS** on both,
one nit (the no-design `Design:`-omission exception sat in upstream prose at line 21 but not at the
template block's copy point on line 26) → fixed in `ace2f07` by folding the omit instruction into the
block's angle-bracket placeholder (no HTML comment, which would leak on verbatim copy). Round-2 re-review
of `ace2f07`: **PASS**, no defects — exception now visible at the copy point, no contradiction with line 21
or `on/SKILL.md:26`, no regression.

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Meaningful tests/verification | OK | C1 OK: machinery removal verified — `# Decisions`/`Governs`/`D-N`/SHIPPED-collapse gone from runtime files; `up` never reads removed content. C2 OK: `design.md` conforms to the 5-section design-template, all refs resolve; doc-division rule in steering-template, applied by `/rn:on`, consistent across `on`/template/README. |
| Edge case coverage | OK | Probed: no-design session (no `design.md`, no `Design:` line); `Design:` pointing outside `.rn/{slug}/`; pending-decision home after Decisions removal (rides State→Notes). Two round-1 leftovers fixed in `30f8fc1` ((1) `dn` Step 2 "or decisions" dropped; (2) no-design `Design:` line omitted in `on` + template); round-2 nit fixed in `ace2f07` (omission surfaced at the block copy point). All re-verified PASS. |

## Overall Verdict

- Self-check: OK (deliverable + two fixes + nit fix)
- QA: Round 1 PASS; Round 2 PASS (both criteria), one nit fixed in `ace2f07` and re-reviewed PASS
- Ready for user review: Yes — QA closed on `ace2f07`; awaiting user review on PR #14
