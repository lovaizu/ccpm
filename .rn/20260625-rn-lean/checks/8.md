# 8 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| `/rn:ty` registers approval of the pending confirmation and the flow advances (gate passes / item accepted), with NO revision; not a no-op; does not trigger a revise | OK | `rn/skills/ty/SKILL.md` body `# /rn:ty — Approve`: step 1 (revised) identifies the pending approval, step 2 records it as approved, step 3 advances the flow (plan/design gate → next task; evaluation gate → session can close; reviewed item → final). No revise/edit verb anywhere; the "no-op" failure mode is fenced only by step 4 (nothing pending → say so), so the default path always advances. Ambiguous-pending fix: step 1 now restricts the target to approval/sign-off only — "Find the most recent approval / sign-off request — a scheduled gate (plan / design / evaluation) or a reviewed deliverable. Exclude weigh-in / escalation questions; those get answered, not approved here. State the identified target back. Proceed only if it is unambiguous; if more than one approval is plausibly pending, or it is unclear what is being approved, ask the user which before recording approval." So a weigh-in/escalation question can no longer be approved, and an ambiguous pending state forces a which-one question before any approval is recorded. |  |  |
| Frontmatter YAML parses; no mid-value ` : ` | OK | `description` scalar inspected: no ` : ` and no bare `: ` inside the value (counts both 0). Only colons in frontmatter are the 3 key delimiters. `name: ty`, `disable-model-invocation: true` present. (pyyaml unavailable in env; verified by manual scalar inspection.) |  |  |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Meaningful tests/verification | OK | QA PASS: approval is recorded and the flow advances per branch (plan/design → next task; evaluation → close; reviewed item → final); no revision verb anywhere; default path always advances, so not a no-op. |
| Edge case coverage | OK | Ambiguous-pending fixed: step 1 restricts the target to sign-off requests only, excludes weigh-in/escalation questions, states the target, and asks which when more than one approval is pending. Nothing-pending handled by step 4. Frontmatter YAML valid (no mid-value ` : `). |

## Overall Verdict

- Self-check: OK
- QA: OK (PASS; one should-fix on ambiguous-pending applied in `0a09993`)
- Ready to check off: Yes
