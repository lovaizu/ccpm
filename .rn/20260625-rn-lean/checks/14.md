# 14 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| A session's `steering.md` carries the design sign-off (when needed) and evaluation sign-off as ordinary tasks placed by planning, each gated on `/rn:ty`/`/rn:gm`; `on`/`task-verify-workflow.md` contain no hardcoded gate branch for either (failure modes absent: a session finishing without an evaluation sign-off task; the plan gate turned into a self-approving task). | OK | `rn/references/planning-workflow.md` Step 4 now places a **Design sign-off** task (Completion criteria: user approves `design.md` via `/rn:ty`/`/rn:gm`) when `design.md` exists and is not settled at plan time, and folds it into the plan-gate hand-off (Step 5) otherwise; Step 4 also always places a final **Evaluation sign-off** task (Completion criteria: user approves the Acceptance-criteria run via `/rn:ty`/`/rn:gm`) as the session's last task. Step 5's "Design gate" bullet now points to the Step-4-placed task instead of describing an ad hoc unplaced action. `rn/skills/on/SKILL.md` was read and confirmed unchanged — it only delegates to `planning-workflow.md`/`task-execute-workflow.md`/`task-verify-workflow.md`, no gate logic lives there. `rn/references/task-verify-workflow.md` Phase: Complete step 3 ("Advance") no longer contains the "if all tasks are done, run the evaluation gate" branch — it now reads "a sign-off task goes straight to the gate per the Process selection rule above; any other task begins at Phase: Execute", with the evaluation-gate mechanics carried by the Evaluation sign-off task's own Steps (via the new Process-selection "Sign-off task" instance) rather than hardcoded in Advance. The plan gate itself remains planning's own closing hand-off in Step 5 (never a task), so it cannot self-approve. Grepped `rn/` for `"evaluation gate"` and `"Acceptance criteria"`/`"Acceptance-criteria"` (case-sensitive) after edits: the only remaining "evaluation gate" hits are `rn/docs/design.md` (out of scope, already correct per the task) and `rn/skills/ty/SKILL.md` (generic description of what `/rn:ty` resolves — not a hardcoded "when all tasks done" branch, so out of scope and unchanged, as instructed). No hardcoded evaluation-gate branch remains in any in-scope file. `rn/skills/up/SKILL.md` Step 7's matching hardcoded bullet was also removed for the same reason (design.md principle A applies wherever the task loop is entered).<br><br>**Fix round (4 independent reviewers' findings, all 6 fixes applied and re-verified):** **Fix 1** (dangling "then check off"): `task-execute-workflow.md:60-65` and `task-verify-workflow.md:60-65` Sign-off task bullet rewritten — check-off now reads "approve → check off in `steering.md`" and revise reads "revise → address the feedback and re-present the gate; no check-off until later approved" — unambiguously ties check-off to approve only. **Fix 2** (stale Phase: Complete framing): `task-verify-workflow.md:195-202` — opening sentence now "no per-task user gate for a normal task: once Verify clears … — or, for a sign-off task, once the user's gate verdict resolves as approved — the coordinator checks the task off directly", and step 1 carries the same carve-out; the general rule for normal tasks is preserved, only the sign-off-task exception is added. **Fix 3** (gate mechanics leaking into Completion criteria): re-read `steering-template.md:97` ("Criteria vs steps" row) confirming criteria = end-state only, mechanics = Steps. `planning-workflow.md:26-27` rewritten: Completion criteria now read "`design.md` is approved" / "the Acceptance criteria run is approved" (no command names); `/rn:ty`/`/rn:gm` mechanics moved into explicit "Steps:" sentences describing what the placed task presents and how it takes the verdict. **Fix 4** (hyphenation): both workflow files' Sign-off bullet and `planning-workflow.md`'s Evaluation sign-off bullet now read "Acceptance criteria" (no hyphen), matching `steering-template.md:32` and both files' existing "Evaluation gate" bullet (line 37). Re-grepped `rn/` for `"Acceptance-criteria"` post-edit: one hit remains at `rn/docs/design.md:45`, confirmed via `git log -S` to predate this fix round (introduced in commit `5d848bb`, unrelated file, out of the assigned scope) — zero hits in the in-scope files. **Fix 5** (grammar polish): the colon+bare-verb clause in the Sign-off bullet is now "…entirely, going straight from picking the task to the gate" (normal descriptive sentence) in both files; the Process-selection intro sentence now judges kind first ("first, whether the task is a sign-off task or a build task; for a build task, its medium … and whether it produces or revises structure/approach") in both files; `planning-workflow.md:26` now reads "at the point **where** heavy build would otherwise start". **Fix 6** (missing evaluation backstop, most important): (1) planning-time self-check added as `planning-workflow.md:28` — "Before persisting (Step 5), confirm the last task in the list is 'Evaluation sign-off' — if it is not, this step was not followed correctly," placed as its own bullet parallel to Step 3's "force no empty design.md" self-check style; (2) runtime backstop added to `task-verify-workflow.md:208-213` (Phase: Complete, Advance step) — "If no unchecked tasks remain and no 'Evaluation sign-off' task was ever encountered in this session, that is a planning defect … escalate to the user immediately via the same always-open Escalation channel described in Triage above; do not close the session silently," reusing the existing "Escalation is an always-open channel" mechanism from Phase: Verify's Triage section (line 188) rather than reintroducing the old hardcoded gate-runner branch; confirmed via reading `task-execute-workflow.md` that its shared header ends before Phase: Complete (Phase: Complete exists only in `task-verify-workflow.md`), so no duplication into `task-execute-workflow.md` was needed. **Post-edit verification**: `diff <(sed -n '46,72p' task-execute-workflow.md) <(sed -n '46,72p' task-verify-workflow.md)` → empty (byte-identical shared header preserved); all three edited files re-read in full end to end — no new contradiction, prose reads cleanly.<br><br>**Fix round 2** (adversarial re-review, 1 real contradiction + 1 convergent minor nit): **Fix 1** (Design expert, NG — "Review gates" still contradicted the sign-off-task concept): `task-execute-workflow.md:32-48` and `task-verify-workflow.md:32-48`, identically — "never per task" → "never on any other task"; the Design-gate and Evaluation-gate bullets now each note they are "realized as the Design/Evaluation sign-off task below"; the "per-task boundary is not a user gate" sentence now reads "…is **not** a user gate for ordinary build tasks — the two sign-off tasks (Design sign-off, Evaluation sign-off) are the exception: their own per-task boundary **is** the design/evaluation gate itself. Per-task quality on every other task is caught by…" — removes the contradiction with the Sign-off task Process-selection bullet a few lines below (and with `rn/docs/design.md`'s "A sign-off task is a user gate") without touching the already-correct Plan-gate bullet or the Process-selection section. **Fix 2** (QA + Craft, convergent minor — ambiguous "this step" + no remediation): `planning-workflow.md:28` — "if it is not, this step was not followed correctly" → "if it is not, add it before persisting", fixing the dangling antecedent (now unambiguously points at the missing-task condition, not the self-check bullet itself) and giving the bullet a concrete corrective action like every other bullet in the list. **Re-verification**: `diff <(sed -n '10,120p' task-execute-workflow.md) <(sed -n '10,120p' task-verify-workflow.md)` → empty (whole shared header, Roles through Check file format, still byte-identical); `diff <(sed -n '46,72p' ...)` (Process selection) → empty; both files' Review gates + Process selection + Sign-off task bullet, and `planning-workflow.md` Step 4 in full, re-read end to end post-edit — no remaining contradiction, no new one introduced, Fix 2's bullet reads cleanly against its neighbors. |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | Round 1: PASS, with 3 adjacent-file gaps noted as out-of-scope/non-blocking (no worked Sign-off-task example in `steering-template.md`, README example task list not updated, `up/SKILL.md` pre-migration edge case) — none of these violate the stated failure modes, so left un-actioned. Round 2 (post fix-round-1, re-run against the backstop specifically): traced the exact regression scenario (3 tasks, no Evaluation sign-off ever placed) end to end against the literal Advance-step text and confirmed the new escalation clause fires — a real, independently-sufficient runtime backstop, not advisory prose. Noted one edge case (a *misplaced*, not *missing*, Evaluation sign-off task) as outside the named failure mode, not a blocking gap. PASS both rounds. |

## Expert Reviews (axes the task needs)

### Design Expert (tasks that produce or revise structure/approach)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | OK | Round 1: PASS — sign-off-task shape matches `design.md`'s flowchart (skip Execute/Verify, straight to gate). Round 2 (post fix-round-1): found a real defect — the dangling "then check off" clause was fixed, but the file's own "Review gates" section (~30 lines above the Sign-off task bullet) still stated "never per task" / "the per-task boundary is not a user gate" as unqualified universal claims, directly contradicting the new Sign-off task bullet in the same file and `design.md`'s "a sign-off task is a user gate" — **NG**, fix required. Round 3 (post fix-round-2): re-read both files' "Review gates" section fresh — now correctly carves out the two sign-off tasks as the exception, matches `design.md:24,44-48,105-106` verbatim in spirit, byte-identical between both files, no remaining contradiction found on a full fresh read. PASS. |
| System-wide integrity (interfaces, cross-doc consistency) | OK | Confirmed `planning-workflow.md`'s Design/Evaluation sign-off Completion-criteria wording aligns with `steering-template.md`'s "Criteria vs steps" rule after fix round 1 (gate mechanics moved to Steps); confirmed all three files describe the same sign-off-task mechanism consistently; confirmed `up/SKILL.md`'s Step 7 edit leaves its resume flow coherent (no gap — `task-execute-workflow.md`/`task-verify-workflow.md` now own all gate-routing logic). PASS. |

### Craft Expert (coding / writing / visual — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | Round 1: found 2 defects — the "then check off" clause read as applying to both approve/revise verdicts, and a colon+bare-verb-clause sentence broke from the file's descriptive voice; fixed in fix round 1. Round 2 (post fix-round-1): found the "Acceptance-criteria" vs. established "Acceptance criteria" hyphenation inconsistency, plus a missing "where" and an ambiguous "and…or that" construction; all fixed. Also flagged (convergent with QA) the ambiguous "this step" antecedent + missing corrective action in the new self-check bullet; fixed in fix round 2. Round 3 (post fix-round-2): re-confirmed the self-check bullet fix reads cleanly, and the reworded "Review gates" section (Design-driven fix) introduces no new grammar/punctuation issue — the `design/`↔`evaluation` line-wrap matches the file's pre-existing wrap style, not a new artifact. PASS. |
| Consistency with existing style | OK | Terminology ("Design sign-off"/"Evaluation sign-off") confirmed identical everywhere across all 3 files in every round; byte-identity of the shared header (`Roles` through `Check file format`, and specifically `Review gates`/`Process selection`) between `task-execute-workflow.md` and `task-verify-workflow.md` re-confirmed after every fix round via direct `diff`. PASS. |

### Verification Expert (test / fact-check / dry-run — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | OK | Round 1: verified every cross-reference the diff introduced resolves to a real target; traced Trace A (design not settled) end to end as correct; traced Trace B (session finishing) and found the removed hardcoded branch left **no replacement backstop** — a genuine regression against the task's own stated failure mode — **NG**, fix required. Round 2 (post fix-round-1): re-traced both scenarios literally against the new text — Trace 1 (planning correct) confirmed internally consistent end to end; Trace 2 (the regression scenario — no Evaluation sign-off task ever placed) confirmed the new escalation clause's condition fires exactly as traced, with a real, non-optional action, not advisory prose; confirmed the "Triage above" cross-reference resolves to the actual pre-existing "always-open Escalation channel" language. PASS. |
| Coverage (edge cases / claims / steps) | OK | Both rounds independently re-ran the greps (`"evaluation gate"`, `"Acceptance-criteria"`) and the byte-identity diff rather than trusting prior claims; confirmed the 4-file (then 3-file) change scope was complete via `grep -rln` across all files referencing the two workflow files — no other hardcoded gate branch missed. |

## Overall Verdict

- Self-check: OK
- QA: OK
- Design expert: OK
- Craft expert: OK
- Verification expert: OK
- Ready to check off: Yes — self-check OK; fix round 1 addressed 6 findings (1 regression — missing evaluation-sign-off backstop — plus 5 wording/consistency issues) from the initial 4-axis review; fix round 2 addressed 1 further contradiction (Design, NG — stale "Review gates" wording) plus 1 convergent minor nit (QA+Craft); round-3 re-review of the two affected axes (Design, Craft) both PASS, and QA/Verification's round-2 PASS still holds since fix round 2 didn't touch what they checked. Coordinator's own diff review (`git diff d9171e7..7e9d007`, plus targeted `diff`/`grep` re-runs after each round) confirms the shared header stays byte-identical and no hardcoded gate branch remains anywhere in scope.
