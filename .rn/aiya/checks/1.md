# 1 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| aiya/docs/design.md exists, intent-first, internally consistent, no script holds the loop | OK | `aiya/docs/design.md` exists (353 lines). Structure is intent-first, and §1 (L6–46) is now an explicit 3-layer chain in order: (a) the goal (L11–12 — raise an expert's productivity by an order of magnitude through AI agents), (b) hypothesis-level requirements toward it (L14–23 — the work always tracks the goal; the human is freed from babysitting; human–AI coordination happens at a few gates), (c) the AI-agent-specific implementation challenges that surface on attempting (b) (L25–33 — context bloat and drift, answered by the two properties: bounded context, drift detection), closed by the kept bridge sentence (L35–36 "Everything below exists to make these two hold by structure, under a prompt-driven loop with no controller program"). The `rn` characterization is corrected (L38–43): `rn`'s purpose is session-lifecycle management, its enabling of the conductor-and-experts pattern incidental; it never targeted context bloat or drift, so the ~2-stream cap and the human never escaping babysitting are stated as a consequence of those concerns being out of scope, not a deliberate simplification; the sound remainder (bounded state as the only remedy for both, vs. full-transcript replay and retrieval-over-history) is retained (L43–46), with its lead-in clause re-bound to §1's terminology — "answers both challenges at once", since §1 now reserves "requirements" for the hypothesis layer. Doc still reads requirements → approach → structure and flow → details → decisions: §2 Approach (L48–63) → §3 Structure and flow (L65–153) → §4 Details (L156–328) → §5 Decisions (L330–353). No-script claim consistent end-to-end: L35–36; L332–335 Decisions bullet "Prompt-driven, no script holds the loop. The Conductor is the main agent running this procedure as imperative Markdown." Internally consistent after the reframe, re-checked at the shifted lines: CCS stated as YAML throughout (L164 "It is written in YAML", the L201–213 example is YAML syntax, L339 Decisions bullet "CCS is YAML"; TOON appears only as the considered-and-rejected alternative at L341–344); escalation-vs-gates stated once (L273 "exception interrupt, not one of the gates"); §1's new "a few gates" requirement matches §4.4's 6 fixed touchpoints (L290) and §2's steer-at-phase-boundaries. The four previously found-and-fixed defects remain fixed at their new locations: (1) §5's boundedness bullet distinguishes structural-by-construction vs. stated procedural rule vs. bounding convention (L345–353); (2) §1's bounded-context bullet reads "bounded — sub-linear, not growing linearly" (L29–31), matching §4.1 rule 2's "bounded and sub-linear, not byte-constant" (L222–223); (3) the Stage③ mermaid runs the CAP check (attempt >= 3?) before re-aim fires (L144); (4) the 1-Step-1-Turn invariant carries the re-aim clause "capped at 3 attempts total (§4.3)" (L101–104). | OK | Two independent adversarial QA passes (subagents, no conversation history, neutral framing — not shown the self-check). **Round 1** (against the pre-fix artifact): NG — found the 3 defects above via direct tracing (checked the "structural" claim against what's actually enforced vs. merely stated; traced the Stage③ diagram by hand for a 4-failure run; compared §1's absolute claim against §4.1 rule 2's own admission), while confirming intent-first structure, no-script-reliance, the four pillars, `conductor.md` retirement, and no dangling `.rn/`/`acc.md`/`tc.md`/`core.md`/`poc.md`/`conductor.md` references were already clean. **Round 2** (regression, after the coordinator dispatched fixes for all 3): OK — re-traced each fix independently (confirmed the §5 bullet now distinguishes structural-by-construction vs. stated-rule vs. bounding-convention; confirmed §1/§4.1 rule 2 both read "bounded, sub-linear"; hand-traced the reordered Stage③ diagram for a 4-failure run and confirmed no unverified 4th regenerate) and surfaced the 1-Step-1-Turn ambiguity as a new, minor finding — not a regression from the fixes, a pre-existing gap the first pass missed. Coordinator triaged it as a valid, decidable clarity fix (not a matter of taste) and dispatched a one-clause fix; verified directly via `git show 2160899` that the added text cross-references the same §4.3 cap value — holds up. **Round 3** (2026-07-02, against the §1 3-layer rewrite `436a69c`): conditional FAIL — found the L43 referent collision ("both requirements" dangling after §1 reserved "requirements" for the 3-bullet hypothesis layer) and a dangling `./conductor.md` link at `.rn/aiya/poc.md:13`; two further observations (the "So one agent" connective; the concurrent-vs-sequential "work-streams" reading) triaged as pre-existing non-defects, recorded, not acted on. Both valid findings fixed in `e656fd9` (1 of 3 fix iterations used for this cycle). **Round 4** (same QA expert, re-review of `e656fd9`): PASS — re-bound clause verified against §1's challenge terminology (L25–26, L40), poc.md relative path resolution confirmed, no new inconsistency; every criterion OK. |
| Salvageable design preserved in the real artifact (CCS contract, cycle, 6 gates, compression mechanism) | OK | All four pillars present in the rewritten §3/§4: CCS contract = §4.1 (L158–243) — one file per Turn at `.aiya/<issue>/ccs/tNNN.yaml` under replacement semantics (L160–162), 9 fixed components table (L170–181), per-component `type` starting-vocabulary table framed "a starting set, extensible, not frozen" (L183–196), 4 boundedness rules incl. by-path/grep-checkable and soft-cap-as-health-signal (L214–228), retrospective/backtrack read-back over `ccs/t*.yaml` (L238–242). Cycle = §3 Stage ①②③ (L111–153): dispatch→generate+folded-compress (L113–124), read CCS→independent fresh-context verify-Turn blind to the self-report (L126–136), advance/re-aim (capped at 3, L138–153)/gate. 6 gates = §4.4 (L278–327): 3 phases (Goal/Approach/Delivery) × {Planning Gate IN, Output Gate OUT} table (L289–295), storage layout (L305–314). Compression-forcing mechanism = the Decisions bullet at L345–353 — boundedness resting on the subagent return contract (structural-by-construction), the Conductor's read-restriction (§4.1 rule 4, a stated procedural rule), and the CCS size budget + grep invariant (§4.1 rules 1–2, a bounding convention) — plus compression folded into the generate-Turn itself (L122–124, L335–337). | OK | Both QA rounds independently re-derived and confirmed all four pillars from the artifact itself (not from the self-check): CCS contract (§4.1), the dispatch→generate→verify→re-aim→gate cycle (§3 Stages ①②③), the 6 phase gates (§4.4, explicitly labeled "6 fixed touchpoints"), and the compression-forcing mechanism (round 2 confirmed the reconciled §5 bullet still names all three boundedness levers after the fix). |
| design.md is self-contained — no dangling links to non-shipped .rn/ files | OK | `grep -nE '\.\./smith\|acc\.md\|tc\.md\|core\.md\|poc\.md\|conductor\.md\|steering\.md\|\.rn/' aiya/docs/design.md` returns zero matches (exit 1) — re-run and confirmed directly. The 9-component CCS table (§4.1) and the 6-gate table (§4.4) are inlined as literal tables rather than links; the only paths in the doc are the plugin's own runtime layout (`.aiya/<issue>/goal.md`, `approach.md`, `delivery.md`, `ccs/tNNN.yaml`, L308–314) and in-plugin section cross-references (e.g. "§4.1", "§3 Stage ③"), none pointing outside `aiya/docs/design.md` itself. | OK | Both QA rounds independently grepped the full file for `.rn/`, `acc.md`, `tc.md`, `core.md`, `poc.md`, `conductor.md`, `techting` — zero hits each time. Only in-artifact runtime paths and in-doc section cross-references remain. |
| .rn/aiya/conductor.md retired | OK | `test -f .rn/aiya/conductor.md` → absent (confirmed directly: `ls .rn/aiya/` shows only `checks/`, `poc.md`, `steering.md`, `techting-requirements.md` — no `conductor.md`); its substance lives in `aiya/docs/design.md` (CCS contract, cycle, gates, compression mechanism — see row 2 above). | OK | Round-2 QA independently confirmed `conductor.md` absent via `ls .rn/aiya/` and a worktree-wide `find -iname conductor.md` (zero results), plus checked `git log --all --diff-filter=D --summary` for a deletion record — not currently tracked anywhere in the tree. Round-3 QA additionally caught a dangling `[conductor.md](./conductor.md)` link surviving at `.rn/aiya/poc.md:13`; repointed to `aiya/docs/design.md` in `e656fd9`, round-4 re-review confirmed only prose mentions of the retirement remain repo-wide. |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Meaningful tests/verification | OK | Doc-appropriate verification across both rounds: internal-consistency stress test (checked claims against what the doc actually describes, not just against itself — e.g. the "structural" claim was checked against whether any enforcement mechanism is actually named), no-script-dependency audit, four-pillar completeness re-derived from the artifact, self-containment grep, hand-traced state-machine logic for the re-aim/escalation diagram rather than eyeballing it. Round 1 found real defects (NG); round 2 confirmed the fixes by re-tracing logic, not by re-reading changed words. |
| Edge case coverage | OK | Adversarial cases probed and caught: an overclaim ("structural" enforcement where only one of three mechanisms actually was), a quantifier mismatch ("must not grow" vs. "sub-linear"), an off-by-one in a state-machine diagram (traced a 4-failure run by hand), and — on the regression pass — a further invariant-vs-mechanics ambiguity ("1 Step = 1 Turn" vs. re-aim's multiple work-Turns) that the first pass missed. All four were triaged and fixed; the fixes were independently re-verified rather than taken on the fixer's word. |

## Overall Verdict

- Self-check: OK (re-verified against the current `aiya/docs/design.md` after the user-review §1 revision — §1 now runs the explicit 3-layer chain goal → hypothesis-level requirements → AI-agent implementation challenges (the two properties), the `rn` characterization is corrected to session-lifecycle management with the cap/babysitting stated as an out-of-scope consequence, the §1→§2 bridge sentence is kept, and all four criteria plus internal consistency with §2–§5 were re-checked at the shifted line numbers; the four previously found defects remain fixed, see row 1 Evidence; a follow-up QA fix re-bound the L43 clause to "answers both challenges at once" to match §1's requirements/challenges terminology — re-checked, `grep -n 'both requirements' aiya/docs/design.md` returns nothing)
- QA: OK (round-1 independent review found 3 defects — NG; round-2 regression review confirmed all 3 fixed and surfaced 1 further minor finding, which was also fixed and coordinator-verified; round-3 review of the §1 3-layer rewrite found 2 defects — the L43 referent collision and the poc.md dangling link — both fixed in `e656fd9`; round-4 re-review PASS with no new inconsistency; no NG findings remain)
- Ready for user review: Yes
- User-review round (2026-07-02, on PR #1): three revisions applied from live feedback. (1) §1's
  content-free structural opener ("Three layers, each raising the next") deleted — same-lens sweep of
  all 9 remaining section openers found no other instance (`402227c`). (2) §1's "Why these two,
  concretely." paragraph dissolved: the `rn` evidence compressed to one sentence kept in §1 (challenges
  are not hypothetical); the bounded-state-vs-replay/retrieval rationale moved to §5 Decisions as its
  own entry, with the "only remedy" overclaim softened to "of the remedies considered". (3) Dangling
  references re-checked after the move: `grep 'Why these two\|only remedy\|deliberate simplification'`
  over `aiya/` and `.rn/aiya/` hits only this ledger's historical evidence (which records the artifact
  as verified at the time — left as is). Row 1's line numbers describe the pre-revision file.
