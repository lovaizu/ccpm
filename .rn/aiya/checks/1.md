# 1 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| aiya/docs/design.md exists, intent-first, internally consistent, no script holds the loop | OK | `aiya/docs/design.md` exists (330 lines). Structure is intent-first, mapping to 背景→ペイン→ベネフィット→UX→構造→部品: §1 Requirements (L6–29, the two properties — bounded context, drift detection — and the "why these two" pain case against `rn`) → §2 Approach (L31–46, the core-idea benefit) → §3 Structure and flow (L48–136, cast diagram + Stage ①②③ mermaid = the UX/mechanics) → §4 Details (L138–311, CCS format 4.1, verify-Turn 4.2, re-aim/escalation 4.3, phase gates 4.4 = the 構造/部品) → §5 Decisions (L312–330, intent behind each choice). No-script claim is consistent end-to-end: L20–21 "under a prompt-driven loop with no controller program"; L314–317 Decisions bullet "Prompt-driven, no script holds the loop. The Conductor is the main agent running this procedure as imperative Markdown." Internally consistent: CCS is stated as YAML throughout with no stray TOON reference — L148 "It is written in YAML", the L183–195 example is YAML syntax, L321 Decisions bullet "CCS is YAML"; TOON appears exactly once, at L323–326, as a considered-and-rejected alternative inside that same Decisions rationale, not a leftover. Escalation-vs-gates is stated once and consistently: L255 "Escalation is an exception interrupt, not one of the gates." Three adversarial-QA-found internal-consistency defects fixed and re-traced: (1) §5's boundedness bullet (L327–334) no longer claims all three mechanisms are equally "enforced structurally... rather than relying on the Conductor's diligence" — it now names the subagent return contract as structural-by-construction, the read-restriction (§4.1 rule 4) as a stated procedural rule, and the size budget/grep invariant (§4.1 rules 1–2) as a bounding convention, matching §2's honest "default path, adherence observable" framing (L45–46) instead of overclaiming. (2) §1's Requirements bullet (L14–16) now reads "bounded — sub-linear, not growing linearly" instead of an absolute "must not grow," reconciled with §4.1 rule 2's "bounded and sub-linear, not byte-constant" (L205). (3) The Stage③ mermaid (L123–132) now runs the `CAP{"attempt >= 3?"}` check before `RA` (re-generate) fires on each `fail`; hand-traced for 4 consecutive failures — attempt goes 0→1→2→3 across three re-aims (three regenerates, three re-verifies), and on the 4th fail CAP catches at attempt=3 and escalates before any 4th regenerate — consistent with "capped at 3 attempts" (L135) and "at most 3 bounded verdicts" (L257–258). A fourth, smaller ambiguity found on regression QA — the "1 Step = 1 Turn" invariant (L84–86) didn't acknowledge that re-aim retries dispatch further work-Turns within a Step — is also fixed (commit `2160899`): the invariant now reads "...on the normal, passing path; a failing Step re-dispatches the work-Turn via re-aim (Stage ③ below), adding further work-Turns within that same Step, capped at 3 attempts total (§4.3)," cross-referencing the same cap value used in Stage ③ and §4.3. | OK | Two independent adversarial QA passes (subagents, no conversation history, neutral framing — not shown the self-check). **Round 1** (against the pre-fix artifact): NG — found the 3 defects above via direct tracing (checked the "structural" claim against what's actually enforced vs. merely stated; traced the Stage③ diagram by hand for a 4-failure run; compared §1's absolute claim against §4.1 rule 2's own admission), while confirming intent-first structure, no-script-reliance, the four pillars, `conductor.md` retirement, and no dangling `.rn/`/`acc.md`/`tc.md`/`core.md`/`poc.md`/`conductor.md` references were already clean. **Round 2** (regression, after the coordinator dispatched fixes for all 3): OK — re-traced each fix independently (confirmed the §5 bullet now distinguishes structural-by-construction vs. stated-rule vs. bounding-convention; confirmed §1/§4.1 rule 2 both read "bounded, sub-linear"; hand-traced the reordered Stage③ diagram for a 4-failure run and confirmed no unverified 4th regenerate) and surfaced the 1-Step-1-Turn ambiguity as a new, minor finding — not a regression from the fixes, a pre-existing gap the first pass missed. Coordinator triaged it as a valid, decidable clarity fix (not a matter of taste) and dispatched a one-clause fix; verified directly via `git show 2160899` that the added text cross-references the same §4.3 cap value — holds up. |
| Salvageable design preserved in the real artifact (CCS contract, cycle, 6 gates, compression mechanism) | OK | All four pillars present in the rewritten §3/§4: CCS contract = §4.1 (L140–226) — one file per Turn at `.aiya/<issue>/ccs/tNNN.yaml` under replacement semantics (L142–144), 9 fixed components table (L152–164), per-component `type` starting-vocabulary table framed "a starting set, extensible, not frozen" (L166–179), 4 boundedness rules incl. by-path/grep-checkable and soft-cap-as-health-signal (L197–211), retrospective/backtrack read-back over `ccs/t*.yaml` (L221–225). Cycle = §3 Stage ①②③ (L94–136): dispatch→generate+folded-compress (L96–107), read CCS→independent fresh-context verify-Turn blind to the self-report (L109–119), advance/re-aim (capped at 3, L121–136)/gate. 6 gates = §4.4 (L261–310): 3 phases (Goal/Approach/Delivery) × {Planning Gate IN, Output Gate OUT} table (L272–278), storage layout (L288–297). Compression-forcing mechanism = the Decisions bullet at L327–335 — boundedness resting on the subagent return contract (structural-by-construction), the Conductor's read-restriction (§4.1 rule 4, a stated procedural rule), and the CCS size budget + grep invariant (§4.1 rules 1–2, a bounding convention) — plus compression folded into the generate-Turn itself (L105–107, L318–320). | OK | Both QA rounds independently re-derived and confirmed all four pillars from the artifact itself (not from the self-check): CCS contract (§4.1), the dispatch→generate→verify→re-aim→gate cycle (§3 Stages ①②③), the 6 phase gates (§4.4, explicitly labeled "6 fixed touchpoints"), and the compression-forcing mechanism (round 2 confirmed the reconciled §5 bullet still names all three boundedness levers after the fix). |
| design.md is self-contained — no dangling links to non-shipped .rn/ files | OK | `grep -nE '\.\./smith\|acc\.md\|tc\.md\|core\.md\|poc\.md\|conductor\.md\|steering\.md\|\.rn/' aiya/docs/design.md` returns zero matches (exit 1) — re-run and confirmed directly. The 9-component CCS table (§4.1) and the 6-gate table (§4.4) are inlined as literal tables rather than links; the only paths in the doc are the plugin's own runtime layout (`.aiya/<issue>/goal.md`, `approach.md`, `delivery.md`, `ccs/tNNN.yaml`, L291–297) and in-plugin section cross-references (e.g. "§4.1", "§3 Stage ③"), none pointing outside `aiya/docs/design.md` itself. | OK | Both QA rounds independently grepped the full file for `.rn/`, `acc.md`, `tc.md`, `core.md`, `poc.md`, `conductor.md`, `techting` — zero hits each time. Only in-artifact runtime paths and in-doc section cross-references remain. |
| .rn/aiya/conductor.md retired | OK | `test -f .rn/aiya/conductor.md` → absent (confirmed directly: `ls .rn/aiya/` shows only `checks/`, `poc.md`, `steering.md`, `techting-requirements.md` — no `conductor.md`); its substance lives in `aiya/docs/design.md` (CCS contract, cycle, gates, compression mechanism — see row 2 above). | OK | Round-2 QA independently confirmed `conductor.md` absent via `ls .rn/aiya/` and a worktree-wide `find -iname conductor.md` (zero results), plus checked `git log --all --diff-filter=D --summary` for a deletion record — not currently tracked anywhere in the tree. |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Meaningful tests/verification | OK | Doc-appropriate verification across both rounds: internal-consistency stress test (checked claims against what the doc actually describes, not just against itself — e.g. the "structural" claim was checked against whether any enforcement mechanism is actually named), no-script-dependency audit, four-pillar completeness re-derived from the artifact, self-containment grep, hand-traced state-machine logic for the re-aim/escalation diagram rather than eyeballing it. Round 1 found real defects (NG); round 2 confirmed the fixes by re-tracing logic, not by re-reading changed words. |
| Edge case coverage | OK | Adversarial cases probed and caught: an overclaim ("structural" enforcement where only one of three mechanisms actually was), a quantifier mismatch ("must not grow" vs. "sub-linear"), an off-by-one in a state-machine diagram (traced a 4-failure run by hand), and — on the regression pass — a further invariant-vs-mechanics ambiguity ("1 Step = 1 Turn" vs. re-aim's multiple work-Turns) that the first pass missed. All four were triaged and fixed; the fixes were independently re-verified rather than taken on the fixer's word. |

## Overall Verdict

- Self-check: OK (re-verified against the current `aiya/docs/design.md` — §1 Requirements/§2 Approach/§3 Structure and flow/§4 Details/§5 Decisions structure, YAML-format CCS — all four criteria still hold; all four found defects — §5 overclaim, §1/§4.1 rule 2 "must not grow" contradiction, Stage③ re-aim cap off-by-one, and the 1-Step-1-Turn/re-aim ambiguity — are fixed and re-traced, see row 1 Evidence)
- QA: OK (round-1 independent review found 3 defects — NG; round-2 regression review confirmed all 3 fixed and surfaced 1 further minor finding, which was also fixed and coordinator-verified; no NG findings remain — 2 of the 3 allowed fix iterations used)
- Ready for user review: Yes
