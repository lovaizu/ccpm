# Task #10 self-check — FB round 3 encoded

Checked against the working tree at the time of the task-#10 commit. Every clause of the
FB-round-3 acceptance criterion, plus the task's own completion criteria. No sampling.

## The FB-round-3 acceptance criterion, clause by clause

| # | Clause | Result | Where |
|---|---|---|---|
| 1 | Step 3 tests slot → reader (the original subtraction) | OK | `SKILL.md:40` "A heading stays only if that reader needs it to reach the purpose." |
| 2 | Step 3 tests reader → slot | OK | `SKILL.md:41` "List what that reader must have to reach the purpose and name the slot carrying each" |
| 3 | A slot that defers the thing elsewhere does not count as carrying it | OK | `SKILL.md:41` "a slot that defers the thing elsewhere does not carry it" |
| 4 | A need with no slot is raised as a proposed slot, with position and reader evidence | OK | `SKILL.md:41` "Raise it as a **proposed slot**: where it belongs, and the reader evidence for it." |
| 5 | Never absorbed as prose under the nearest heading | OK | `SKILL.md:41` "Never absorb it as prose under the nearest heading; that is how the answer ends up at the back." |
| 6 | A supplied format faces both tests; either verdict is an axis conflict raised with the user | OK | `SKILL.md:43` "faces both tests. Either verdict is an axis conflict — raise it with the user" |
| 7 | Headless: the unjustifiable slot stays unfilled, the proposed one is placed and recorded | OK | `SKILL.md:43` "Headless: leave that slot unfilled, place the proposed one where the reader needs it, and record both in the note." |
| 8 | Step 11 sorts a **format defect** as its own class | OK | `SKILL.md:107` third bullet of the stumble sort |
| 9 | The format defect routes to step 3's proposed slot with the reader's words as evidence | OK | `SKILL.md:107` "Raise it as step 3's proposed slot, with the reader's own words as evidence." |
| 10 | An unruled proposed slot ships named rather than blocking delivery | OK | `SKILL.md:109` "subject defects, and a proposed slot the owner has not ruled on, ship named instead of blocking" |
| 11 | Step 10's admission item is stated in both directions | OK | `SKILL.md:85` "and every reader need carried by a slot (step 3, both directions); content kept out, and slots proposed, named in the note" |
| 12 | The delivery note carries proposed and unfilled slots with their evidence | OK | `SKILL.md:113` "any slot proposed or left unfilled, with its evidence" |
| 13 | The article axis leads with the answer | OK | `SKILL.md:133` "The question and its answer … and the answer to it, in 1–2 sentences" |
| 14 | Its closing is not a recap | OK | `SKILL.md:135` "In closing (the limits, what it does not settle, pointers onward — not a recap of the answer)" |
| 15 | The other four axes re-checked against "conclusion first" | OK | Guide `SKILL.md:138` goal first; Reference `:143` the map first; ADR `:149` decision as "the headline"; Evaluation `:154` conclusion/recommendation first. No change needed; recorded in `design.md` 4.3. |
| 16 | The carryover measurement runs at step 5 against the filled outline | OK | `SKILL.md:55` "set the filled outline beside the input: a bullet carrying the input's phrasing was transcribed, not written — re-fill it from meaning now" |
| 17 | Step 10's side-by-side stays as the backstop | OK | `SKILL.md:84` unchanged carryover item |
| 18 | The reader trial's brief asks what read as machine-written | OK | `SKILL.md:100` "including anything that read as machine-written: a sentence you had already read, a figure that disagrees with the prose?" |
| 19 | The loop ends on a trial, never on an edit | OK | `SKILL.md:109` "End on a trial, never on an edit" |
| 20 | A post-cap fix gets a changed-passages-only read | OK | `SKILL.md:109` "hand a fresh reader the changed passages and only those" |
| 21 | Whatever is unconfirmed ships named as unverified | OK | `SKILL.md:109` "whatever that read cannot confirm ships named as unverified" |
| 22 | Step 10 records produced-vs-input size and requires the growth to be argued | OK | `SKILL.md:91` "Produced size against the input's is recorded and any growth named as something the step-2 reader needed." |
| 23 | No bar attached to that ratio | OK | `SKILL.md:91` "No bar — growth you cannot argue for is unadmitted fill."; `design.md` D-11 "still no bar" |
| 24 | D-10 reopened as record-and-argue, with the round-2 evidence | OK | `design.md:368` "**Then declined** … **Round 3 produced it** — see D-11"; `design.md:384` D-11 records +43% for no gain |
| 25 | A declared composite is tested for separability at step 5 | OK | `SKILL.md:45` "at step 5, walk each part alone — a part whose reader cannot reach its purpose without crossing into the other is not a part" |
| 26 | The body stays under 500 lines and inside 1,000–3,000 words | OK | 190 lines, 2,994 words (`wc -l -w`) |

## Task #10 completion criteria

| # | Criterion | Result | Evidence |
|---|---|---|---|
| 1 | Every clause of the FB-round-3 criterion is present in `SKILL.md` | OK | 26/26 above |
| 2 | `README.md` describes the shipped behavior | OK | Article axis line ("the answer, up front"), the additive direction ("comes back as a proposed slot with the reader's own words behind it"), composite separability ("checks that each part actually stands alone") |
| 3 | `design.md` describes it, including the additive direction and the reopened length measure | OK | 4.3 (article skeleton vs ceiling), 4.5 (both directions + origin), 4.6 (floor-level tells belong to the reader), 4.7 (measurement moved to step 5), 4.9 (format defect), 4.10 (end on a trial), 4.11 (composite separability), D-10 reopened, D-11 new, §5.2 word-budget note |
| 4 | Both strict validations pass | OK | `claude plugin validate ./writ --strict` and `claude plugin validate . --strict` — both "Validation passed" |
| 5 | Body within 500 lines / 1,000–3,000 words | OK | 190 lines, 2,994 words |

## Scope checked, and what was not

- Checked: every clause above against the file contents, by line; both strict validations run.
- Not checked: a live `/writ:up` run against a real draft. This round's changes are verified by
  reading the shipped prompt, not by execution — the aiya side's next rebuild is the field test, and
  its result decides whether the additive admission actually moves the defect the round found.
- Standing gap: acceptance criterion 9 tests marketplace/README sync only, not README-vs-implementation
  agreement (carried into #8's sign-off questions).
