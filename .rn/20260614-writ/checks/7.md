# 7 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| SKILL.md admits content solely through the step-2 reader's path to the purpose, at both heading and point level, with routed-out content reported in the what-changed note and unjustifiable external slots escalated, not filled | OK | Step 3: "The step-2 reader admits every heading: it stays only if that reader needs it to reach the purpose"; excluded headings named in the what-changed note (whose it is, where it belongs); user-supplied outline/format slots pass the same test, unjustifiable slot raised with the user (headless: recorded in the note), "never fill it silently". Step 4: "The step-3 admission holds per point". Step-10 checklist item covers both granularities. Delivery para reports routed-out content and unfilled slots. | | |
| Experiential self-check items run in a fresh-context subagent (document + reader definition only) whose pass gates delivery; mechanical items remain self-checked | OK | Step 11 "Reader trial, then deliver": subagent gets "ONLY the produced document and the step-2 reader definition — no outline, no draft, no conversation history", checks purpose-reached and headings-carry-argument, reports stumbles; "A clean pass gates delivery." Step 10 keeps the mechanical items (single axis, admission, form/diagram, voice, seven tells, Assumed reader line, claim statuses); the two experiential items were removed from the author checklist. | | |
| The ceiling names minimal reader effort as the target, density as a means, and load-bearing claims distinguishable at a glance; one voice and the seven-tell floor stand unchanged | OK | Ceiling bullet: "the reader reaches the purpose with minimal effort. Means, not ends: density and concreteness … the few load-bearing claims distinguishable at a glance — placement, a heading, a lead sentence, not decoration; … one consistent voice." `git diff -U0 \| grep -E '^[+-]\| '` returned nothing — voice-by-reader table and seven-tell floor table byte-identical to before. | | |
| Body <2,000 words; addressee sentence intact; both strict validations pass; CHANGELOG updated | OK | `wc -w writ/skills/up/SKILL.md` = 1995 (was 1887). Addressee sentence present verbatim (grep -c = 1). `claude plugin validate ./writ --strict` → "Validation passed"; `claude plugin validate . --strict` → "Validation passed". CHANGELOG: 4 lines added under `## [Unreleased]` / `### Changed`, `<what> — <why>` format, no version bump. Anchors verified after edit: #the-five-axes, #voice-by-reader, #form (x2), #the-two-tiers (x2), #floor-checklist-the-seven-ai-tells (x2) all resolve to existing headings. Step refs verified: Order line lists 11 stages matching steps 1–11; internal refs (steps 2, 3, 3–4, 5, 6, 9, 10–11) all match final numbering. | | |
| The dogfood flags the FB's three named noise spots and the unjustified "Out of scope" slot | N/A — run by coordinator | Per steering, the coordinator runs the dogfood with independent fresh-context agents. | | |

### Fix round 1 (self-check, 2026-08-20)

Applied the expert-review fixes on top of 43d8dd9; self-check evidence:

- Step 7/8 leak closed: both steps now state that additions face the step-3 admission ("Rendering adds no content the filled outline does not carry…"; "Anything added here — an example, a number — faces the same step-3 admission").
- Step 11 trial reports a third question (content not needed to reach the purpose, and whom it seems to serve); rerun explicitly launches "a new fresh-context subagent"; "ONLY" → bold "**only**" per the file's emphasis convention.
- Step 5 reworded to an author-side story/order walk ("Walk the filled outline top to bottom in the step-2 reader's order… a real reader reads at step 11") — no longer contradicts step 11's "The author cannot simulate ignorance".
- Wording: step 3 lead now "Every heading must earn admission from the step-2 reader"; "passes the same test" → "faces the same test"; step-10 checklist "routed-out content" → "content kept out"; delivery para "content that steps 3–4 kept out".
- CHANGELOG: the four intra-unreleased `### Changed` entries folded into `### Added` (section removed); first folded entry re-agented to "Content is admitted only where the defined reader needs it to reach the purpose".
- README: reader trial added to the finish paragraph (fresh reader gets only the document and who it's for; delivery waits on a clean pass), matching the existing voice, no restructure.
- Body word count 1,966 (<2,000; whole file 2,072 by `wc -w`). All 5 `(#…)` anchors resolve; step refs (2, 3, 3–4, 6, 9, 11) verified against final numbering. Seven-tell table, voice-by-reader table, addressee sentence, frontmatter untouched. `claude plugin validate ./writ --strict` and `claude plugin validate . --strict` both pass.

### Fix round 2 (self-check, 2026-08-20)

Applied the re-review fixes on top of 844f4e0; self-check evidence:

- Step 11 unneeded-content report wired to an imperative: "Fix every stumble; cut or move out reported unneeded content under the step-3 admission, named in the what-changed note", and the clean pass is now defined as "no stumbles, no unneeded content".
- Post-trial fixes re-checked mechanically: "A fixed document re-passes step 10, then rerun with a new fresh-context subagent."
- Stale granularity updated in both places: step-10 checklist item now "(steps 3–4, 7–8)"; delivery paragraph now "content kept out at any admission point, steps 3–4 or 7–8".
- Step 5 clash resolved: "top to bottom" dropped — "Walk the filled outline in the step-2 reader's order."
- Step 11 report item 4 recast in direct-question form: "what content did it not need — and, when it can tell, whom that content serves" ("if tellable" coinage removed; "reach the purpose" no longer repeated in the sentence).
- CHANGELOG: "now" dropped from the two remaining delta-language `### Added` entries ("Produced documents mark…", "The five outline axes follow…").
- README: the trial gate stated once — "…and waits until that reader gets through clean" (dropped the duplicate "delivery waits").
- Body word count 1,996 (<2,000; whole file 2,102 by `wc -w`). All 8 `(#…)` anchor occurrences resolve to existing headings; all step refs (2, 3, 3–4, 3–4 7–8, 5→order, 6, 9, 10, 11) verified against final numbering. Seven-tell table, voice-by-reader table, addressee sentence, frontmatter untouched. `claude plugin validate ./writ --strict` and `claude plugin validate . --strict` both pass.

### Fix round 3 (self-check, 2026-08-20)

Applied the single-reader fix on top of e9de06a; self-check evidence:

- Step 2 now requires one reader in one reading stance: "Write three lines about **one person in one reading stance**: WHO … WHAT … HOW …", followed by "If the input implies several audiences, pick the primary; the admissions (steps 3–4) keep the others' needs out, and the what-changed note names them — a bundled reader admits everything." Closes the field-observed slack where a bundled WHO let every sentence claim admission via someone in the bundle.
- Paid for by meaning-preserving trims: "while this skill runs" (layers map), "Pick each for the reader's speed" (step 6, duplicate of the Form section lead), "(why it was chosen)" and the "not yet shown" variant (two-tiers status line), "in one document" (axes intro), step-5 tightening ("Fix order and gaps", "The author's structure check"), "(伝えたいこと)" gloss (step 1), "new" before fresh-context subagent and "conversation" before history (step 11), "This is" (step 4), "the AI tells" → "the tells" (intro), "skip the diagram" → "skip it" (Form), "When the input was a topic, not a draft" → "For a topic input" (delivery).
- CHANGELOG: one `### Added` entry under `[Unreleased]` — reader definition names one person in one reading stance, so a document cannot quietly serve three audiences at once.
- Body word count 1,997 (<2,000; whole file 2,102 by `wc -w`). All 8 `(#…)` anchor occurrences resolve to existing headings; step refs (2, 3, 3–4, 3–4/7–8, 6, 9, 10, 11) match final numbering. Seven-tell table, voice-by-reader table, addressee sentence, frontmatter untouched; steps 3, 4, 7, 8, 10, 11 semantics unchanged. `claude plugin validate ./writ --strict` and `claude plugin validate . --strict` both pass.

## Coordinator-run dogfood (fresh-context agents, 486a1af:aiya/docs/design.md)

Four independent runs; no run was told what to find. Reader definition from
`486a1af:.rn/20260813-aiya/checks/4-writ-note.md` (the ratified one) unless noted.

- **Round 1 — reader trial** (ratified reader): 22 stumbles, all real, none of the FB's three spots.
- **Round 1 — heading/slot admission test** (ratified reader): all 25 headings + 22 bold slots
  judged individually; sole FAIL = the "(proposed)" qualifier on run.yaml. Established that
  "Out of scope" is a §1 paragraph, not a heading — steering criterion corrected (43d8dd9).
- **Round 2 — reader trial with the not-needed question** (ratified reader, post-fix step 11):
  17 stumbles + 6 not-needed passages of exactly the FB's class (§3.3 revision-history sentence,
  inline discarded-alternative defense, README cross-references, "so five stays five", §7
  duplicate attribution). FB spots 2 and 3 explicitly *defended* as serving the ratified reader
  ("draws the novelty boundary / the measurement boundary").
- **Diagnostic — narrowed reader** (approval role removed; pure first-time comprehender):
  16 stumbles + 12 not-needed passages; **FB spot 2 ("Out of scope") surfaces as both stumble #1
  and not-needed #1** ("serves an evaluator, not a reader trying to comprehend"). Spots 1
  (opening lede) and 3 (§2 provenance opener) not reproduced in any of the four runs.

Reading: the mechanism catches the FB's noise *class* in volume, and spot 2's flip proves the
divergence sits in the step-2 reader definition (a bundle including the approver), not in the
checking machinery — which motivated fix round 3 (one person, one reading stance). Spots 1 and 3
are not reproducible as admission noise under any tested reader; the FB's own wording for spot 3
("pushes the actual core to second place") is an ordering complaint, addressed by the ceiling's
load-bearing-claims rule at construction, not by detection.

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | Round 1: all 5 criteria OK per-criterion; found the substantive leak (step-7 prose bypassing admission) plus 3 lows. Round 2: F1/F2/rerun-freshness CLOSED with quoted lines; flagged the unneeded-report-has-no-consumer gap (fixed in round 2 of fixes). Round 3: trims verified gate-safe (incl. "new" — a reused agent cannot satisfy "fresh-context"/"no history"); 2 residual enforcement gaps on the singular-reader rule recorded below. |

## Expert Reviews (axes the task needs)

### Design Expert

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | OK | Admission is construction-time (steps 3, 4, 7, 8) with an independent probe (step 11), matching the root cause; mechanical/experiential split judged principled item by item; singular-reader rule sits at the defining spot (step 2) and makes the voice-table lookup and axis match single-valued. |
| System-wide integrity (interfaces, cross-doc consistency) | OK | SKILL.md ↔ CHANGELOG ↔ README describe the same trial mechanism; CHANGELOG Changed→Added fold verified accurate; README judged not to need the reader-definition detail (below its altitude). Nit recorded: CHANGELOG "their own place" could borrow the preciser "routed to the what-changed note". |

### Craft Expert (writing)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | All six round-1 wording fixes landed; new clauses read in-register ("faces the same step-3 admission" refrain judged deliberate, not restatement); no AI tell committed in any new line across three rounds. |
| Consistency with existing style | OK | Fragments match the file's verbless-appositive register; trims judged padding-only. 3 polish nits recorded below ("the tells" unglossed at first use; "the admissions" plural vs the file's singular vocabulary; CHANGELOG new-entry register ×2). |

### Verification Expert (fact-check)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | OK | Both strict validations run at every round (all pass, exit 0); word counts measured (final: body 1,997 < 2,000, whole file 2,102); protected regions diffed 8b2e42e→a1087d9 byte-identical (floor table, voice table, frontmatter, addressee sentence); plugin.json 0.1.0 untouched; branch in sync with origin. |
| Coverage (edge cases / claims / steps) | OK | All 8 anchor occurrences and 20 step references enumerated and verified each round; CHANGELOG structure grepped (one [Unreleased], one Added, no Changed); git state clean except this check file. |

## Residual findings (recorded, unfixed — iteration cap 3 reached)

1. (QA, low) Multi-audience primary pick rides no marker path: step 2's Gate triggers on "cannot
   be answered", not on "answered several ways", so the pick surfaces neither interactively nor as
   `Assumed reader:`; step 10's "iff inferred" is undecidable for this case.
2. (QA, low) Nothing downstream verifies reader singularity — a bundled step-2 definition still
   passes steps 10–11 verbatim. A step-10 item ("Reader is one person in one stance") would close it.
3. (Craft, low) "the tells" at first body use dropped its "AI" gloss (line 11).
4. (Craft, low) "the admissions (steps 3–4)" plural vs the file's consistent singular
   ("the step-3 admission").
5. (Craft/Design, low) CHANGELOG singular-reader entry: "their own place" vague; "three audiences"
   reads as an unexplained specific.
6. (Verification, info, pre-existing) README L74 article-shape wording sits adjacent to a
   contradiction with the article axis's "instructions are not [welcome]" — predates this task.

## Overall Verdict

- Self-check: OK
- QA: OK (2 low residues recorded, non-blocking per QA)
- Design expert: OK (1 nit recorded)
- Craft expert: OK (3 nits recorded)
- Verification expert: OK
- Ready to check off: No — escalated to the user: the dogfood completion criterion is met for
  spot 2 (under a single-stance reader) and for the noise class at large, but spots 1 and 3 are
  not reproducible in four independent trials; the user decides whether the criterion stands
  satisfied in substance. Residual findings 1–5 await the user's call on a follow-up round.
