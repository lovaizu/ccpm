# 12 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| Reading the body top to bottom is doing the work: no step defers to a rules section for what it needs, and where a step points at a file, the pointer carries no summary and the step orders the read. | OK | Re-read end to end at `142441f`, all eleven steps. `## Reference` is gone: `grep -n "](#\|Reference →\|## Reference" writ/skills/up/SKILL.md writ/skills/up/references/axes.md` returns nothing (exit 1). The eleven `###` steps are the only sections (`grep -c "^### "` = 11). Every former catalog block sits at its step — voice table + form list at `:55-70` (step 6), the three claim statuses at `:78` (step 7), the ceiling at `:82-86` (step 8), the floor + seven-tell table at `:90-102` (step 9). The one pointer is `:32`: "Read `${CLAUDE_SKILL_DIR}/references/axes.md`; if that read fails, take the failure branch below. Match the reader and purpose to exactly one axis; its skeleton is your outline." — an order to read, naming the file, with no statement of what it contains. The clause added in round 3 is a branch marker to the failure path at `:41`, not a summary: a run whose read fails no longer executes `:34-37` against nothing before reaching the instruction not to. `axes.md:3` names the numbered items as slots, which is a definition inside the pointed-at file, not a summary at the pointer. **Round 3 also removed the one line that was not work:** the order line at the old `:16` restated the eleven `###` headings in condensed words — tell 2, whose fix is "Keep one" — so reading the headings is now the only place the order is stated. Live evidence: a run of the procedure end to end reached and used every step, including `:110`'s ordered re-read of `axes.md` — recorded step by step in `checks/12-dogfood.md`. | OK | Verified independently by QA and Design in all three review rounds, each walking all eleven steps rather than sampling. One qualification both raised and neither treated as a failure: the frontmatter `description`, always in context, names the five axis *names*, so the pointer withholds only the skeletons — recorded as carried item 6. |
| Every `design.md` §4 guarantee still names a mechanism and a breach-catch in the rebuilt body. | OK | Full 4.1–4.11 trace below, each row citing the line in the shipped `SKILL.md` (or `axes.md`) that carries the mechanism and the line that carries the breach-catch, every cited line printed back after this round's edits and every line number re-resolved against the shipped file at `142441f`. Every guarantee still has both. Round 3 changed two classifications and one mechanism. **4.6** was recorded as a detection; it is not one for half of what it catches — `:141` makes the note carry the run's *description* of what each trial was handed, not the brief itself, so a run that over-briefed lists two items anyway. `design.md` 4.6 now states it as a detection for the stumbles (the trial reports are committed beside the note) and an **attestation** for the brief, and names the clause that would upgrade it. **4.11** gained the wording a two-axis run needs: `:43` and `:110` were singular ("the axis chosen", "the slots quoted at step 3") while `:109` accommodated one axis per part, so a declared composite had nothing telling it to quote and re-check two skeletons; both now read one per part. **4.1**'s coverage claim was false and is corrected — see the round-3 fix table. The detect-or-attest column, recounted: two detect outright (4.9, 4.10), one attests only (4.7), eight are mixed. `design.md` 1.4, 3.1, 3.2, 4.1, 4.3, 4.6, 4.11, D-9 and §5.2 were rewritten to match the shipped body, and every `SKILL.md` line number in it re-resolved. | OK in letter, weak in force | Design's round-3 trace confirms all eleven subsections carry a mechanism and a catch in the body, and every `design.md` line citation resolves. But it counts nine of the thirteen step-10 items as self-attestation, and two guarantees were falsified or misclassified by the live run: 4.2 (step 9 marked tells 2 and 3 absent, `:116` returned PASS, a fresh reader found both) and 4.6 (recorded as a detection when the note carries only the run's description of the trial brief). 4.6's classification was corrected in round 3; the attestation weight is carried item 4. |
| No new problems: a live run reaches ends 1–8 no worse than before the rebuild, evidenced by the dogfood run recorded in `checks/12.md`. | OK (run produced; the ends are not the author's to grade) | **Where the run is recorded:** the criterion names this file, and this file points at the run — `.rn/20260614-writ/checks/12-dogfood.md`, with the three reader reports verbatim at `.rn/20260614-writ/checks/12-dogfood/trial-1.md`, `trial-2.md` and `trial-3.md`. Round 3 did **not** re-run the dogfood: the recorded run stands as taken against `d2660dd`, including its failures, and the round-3 fixes are not evidenced by it. The run was recorded in full at that commit: input `rn/README.md` (789 words), all eleven steps executed and recorded, three fresh-context reader trials whose reports are committed verbatim at `checks/12-dogfood/trial-{1,2,3}.md`. What the run shows, stated without grading: the procedure reached step 11 and terminated on a trial rather than an edit; step 10's ordered re-read of `axes.md` was performed; one step-10 item FAILed (`Assumed reader:` absent on a headless run), was fixed and re-checked, and no verdict outside the body's own PASS/FAIL/UNVERIFIED was invented; the delivery gate did **not** open — seven prose defects survive round 3 and ship named under the three-round cap, two of them tells the author's own step-9 pass had marked absent. Six subject defects were named for the input's owner and none was patched. Carryover fell across the rounds, 25.33% → 18.70% → 4.90%, every shared span being a literal command, transcript or figure. **Not graded here:** steering reserves ends 1–8 for a fresh-context reader and forbids the author's own reading, so the per-end verdicts are for the independent grader, not for this column. The prior run's per-end table has been removed rather than carried, since it was taken against a body two fix rounds old. | **NG** | Graded independently, in the split steering requires. **Ends 1 and 2** were given to a fresh reader holding only the delivered document and the step-2 reader definition: both **PASS** — "line 5 alone decided me"; of the seven tells, five absent, generalities twice and one voice slip on "session". **Ends 3–8** were graded against the run's own artifacts: **3 PASS, 5 PASS** (decision half; the proposal half this input cannot exercise), **4 NOT EXERCISED** (one reader definition, so the discriminating test never ran), **6, 7, 8 FAIL**. The three failures are the run not doing what the body orders — three reported unneeded blocks shipped and unnamed (`:137` makes that removal unconditional, and the cap covers prose defects only); "subagent", a word absent from the input, imported after step 7 closed it and asserted with no source, which is the reconciling clause end 7 forbids; and no step-10 re-pass after either fix cycle, so four of the thirteen PASSes are stale for the delivered text while the note reads "Unverified at delivery: nothing." **The comparative half is unmeasurable**: ends 1–8 were created at `727bde1`, the commit before this range, and no earlier body has ever been graded against them. |

## Method Applied

### design.md §4 trace — guarantee → mechanism → breach-catch → detect or attest

Line numbers are in the shipped `writ/skills/up/SKILL.md` at `142441f` unless marked `axes.md`. Every
line cited here was printed back and read after this round's edits. The last column says whether the
breach-catch would actually **detect** a breach — leave an artifact a third party can check against
the rule — or only **attest** that the run complied.

| § | Guarantee | Mechanism in the body | Breach-catch in the body | Detect or attest |
|---|---|---|---|---|
| 4.1 | A rule about the produced document is never misread as a property of the prompt | `:12`, before step 1, cut by grammar and scoped to **rules**: the imperatives address the run and none acts on this prompt; every rule — here and in the files the steps order read — is a rule for the work, the produced document or the outline built toward it, never for this file; steps 10 and 11 test that document. The addressee also rides in the rule's own sentence where the misreading happened: `:64`, the form rule that produced D-3 | `:120`, step 10's last item: the ceiling, the floor, the forms and the claim statuses were judged on the produced document, not on this prompt and not on the outline it was built from | **Attest** in the body, plus one detection that now actually runs: ```` ```mermaid ```` absent under `writ/skills/` catches D-3's exact failure class, and steering's `# Rules` puts it in the validation gate beside the two `claude plugin validate` calls, so every task runs it |
| 4.2 | Ceiling work is not layered onto uncleared tells, without paying for a pre-scrub pass | `:10` (build fresh, "step 9 is a net for stragglers"); step 8 (`:80-86`) precedes step 9 (`:88-102`) | `:116` "None of the seven tells remain (step 9)"; `:141` the note closes with the tells step 9 caught | `:116` **attest**. `:141` **detects a caught tell** but has no slot for a tell that survived — carried forward |
| 4.3 | One coherent shape suited to how the reader reads, and the shape actually comes from the file | `:32` "exactly one axis" and the ordered read; `axes.md:3` "One role each. **Do not mix axes.**" and the sentence naming the numbered items as slots; `:39` composite as the one sanctioned exception; `axes.md:6` the article slot 1 carrying the answer; `:43` the emit quotes the chosen skeleton's slots as they stand in the file | `:109` "One axis, no mixing …"; `:110` re-read `axes.md` and compare the step-3 quotation word for word, every outline departure being one the step-3 admission argued for; `:41` and `:106` give the failed read a defined verdict (UNVERIFIED) | `:109` **attest**. `:110` **detects**: the quotation and the file are both artifacts, so anyone reading the transcript can diff them without trusting the run |
| 4.4 | Voice, form and axis derived from an actual reader, not memorised | `:26` three reader lines, one person one stance; `:28` the Gate — ask, or infer and disclose `Assumed reader:` | `:117` "`Assumed reader:` line present iff the reader was inferred"; `:115` "Voice and closing fit the step-2 reader" | `:117` **detects**: a literal line is present or it is not, and this round's dogfood failed it and had to fix it. `:115` **attest** |
| 4.5 | No sentence ships because someone once wanted it there; and every reader need has a slot | `:34` slot → reader; `:35` reader → slot with the proposed slot; `:37` a supplied format faces both; `:47` per point (step 4); `:76` at rendering (step 7); `:86` at the ceiling (step 8) | `:112` self-check, stated in both directions; `:129` the trial's fourth question, "What content did it not need — and … whom does that content serve?" | `:112` **attest**. `:129` **detects**: an independent reader names the content, as it did three times in the dogfood's round 1 |
| 4.6 | The experiential checks are judged by someone the curse of knowledge cannot blind | `:124` "Launch a fresh-context subagent with **only** the produced document and the step-2 reader definition"; `:128` the trial brief asks for anything that read as machine-written | Added in round 2: `:141` makes the note list what each trial was handed, item by item, and every stumble it reported with its class and what was done about it. `:137` gates delivery and re-runs with a new fresh reader; `:139` forbids standing in when no subagent can launch | **Mixed.** **Detects** for the stumbles: a waved-through stumble is one with no disposition beside it, and the trial reports are committed beside the note. **Attests** for the brief: the note carries the run's own description of what each trial was handed, not the brief itself, so a run that over-briefed lists two items anyway — a detection would take reproducing each brief verbatim |
| 4.7 | "Build fresh" is a mechanism, not an intention | `:47` bullets in your own words (step 4); `:51` step 5 sets the filled outline beside the input; `:76` step 7 closes the input | `:111` self-check reads the two side by side — "Having been told to rebuild is not evidence that you did" | **Attest as worded** — "read the two side by side and confirm it". The design's own evidence for the same property is a token-span count; the body asks only for the reading. Carried forward |
| 4.8 | A proposal in the input survives as a proposal | `:20` step 1 sorts in force / proposed / hypothesis, "Content leaves only through the step-3 admission"; `:22` emits the proposed ones; `:78` "one not yet in force says so" | `:119` self-check claim-status item; `:141` the note lists what was kept out | `:119` **attest**. `:141` **detects** for content kept out — the note names it, so a dropped proposal that was never named is a hole a reader of the note can see |
| 4.9 | The procedure never smooths over a defect in the subject | `:133-135` the three stumble classes — prose defect, subject defect, format defect | `:137` only prose defects gate delivery; a glossed subject defect surfaces at the next fresh reader ("reruns with a new fresh reader") | **Detects**, but only on the next round and only within the three-round cap |
| 4.10 | The delivery gate can actually open | `:137` gated on no prose defect; three-round cap; "End on a trial, never on an edit … whatever that read cannot confirm ships named as unverified" | `:141` the note must carry every residual — tells caught, slots unfilled, content kept out, trial briefs, stumble dispositions | **Detects**: the residual list is an artifact; an unnamed residual is a discrepancy between the note and the trial reports, which are both committed |
| 4.11 | writ has a legal answer for a document that serves two readers | `:39` the split is the first answer, each part carrying its own reader line and axis at its head; `:51` step 5 walks each part alone in its own reader's order; `:124` one trial per part; `:43` and `:110`, from round 3, quote and re-check one skeleton per part | `:109` the axis self-check accepts exactly that shape and nothing looser; the per-part trial reports material its reader did not need | `:109` **attest**. The per-part trial **detects**. Not exercised by this round's dogfood — no composite arose |

### Claims checked, and against what

| Claim | Source it was checked against | Result |
|---|---|---|
| SKILL.md body should stay under 500 lines | `~/.claude/plugins/marketplaces/claude-plugins-official/plugins/skill-creator/skills/skill-creator/SKILL.md:90` ("<500 lines ideal") and `:96` ("Keep SKILL.md under 500 lines…"), both printed back | Confirmed. Body (everything after the closing `---`) is 139 lines; the whole file is 143. |
| The official word figure is 1,000–3,000 for the body | Two sources, both printed back this round: `plugin-dev/agents/skill-reviewer.md:68` "SKILL.md body should be 1,000-3,000 words (lean, focused)", and `plugin-dev/skills/skill-development/SKILL.md:190` "**Keep SKILL.md lean:** Target 1,500-2,000 words for the body" plus `:220` "SKILL.md is lean (~1,500-2,000 words)" | **The previous citation was selective.** The same official plugin family carries a tighter target the earlier round did not name. The body is 2,650 words: inside the reviewer's 1,000–3,000 band, and above the 1,500–2,000 target, which is not being met. Both figures and where the body sits against each are now in `design.md` D-9. |
| `references/` is the official home for detail moved out of the body | `skill-creator/SKILL.md:80-108`; `skill-reviewer.md:73-77` | Confirmed. |
| `${CLAUDE_SKILL_DIR}` resolves to the skill's own directory | Official plugin usage at `claude-security/skills/claude-security/SKILL.md:41-43,51-52,68`; the CLI changelog entry adding the variable (`~/.claude/cache/changelog.md:4007`); and a live headless run in the previous round that reported reading the absolute path and quoted `# The five axes` back | Confirmed. The live leg was taken against the pre-fix-round body; the pointer line is unchanged except for the appended fallback clause. |
| `compatibility:` is an allowed frontmatter key | `skill-creator/skills/skill-creator/scripts/quick_validate.py:42`, printed back: `ALLOWED_PROPERTIES = {'name', 'description', 'license', 'allowed-tools', 'metadata', 'compatibility'}` | Confirmed — allowed, and therefore silently accepted by validation whether or not it does anything. |
| `compatibility:` never reaches the run | Probe re-run this round with three distinct tokens — one in `compatibility:`, one in `description:`, one in the body's `#` heading — invoked as `claude -p "/probe:probe …" --plugin-dir ./probe` and asked to quote its instructions verbatim | Confirmed, and **the earlier round's account of the evidence was wrong**. The probe quoted a harness-generated `Base directory for this skill: <path>` line followed by the body from its `#` heading. The body token was present; the `compatibility:` token was reported absent from the whole context; the `description:` token appeared only in the available-skills listing, not in the instruction text. So the instructions begin at the body, not at the `description` as previously recorded. The conclusion is unchanged — `compatibility:` is inert and its removal was right — and `design.md` 4.6 now states the evidence as the probe actually shows it. |
| No mermaid figure sits in the prompt body | `grep -rn '```mermaid' writ/skills/`, re-run after the round-3 edits | Confirmed: no match, exit 1. This is D-3's exact failure class — a procedure flowchart embedded in the prompt — and the only thing in the whole design that detects it rather than asking the run to attest. Round 3 moved it out of `design.md`, where nothing was scheduled to run it, into steering's `# Rules` as part of the validation gate. |
| Nothing was lost from the old body in the relocation | Read the old `## Reference` section (`git show 6054660^:writ/skills/up/SKILL.md`) block by block against the new steps | **The previous round's evidence for this was a token-frequency diff, which cannot see this class of loss and did not: the old head sentence "Steps 10–11 test them against the produced document." was deleted with no replacement, and its words all survive elsewhere in the file, so the diff reported nothing.** That claim is withdrawn as evidence. Read block by block instead: every old catalog block is placed (two tiers → `:82-84` / `:90-102`, five axes → `axes.md`, form → `:64-70`, voice → `:55-62`, floor table → `:94-102`, claim statuses → `:78`), and the one sentence the diff missed is restored at `:12`. |
| Both strict validations pass | `claude plugin validate ./writ --strict` and `claude plugin validate . --strict`, re-run after the round-3 edits | Both "✔ Validation passed", exit 0. |
| The shipped prompt clears the writ floor itself | Two passes. Mechanical: `grep -inE "moreover\|furthermore\|in addition\|it is thought\|it should be noted\|it is important\|generally"` over both files. Read: the seven tells applied to every changed passage | Mechanical pass reaches **tells 4 and 7 only** — it cannot see padding, restatement, generalities, bulleting or voice drift, and last round it was offered as if it covered the floor. Its result: the only hits are the checklist's own examples at `:99` and `:102`. Read pass, this round's changes: tell 1 — step 6's announcing lead and step 7's announcing sentence were both cut; tell 2 — the five identical markers are gone, and the two remaining addressee statements (`:12` states the rule, `:120` checks it was honored) follow the file's existing rule/self-check pairing rather than repeating a payload; tells 3–7 — no new vague claim, connective, non-parallel list, register shift or hedge in any changed line. |

### Fix round — the four review findings

Commits `3bebefa`, `2018a2f` and `6455aa9`. Files: `writ/skills/up/SKILL.md`,
`writ/skills/up/references/axes.md`, `writ/docs/design.md`. Both pushed to `origin worktree-writ`.

| Finding | What changed | Evidence |
|---|---|---|
| **A. The addressee marker** — verbatim at five sites, ungrammatical at two, covering eight of eleven rule blocks, and its breach-catch outside the body | Shape **(c), coverage by scope**, taken over (a) per-block grammar and (b) a lead-in token. The five markers and `axes.md`'s head note are deleted. `:12` states the rule once, over every rule in the body **and** the files the steps order read, so `:41`, `:106-120` and `:124-141` are covered without a token each and no future block can fall outside it. The lost testing clause returns in the same sentence: "Steps 10 and 11 test them against that document." Where the misreading actually occurred the addressee rides in the rule's own sentence, grammatically and without repetition: `:64` "choose the form of each part of **the document you produce**" and `:106` "Mark each PASS or FAIL against the produced document". | `grep -r "not of this prompt" writ/skills/` exits 1, no match. `grep -c "never this prompt" SKILL.md` = 1. Mechanical check for a breach, stated in `design.md` 4.1: the scope sentence present exactly once and no marker anywhere — but the check the guarantee rests on is `:120`, inside the body. Why (c) over (a)/(b): both leave coverage an enumeration, so a block added later is uncovered by default and the catch stays "find the block with no token", which needs a definition of "block" that neither supplies. |
| **B. Nothing checked that the skeleton came from `axes.md`** | Step 3's emit (`:43`) now carries "the axis chosen, its skeleton's slots quoted from `axes.md` as they stand there, then the outline headings specialized to this content — one per slot". Step 10 gains `:110`: re-read `axes.md` and compare the outline's slots to the skeleton slot for slot, against the step-3 emit. `:34` gains the missing read-failure fallback — say so, ask for the file, and headless, record the outline as unverified against `axes.md`. | Verifiable from the run's own output rather than from trusting the read: the quoted slots and the file can be set side by side by anyone reading the transcript. The previously fetched-only skeleton was the one block where a from-memory reconstruction passes every existing check — `:109` "One axis, no mixing" is satisfied by a confabulated skeleton, and the deliberate slot-1 fix (4.3) lives only at `axes.md:6`. |
| **B (adjacent). D-13's criterion for what may live in `references/`** | D-13 rewritten. The recorded criterion — "long enough to crowd the procedure and the only one used at a single step" — is replaced by memory-substitution risk, which is D-13's own thesis, with the honest consequence stated: ranked that way the axes are the *worst* candidate, not the best, since the tells, the voice table and the ceiling are writ-specific lists a model cannot confabulate while the skeletons are Diátaxis/MADR-shaped. What buys the split is recorded as the fidelity check above, not the size argument. The bounded exposure is recorded too: the frontmatter `description` already names all five axes, so the pointer withholds only the skeletons — and the names stay in the description, since skill discovery depends on them. | `design.md` D-13, rewritten in place; the numbers in it and in §5.2 corrected to body-only counts. |
| **C. Prose defects** | `:76-78` step 7 merged into one flow — the announcing sentence and the interposed marker are gone, "Throughout the document:" now leads the payload itself. `:90` step 9's floor named as "the counterpart to step 8's ceiling", restoring the pairing that glossed "attractive quality". `:37` "which **step 8's** ceiling puts first", so the forward reference is usable where it stands. `:53-55` step 6's lead deleted; the **Voice** and **Form** subheads carry what it restated. `axes.md:3` third-person narration of the caller deleted with the marker; the file now opens on its own imperative rule. | Read back at each line. Step 6 is heading → subhead with no interposed sentence; step 7 is two paragraphs (write, then what the document carries throughout) rather than three. |
| **D. `design.md` consistency** | §3.2's stale row label `SKILL.md — process layer` → `SKILL.md — the ordered steps`, and its sibling row's "every such block carries the same addressee marker" → the scope line. §3.1's overclaim ("every rule states which of the two it governs where it sits") replaced with what is true: the layers are not separated, and one line before step 1 puts every rule under the same addressee. §1.4 and §2.2 no longer record the body layout — scope now says the layout is not a scope line and §5 owns it, and the binding constraints drop the clause entirely, per steering `# Assumptions`. §5.2's counts relabelled and corrected to body-only. The five over-wide lines re-wrapped, plus two more the edits had left at 144 and 113 columns. `design.md` 4.11 and `SKILL.md:41`/`:51` fixed the step-3-states-it/step-5-performs-it split. | `awk 'length>108'` over `design.md` now returns only the §3.2 table rows and one mermaid line, both exempt. Steering's `# Assumptions` line — "How the skill body is laid out … is a design decision, not a scope constraint: `design.md` §5.2 owns it" — is now true of §1.4 and §2.2. |
| **E. `compatibility:` frontmatter is inert** | Removed from `SKILL.md`. The fallback it described is stated in the body at `:139` and was already the only thing carrying it. `design.md` 4.6 rewritten to say so, and nothing in the repo now claims frontmatter declares a runtime dependency (`grep -rn compatibility writ/ .claude-plugin/ README.md` → one hit, 4.6's account of the removal). | Probe run recorded in the claims table above. Both strict validations still pass. |

### Fix round 2 — the four review findings

Commit `d2660dd`. Files: `writ/skills/up/SKILL.md`, `writ/skills/up/references/axes.md`,
`writ/docs/design.md`. Pushed to `origin worktree-writ`.

| Finding | What changed | Evidence |
|---|---|---|
| **A. The fidelity check contradicted the admission test** — `:43`'s "one per slot" and `:110`'s "slot for slot" were both broken by step 3, which mandatorily drops a heading (`:36`), raises a proposed slot with no skeleton behind it (`:37`) and forbids filling an unjustifiable one (`:39`) | The check is now on the **fidelity of the quotation**, not identity of the outline. `:112`: "The slots quoted at step 3 are `axes.md`'s own — re-read the file and compare the quotation word for word. Every departure of the outline from those slots is one the step-3 admission argued for (step 3)." `:45`'s emit drops "one per slot" and instead requires each departure to carry its admission. Two adjacent holes closed: `:43` puts the read-failure fallback on its own line and extends it to the emit ("quote no slots"), and `:108` defines the third verdict once — "An item whose evidence an unreadable file puts out of reach is marked UNVERIFIED and named in the note — never settled from memory" — which covers a failed re-read at step 10 as well as a failed read at step 3. `axes.md:3` now names the numbered items as slots, so both files use one word. `design.md` 3.2, 4.3 and D-13 rewritten off "slot for slot". | Read back at `:43`, `:45`, `:108`, `:112`, `axes.md:3`. Demonstrated live: this round's dogfood outline has three headings under skeleton slot 2 and one proposed slot — a shape the old `:110` would have failed and the new `:112` passes, with each departure argued at step 3. |
| **B. `:120` was false for a correct run** — it claimed no rule was judged against the working outline, while steps 3–5's rules *are* about the working outline | `:122` now reads "The ceiling, the floor, the forms and the claim statuses were judged on the produced document — not on this prompt, and not on the outline it was built from." It names the four rule families that govern the finished document, all of which sit at steps 6–9, so it is true when the run is correct and false when a rule was applied to the prompt or settled on the outline. `design.md` 4.1 records why the wording is scoped that way. | Read back at `:122`. Ticked honestly in this round's dogfood step 10. |
| **C. The scope line asked for a classification** — `:12` governed "every rule below about how a document should read", so applying it meant first classifying a block; it also enumerated five terms defined 70-odd lines later, and repeated the list at `:120` | `:12` now cuts by **grammar**, not topic: "The imperatives address you; none of them acts on this prompt. Every other statement — here, and in the files these steps order you to read — describes **the document you produce**. Steps 10 and 11 test that document, not this file." No classification step, no enumeration, no forward reference, and the restored testing clause now names what is tested (the document) instead of trailing after "the files these steps order you to read". Two moods are covered rather than one: imperatives by "none of them acts on this prompt", declaratives by "describes the document you produce", so no block sits outside it. | Read back at `:12`. `grep -c` on the old enumeration returns 0. Coverage argument recorded in `design.md` 4.1. |
| **D. §4.6 had no breach-catch in the body** — `:137`/`:139` were the mechanism and the unavailable-environment fallback, and `design.md` stated the catch as an after-the-fact symptom | `:143` now makes the what-changed note carry "what each trial was handed, listed item by item, and every stumble it reported with its class and what was done about it". A brief carrying more than the document and the reader definition is a listed item that should not be there; a waved-through stumble is one with no disposition beside it. Both breaches become visible in the run's own output rather than only in a later reader's struggle. | Read back at `:143`. Exercised this round: `checks/12-dogfood.md` step 11 lists the two items each trial was handed, and every stumble from all three rounds is tabled with its class and disposition. |
| **E. Prose defects (7 in `SKILL.md`, 1 in `axes.md`)** | `:80` restored the finite verb ("every claim **carries** one of three statuses"). `:53` made the composite walk imperative, moved it to sit immediately after "Walk the filled outline in the step-2 reader's order", and restored its tail to "the split goes back to the owner as the answer" (the wording at `397ff9d`). `:43` moved the read-failure contingency out from between "its skeleton is your outline" and "Then test the outline…", onto its own line. `:108` returned to "Mark each PASS or FAIL; any FAIL, fix and re-check", dropping the restatement `:122` already carries. `:66` unstacked the form lead and tied step 6 back to the story its heading promises: "for each part of the story walked at step 5, choose the form that carries it fastest in the document you produce". `:57` made the voice lead imperative to match it. `axes.md:3` names the numbered items as slots. | Each line printed back after the edit. Step 6's heading now has a tie: voice from the step-2 reader, form from the step-5 story. |
| **F. `design.md` corrections (10)** | D-9's stale "the body sits at 190 lines" corrected to 139 lines / 2,650 words, with both official word figures named. The 4.1 grep commands scoped to `writ/skills/` so they produce their stated result, and the mermaid detector added beside them. The frontmatter-probe narrative restated to match a real probe. Grammar: "the files the steps order **the run to** read", "which step 3 orders **the run to** read", and the mis-attached relative in "one sentence at the head of a `## Reference` section — a section D-13 dissolved". §1.4's "§5" made exact: "§5.1's D-13 and §5.2". `:507`'s 362 words attributed: 384 at the move, 372 now. Terminology fixed to "the produced document" at §3.1. Five short mid-paragraph lines and the over-wide ones re-flowed. | `grep -rn "not of this prompt" .` still exits 0 with two hits (this file and `design.md`'s own account), which is why the command is now scoped — verified by running both forms. `awk 'length>104'` over `design.md` returns nothing outside tables and fences. Word/line counts recomputed from the file, not carried forward. |

### Fix round 3 — the two reviewer FAILs

Commit `142441f`. Files: `writ/skills/up/SKILL.md`, `writ/docs/design.md`, `.rn/20260614-writ/steering.md`
(one rule line). Pushed to `origin worktree-writ`. The dogfood was **not** re-run: the recorded run
stands as taken against `d2660dd`.

| Finding | What changed | Evidence |
|---|---|---|
| **A. The scope sentence at `:12` was false** — "every other statement … describes the document you produce" is untrue of at least eight declaratives in the body (`:10`, the order line, `:49`, `:53`, `:92`, `:108`, `:126`, `:139` as numbered before this round), and untrue of itself: `:12` is a declarative in the body, so by its own terms it described the produced document. The grammatical cut bought total coverage by asserting something untrue — the category error the guarantee exists to prevent, one level up | The line now governs **rules**, not sentences, and names the work's two artifacts so the claim is true of every rule: "The imperatives address you; none of them acts on this prompt. Read every rule — here, and in the files these steps order you to read — as a rule for the work: **the document you produce**, or the outline you build it from, never for this file. Steps 10 and 11 test that document." Both reviewer proposals said "a rule for the document you produce"; that is still false for the rules that govern the *working outline* (`:47` one fact per bullet, `:51` walk the filled outline), which `design.md` 4.1 already had to carve out for `:120`. Naming both artifacts closes that hole without a classification step | Read back at `:12`. True of itself: sentence 1 is a true description of the imperatives; sentence 2 is an imperative, so sentence 1 covers it, and applied to itself it says only that it does not constrain this file — which it does not; sentence 3 is a true description of steps 10 and 11. Still reaches `references/axes.md` ("in the files these steps order you to read"), still enumerates nothing, still asks no run to classify a block by topic: a rule's own wording already says which artifact it acts on, and the line only excludes the third candidate, this file. `design.md` 4.1 gains a paragraph stating the corrected coverage — over every rule, not every sentence — and why a non-rule needs no addressee |
| **B1. `:53` had lost its antecedent** in round 2's move — the nearest nouns to "Does it reach the purpose" were *a declared composite*, *a part*, *the split* | Named what is walked: `:51` now reads "Does the walk reach the purpose with no gap, no repeat, no detour?" | Read back at `:51`. Chosen over moving the composite sentence, which would have separated the composite walk from the walk it modifies |
| **B2. `:80` did not parse** — "Throughout the document: Markdown, and every claim carries one of three statuses" joined a bare noun to a finite clause with `, and` | `:78`: "The document is Markdown throughout, and every claim carries one of three statuses — …" | Read back at `:78`. The reviewer's proposed wording, taken as offered |
| **B3. `:108` was a garden path** — "An item whose evidence an unreadable file puts out of reach…" | `:106`: "An item whose evidence sits in a file you cannot read is marked UNVERIFIED and named in the note — never settled from memory." | Read back at `:106`. `design.md` 4.3 quotes this line and was updated with it |
| **B4. `:16` was the eleven `###` headings said again in condensed words** — the file's own tell 2 | **Cut.** Step 8's ceiling demands headings that carry the argument alone; the eleven `###` headings do, so the arrow chain restated them, and tell 2's fix is "Keep one". The counter-argument — an arrow chain shows ordering at a glance — is answered by the file's own step 6, which says to skip a figure for "a short linear sequence, or a structure a sentence can carry"; eleven ordered numbered headings are that sequence, and the numbers already carry the order. Holding the prompt to a rule it hands its readers is worth more than a second statement of the order | `grep -n "^Order:" writ/skills/up/SKILL.md` exits 1. `## The procedure` now sits directly above `### 1. Understand the input`; the body dropped from 139 to 137 lines and nothing else references the order line (`grep -rn "story-check → voice" writ/` exits 1). Nothing about ordering is lost: `:10` already says to build "through the steps below, **in order**", and the eleven headings are numbered |
| **B5. "the step-3 admission" was used nine times and never defined** (`:22`, `:28`, `:45`, `:49`, `:78`, `:88`, `:112`, `:139`, `:143`), and step 3 never used the word for its own two-way test | `:32` now defines it where the test is: "Then test the outline against the step-2 reader **both ways** — the **step-3 admission** the later steps refer back to." | Read back at `:32`. The one use that precedes step 3 (`:20`, step 1's "content leaves only through the step-3 admission") stays a forward reference by step number, which is how the rest of the file refers forward |
| **B6. Step 3's read-failure branch was its fourth paragraph**, so a run whose read failed at `:34` executed `:36-41` against nothing before reaching the instruction not to | `:32` carries a branch marker on the read clause itself: "Read `${CLAUDE_SKILL_DIR}/references/axes.md`; if that read fails, take the failure branch below." | Read back at `:32`. The marker is a pointer to `:41`, not the contingency itself, so round 2's fix — read → match → test unbroken — still holds, and the pointer still states nothing about the file's contents |
| **B7. Two pre-existing imprecisions** — `:39` called *either* verdict an axis conflict (a passing verdict is not), and `:28` said "the Gate below picks the primary" when the Gate asks the user or infers headlessly | `:37`: "A failure either way is an axis conflict". `:26`: "With several implied audiences, take one as primary through the Gate below" | Read back at `:37` and `:26`. `:28`'s Gate is unchanged and now matches what `:26` says of it |
| **C. A composite run had no wording for two skeletons** — `:111` accommodated one axis per part while `:45` said "the axis chosen" and `:112` "The slots quoted at step 3", both singular | `:43`: "the axis chosen — one per part for a declared composite — with each skeleton's slots quoted from `axes.md` as they stand there". `:110`: "Every skeleton quoted at step 3 — one per part for a declared composite — carries `axes.md`'s own slots: re-read the file and compare each quotation word for word." | Read back at `:43` and `:110`; both now work unchanged for one axis or two. `design.md` 4.11 records the gap and the fix |
| **D. `design.md` corrections (7)** | §4.1's dangling "Guarantees that…" replaced with "The guarantee: …" (the elliptical form only works under the "What does X guarantee" headings of 4.2–4.11), and the coverage paragraph rewritten per A. D-9's "two thirds tighter" corrected to "a third tighter — 2,000 words against 3,000". §5.2's misreadable "the two fix rounds that followed 4.1, 4.3 and 4.6" recast as "three fix rounds since — reworking 4.1, 4.3, 4.6 and 4.11". §4.11's three-way `and` chain split at a colon, and "step 3 (`:41`) only says a composite has to be two documents in fact" replaced with what `:39` actually mandates — the declaration, each part's reader line and axis, the per-part admissions, and the note entry. The quotation of `:64` in 4.1 stripped of bold the source line does not carry. §4.6 restated as an attestation for the brief (see the trace table). §1.4's "§5.1's D-13 and §5.2 own it" narrowed to "§5.2 owns it" | Every `SKILL.md` line number in `design.md` re-resolved against the shipped file and printed back — `:12`, `:32`, `:34`, `:35`, `:37`, `:39`, `:41`, `:43`, `:47`, `:51`, `:64`, `:78`, `:90`, `:106`, `:110`, `:120`, `:124`, `:139`, `:141`. Word/line counts recomputed from the file: body 2,677 words over 137 lines, whole file 2,780 over 141. `awk 'length>105'` returns only the §3.2 table rows and one mermaid line. **Owner drift, resolved in `design.md`:** steering's `# Assumptions` says "`design.md` §5.2 owns it", and the task list, criteria and `# Assumptions` are the coordinator's, so the file that could move did |
| **E. §4.1's one real detection had nothing scheduled to run it** — the mermaid grep sat in a file marked "not read at runtime" | Added to steering's `# Rules` as part of the validation gate: "The validation gate is `claude plugin validate ./writ --strict`, `claude plugin validate . --strict`, and an empty ````grep -rn '```mermaid' writ/skills/```` — no diagram may sit in the prompt body". `design.md` 4.1 records the move | `git diff --stat .rn/20260614-writ/steering.md` = 2 insertions, 0 deletions: one rule item, nothing else in the file touched. All three gate commands re-run after the edits — both validations "✔ Validation passed" exit 0, the grep exit 1 |
| **F. Criterion 3 names `checks/12.md`; the run is in `checks/12-dogfood.md`** | Criterion 3's Evidence cell now opens with the pointer, and the `### Dogfood — where it lives` section names the file and the three trial reports by path | Both read back in this file. The criterion's wording is untouched |

### Carried to the next round (out of scope for #12)

Nine pre-existing gaps in writ's design, found while working this task and recorded so they are not
lost. None is a defect of this task and none was fixed here. Line numbers are the shipped file at
`142441f`.

1. **Step 11's reader brief is subtraction-only, so the delivery loop does not converge.** `:126-129`
   asks four questions; three look for excess and none asks what the reader needed and could not
   find. Evidence, from this task's own dogfood: the three rounds went **6 → 6 → 7 stumbles** with
   every reported defect fixed in between, and **four of round 3's seven were first reported at round
   3 and never attempted once** — they ship under a cap justified only for defects that survived
   rewriting. This is the same subtraction-only asymmetry `design.md` 4.5 records D-11 fixing at step
   3 and leaving standing at step 11. Proposed repair, one line: a fifth trial question — *what did
   you need that was not there, and where did you expect it?* — routed to step 3's proposed slot.
2. **`SKILL.md` never says which wins when step 10's "any FAIL, fix and re-check" (`:106`) meets step
   11's three-round cap (`:137`).** That undefined interaction is what let an earlier dogfood invent a
   third verdict (`PARTIAL`) instead of failing and fixing.
3. **`:141`'s what-changed note requires the tells step 9 *caught*; a tell that *survives* has no
   mandated slot.** 4.2's breach-catch is one-sided, and it was falsified live: this task's dogfood
   marked tells 2 and 3 absent at step 9, `:116` returned PASS, and a fresh reader found both.
4. **Nine of the thirteen step-10 items are self-attestation** — a run that writes PASS passes. The
   sharpest case is `:111`: the design's own evidence for the same property is a maximal-shared-span
   count, while the body says only "read the two side by side and confirm it". Two recorded runs make
   the gap concrete: one performed `:110`'s ordered re-read of `axes.md`, one skipped it, and nothing
   in the body distinguishes them.
5. **`:76` orders the input closed, then `:111` and `:116` require reading it again**, with no
   reconciling clause. This task's dogfood hit it: reproducing the input's mermaid figure and console
   transcript verbatim required the input open at render time.
6. **The frontmatter `description` names all five axes**, so the `references/axes.md` pointer withholds
   only the skeletons. Recorded in D-13; the names stay because skill discovery depends on them.
7. **The `Assumed reader:` line pollutes the delivered document.** `:28` orders it prepended to the
   output on a headless run and `:117` checks it is there — but an independent fresh reader given only
   the delivered document stalled on it: "a brief about me, addressed to someone else, sitting above
   the title … the spec showing through the paint. It should not ship." The disclosure belongs in the
   what-changed note, not in the document.
8. **`:110` has no defined repair.** A confabulated skeleton means the outline, the fill, the story
   walk, the render and the ceiling pass were all built on the wrong shape, and the body offers no
   re-entry to step 3.
9. **`writ/README.md` describes the reader-trial gate without the three-round cap**, and without the
   fact that residual prose defects ship named — which this task's dogfood shows is the normal
   outcome, not the exception.

### Dogfood — where it lives

Completion criterion 3 names `checks/12.md` as where the dogfood run is recorded; this section is
that pointer. The run is at `.rn/20260614-writ/checks/12-dogfood.md`, with the three reader reports
verbatim at `.rn/20260614-writ/checks/12-dogfood/trial-1.md`, `trial-2.md` and `trial-3.md`.
Commit `2f290a9`, taken against the body at `d2660dd` and **not re-run for round 3** — the run stands
as recorded, including its seven surviving prose defects. The record deliberately does not grade ends 1–8: steering reserves those for a
fresh-context reader, so an independent grader reads them off that record.

The previous run's per-end table has been removed rather than relabelled. It was taken against
`85570c1`, two fix rounds before the shipped body, its own step-10 table had 11 rows against the
shipped 13, and it invented a `PARTIAL` verdict the body does not define — so it is not evidence for
anything the current body does.

## QA Expert Review

Three rounds. Verdicts below are from round 3 (the last review taken); the round-3 *fixes* were not
re-reviewed, because the three-iteration triage cap was reached — see the Overall Verdict.

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | NG | Round 1 found the objective's own rule bypassed: the implementer graded ends 1 and 2, which steering reserves for a fresh reader, and a recorded step-10 failure was written as `PARTIAL` — a verdict the body does not define — then graded PASS against an end written as an absolute. Round 2 found the committed dogfood stale by two commits, its eleven-row step-10 table missing exactly the two items the design nominates as its breach-catches. Both were fixed: the invented verdict is gone, `UNVERIFIED` is defined once at `:106`, the run was re-taken with the three reader reports committed verbatim, and the ends went to independent graders. What remains NG is not the approach but the result — the grading returned three failed ends, and the criterion's comparative half has no baseline that could ever exist. |
| Checks that would pass even if the thing were broken | NG (partly addressed) | Round 1 named eight; the demolished ones were withdrawn rather than restated — the `Emit:` analogy for the addressee marker, the token-frequency diff (structurally blind to the loss it missed, a sentence whose words survive elsewhere), the grep-based floor check that reaches two of seven tells, and "a headless invocation starts the procedure" as evidence of execution. The residue is structural and is carried, not closed: nine of thirteen step-10 items are self-attestation, and two recorded runs differ on whether `:110`'s ordered re-read happened at all. |

## Expert Reviews (axes the task needs)

### Design Expert

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | NG at round 3, three defects fixed since | Round 1: the addressee marker repeated verbatim at five sites left three rule blocks uncovered and turned a positional guard into a judgment call. Round 2: the new fidelity check demanded an outline↔skeleton identity that step 3 mandatorily breaks — raised independently by all three reviewers, and demonstrated against this task's own dogfood, which dropped a slot on reader evidence. Round 3: the scope line that replaced the markers was **false** — at least eight declaratives in the body describe the procedure, and the sentence did not cover itself. All three are fixed at `142441f`; the last fix is unreviewed. Standing objection, recorded not accepted: Design holds that externalizing the five axes is unjustified under D-13's own criterion, since ranked by memory-substitution risk the skeletons are the worst candidate and the fidelity check is a mitigation for a self-inflicted wound. The split is kept — task #12 directs it, the reason is procedure readability rather than the retired word cap, and the check now guards the quotation. |
| System-wide integrity (interfaces, cross-doc consistency) | OK | `marketplace.json` and both READMEs need no change (no add/rename/remove); `CHANGELOG.md` owes no entry, since `writ` is unreleased at `0.1.0` with no `writ-v*` tag and a single `## [Unreleased]` "Initial release" line that absorbs a pre-release restructure. `design.md`'s §1.4/§2.2 no longer record the body layout as a scope or binding constraint, which steering reserves to §5.2. Every `design.md` line citation into `SKILL.md` and `axes.md` was re-resolved after each round. |
| The one detection that fires | Improved | §4.1's only mechanical detection of D-3's actual failure class — a diagram in the prompt body — sat in `design.md`, a file nothing runs. It is now part of steering's `# Rules` validation gate: both strict validations plus an empty `grep -rn '```mermaid' writ/skills/`. |

### Craft Expert (writing)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | NG at round 3, all findings fixed since | The file was held to its own seven-tell floor each round and failed tell 2 twice: first the addressee sentence repeated verbatim five times, then the order line at the old `:16` restating the eleven `###` headings in condensed words. Both are gone — the order line was cut in round 3 on the file's own rule ("Keep one"), leaving the headings as the single statement of order. Grammar regressions introduced by the moves were caught and fixed each round: a lost antecedent at `:51`, a finite verb turned participle at `:78`, a garden-path relative at `:106`. |
| Consistency with existing style | OK | Terse imperative second person holds end to end, and the seam into `axes.md` joins: its head note is gone, it opens on its own rule, and it now defines *slots* — the word `SKILL.md` uses when comparing against it. The forward reference to "the ceiling" is glossed as "step 8's ceiling" where it stands, and "the step-3 admission", used at nine sites and never defined, is now introduced where the two-way test is. |
| Losses in the restructure | OK — none | Verified three times at the sentence level, never by word frequency: four moved blocks are byte-identical (the five skeletons, the form list, the voice table, the seven-tell table), and every block of the old `## Reference` is placed. The one genuine loss found — "Steps 10–11 test them against the produced document", whose words survive elsewhere and which a token diff therefore missed — is restored at `:12`. |

### Verification Expert (fact-check)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (claims verified) | | |
| Coverage (claims) | | |

## Overall Verdict

- Self-check: all three criteria OK against the shipped body at `142441f`, after the round-3 fixes to the scope line, seven prose defects, the composite wording, seven `design.md` corrections and the mermaid detector's move into steering's validation gate. Criterion 3's run is produced and its evidence committed at `2f290a9`, taken against `d2660dd` and not re-run; the ends 1–8 verdicts inside it are the independent grader's, not the author's. Nine pre-existing writ design gaps are recorded as carried forward, unfixed — the first is that step 11's reader brief is subtraction-only, so the delivery loop does not converge
- QA: **NG** — criteria 1 and 2 clear; criterion 3 does not. The run is real and honestly recorded, but independent grading returned ends 6, 7 and 8 FAIL and end 4 NOT EXERCISED, and the criterion's comparative half has no baseline that could exist.
- Design expert: **NG at round 3**, with its three findings fixed at `142441f` and unreviewed. One standing objection recorded and not accepted (the axes split). Its most serious finding is carried, not closed: the delivery loop does not converge — 6 → 6 → 7 stumbles with every reported defect fixed in between — because step 11's reader brief can only ever report excess.
- Craft expert: **NG at round 3**, all findings fixed at `142441f` and unreviewed.
- Verification expert: **NG** — the mechanics verify cleanly; the evidence for criterion 3 does not cover the shipped body, because round 3 was not dogfooded.
- Ready to check off: **No.** Criteria 1 and 2 are met. Criterion 3 is not, and cannot be met inside this task: its three failed ends are the run departing from rules the body states but cannot detect, which is what the carried items — first among them the non-converging trial loop — exist to fix. The three-iteration triage cap is reached, so this is recorded and escalated to the user rather than fixed again here. The decision put to the user is to close #12 on criteria 1 and 2 with criterion 3 recorded unmet, take the nine carried gaps plus the round-4 field feedback as #13, and let #8 — the evaluation gate, whose own job is to run ends 1–8 and take the verdict — be where the ends are judged.
