# 8 Completion Check — Evaluation sign-off (Acceptance criteria run)

Sign-off task: no implementation expert, no review axes — this ledger records the coordinator's
end-to-end run of the steering Acceptance criteria (2026-08-22, at 7171a6d + this run's HEAD), one
row per criterion. Verdict is taken on PR #15 via `/rn:ty` / `/rn:gm`.

| # | Criterion (abridged) | Result | Evidence |
|---|---|---|---|
| 1 | Level A — SKILL.md exists, carries reader definition / five axes / pre-output self-check; process vs §output-rules layers split with addressee sentence; imperative, <2,000 words; no mermaid in prompt body | PASS | `writ/skills/up/SKILL.md`: step 2 (WHO/WHAT/HOW), Reference "The five axes" (article, guide, reference, record/ADR, evaluation), step 10 checklist. Layers: "The procedure" vs "Reference", addressee sentence verbatim at the Reference head. Body 1,999 words (file minus frontmatter, `wc -w`). No ` ```mermaid ` block anywhere in the file; the mermaid rule lives only in Reference → Form as a directive to the produced document. |
| 2 | Level A — two-tier quality encoded: ordered fresh-build procedure, floor as final net, seven tells named, §output-rules states both tiers | PASS | Order line lists 11 stages; "build the document fresh" (intro); step 9 "Clear the floor (the net)" runs after writing, quoting the seven-tell checklist table (padding/throat-clearing, restatement, generalities, flavorless connectives, reflexive bulleting, wavering voice, hedging); Reference "The two tiers" states floor (b) and ceiling (a). |
| 3 | Level B — dogfood: mermaid where order/branching warrants, no diagram/prose duplication; two reader definitions change voice and axis; single axis per document | PASS | `checks/5.md` (2026-07-07 two-reader dogfood): different axis and voice per reader, mermaid rendered only where branching warranted, single axis held. Task #7 four-run dogfood (`checks/7.md`) further exercised the fresh-reader trial on a real document. |
| 4 | Level B — floor clear in the rebuilt document; report leads with substance, closes with tells caught | PASS | `checks/5.md`: floor clean in both runs. Delivery paragraph (step 11) orders the note: structure/story/voice tied to reader or purpose first, "any tells step 9 caught" last. |
| 5 | Brush-up use case explicit: input = existing draft (read for intent), output = rebuilt document + what-changed note | PASS | SKILL.md line 9 "Input: a draft to revise… Output: the rebuilt document plus a what-changed note"; step 1 "read for intent — you will not reuse its sentences". |
| 6 | Frontmatter model-invocable, description fires for a human or Claude | PASS | No `disable-model-invocation`; description: "…it fires for Claude itself while drafting or revising such a document". |
| 7 | plugin.json has name/description/version/author; version only in plugin.json | PASS | `writ/.claude-plugin/plugin.json`: name `writ`, description, version `0.1.0` (semver), author lovaizu. `marketplace.json`: no `version` key on any plugin entry (checked programmatically). |
| 8 | writ/README.md exists, scenario + real-console style | PASS | Title, install console block, "Brushing a draft up" scenario with a real `/writ:up` console exchange. |
| 9 | marketplace.json writ entry + root README link, in sync | PASS | Entry: name/description/source `./writ`/category `writing`. Root `README.md` line 8 links `./writ/README.md` with a one-line description. No stale `techting` outside design.md's D-7 rename record (historical, permitted). |
| 10 | Both strict validations pass | PASS | `claude plugin validate ./writ --strict` and `claude plugin validate . --strict` → "Validation passed" (rerun 2026-08-22). |
| 11 | Headless `claude -p "/writ:up …" --plugin-dir ./writ` loads and starts the procedure | PASS | Rerun 2026-08-22 on a throwaway one-line draft: skill loaded, emitted step 1 `Subject: … / Must convey: …`, step-2 Gate fired on the audience-less input and produced the `Assumed reader: <who> / <what> / <how>` line. |
| 12 | All shipped artifacts in English | PASS | SKILL.md, plugin.json, CHANGELOG, both READMEs, marketplace entry, commit messages, PR #15 title/body — English. `instruction.md` stays Japanese per the steering source-of-record exception. |
| 13 | FB round (PR #15): content admitted only via the step-2 reader (heading + point), routed-out reported, unjustifiable slots escalated; fresh-context experiential verification gates delivery; ceiling = minimal reader effort, load-bearing claims at a glance; one voice + seven tells unchanged; dogfood flags the three named spots | PASS | Steps 3–4 and 7–8 admission with what-changed-note reporting and slot escalation ("never fill it silently"); step 11 fresh-context reader trial, "A clean pass … gates delivery"; ceiling restated around minimal effort with "load-bearing claims distinguishable at a glance"; voice table and seven-tell floor byte-identical through #7 (verified per round). Dogfood: spot 2 reproduced under the single-stance reader; spots 1 and 3 not reproducible in four trials — accepted as met in substance by the user's "(a)" verdict (2026-08-20), fold round for residuals landed and re-reviewed clean (`checks/7.md`). |

## Overall Verdict

- Coordinator run: 13/13 PASS (criterion 13's spots 1 and 3 stand on the user's recorded
  "(a)" acceptance in substance rather than reproduction).
- Ready for the gate: Yes — verdict awaited on PR #15 via `/rn:ty` / `/rn:gm`.
