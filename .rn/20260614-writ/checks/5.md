# 5 Completion Check

> Re-checked 2026-07-02 against the current build-fresh SKILL.md (commit 50de261). Task #5's original
> shape was superseded by D-5 (steering): the floor moved from a pre-scrub (step 2) to a final net
> (step 9), and the report format became "substance first, then the tells the net caught". Criteria
> below are judged against that D-5-revised reading.

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| Process names the AI-tell checklist (seven tells) and the build order is explicit — floor as final net per D-5 (original "floor before derivation" wording superseded) | OK | Step 9 "Clear the floor (the net)" points to §Floor checklist — a 7-row table (padding/throat-clearing, restatement, retreat into generalities, flavorless connectives, reflexive bulleting, wavering voice, hedging), each with Spot-it + Fix — and instructs name / quote / fix per tell. Build order is explicit at the top of "## The procedure": "understand → reader → outline → fill → story-check → voice & form → write → brush up → clear the floor → self-check & deliver" (L20), with the framing "step 9 is a net for stragglers, not the main tool" (L11). The original floor-before-derivation placement is intentionally absent — superseded by D-5 (steering D-5, 2026-06-27). | OK | QA 2026-07-02 verified the checklist runs to seven tells, consistent between step 9, the step-10 self-check, and §Floor checklist; the build order is explicit and matches D-5. No finding against this criterion in any round. |
| §Reference/output-rules states both tiers (floor = none of the named tells; ceiling = density, concreteness, single thread, earned diagrams/lists, consistent voice) and keeps the addressee sentence; body <2,000 words | OK | §The two tiers: Floor = "the document carries none of the seven tells in the floor checklist"; Ceiling = "density and concreteness (names, numbers, examples; noise cut); a single load-bearing thread, conclusion first, headings carrying the argument alone; figures and lists only where each beats prose; one consistent voice" (L97–98). Addressee sentence present at §Reference top (`grep -c 'not this SKILL.md prompt'` = 1, L91). `wc -w` = 1,734 whole file / 1,627 body excluding frontmatter — both under 2,000. | OK | QA measured on the final file (23dff57): 1,627 body words (<2,000), addressee sentence exactly once (L91), both tiers stated in §The two tiers. Word count held through both QA fix rounds. |
| Acceptance-criteria floor items (Level A) satisfied | OK | Checked each Level A bullet in steering against the file: ordered fresh-build procedure (do-not-edit-in-place stated at L11); floor scrub as final net with name/quote/fix (step 9); the seven tells named (§Floor checklist); both tiers in §output-rules (§The two tiers); the what-changed report is substance first, then the tells step 9 caught (step 10 Deliver, L85 — matches the D-5-revised wording; the note spec also names any off-axis material step 3 left out, closing the step-3 promise; topic-only authoring now defined there: the note reports the choices made — reader, axis, voice — instead of changes); brush-up frame stated (Input: draft to revise / Output: rebuilt document + what-changed note, L9; read for intent, not reused, L24); frontmatter `name: up`, third-person description firing for a human or Claude itself, `version: 0.1.0`, no `disable-model-invocation`; imperative body; no embedded mermaid (`grep -c '^```mermaid'` = 0). | OK | QA rounds tightened the what-changed note spec this criterion cites: round 1 defined it for topic-only input (f476506), round 2 added the off-axis line step 3 promises (23dff57); focused re-check closed both. All other Level A items verified with no finding. |
| Both strict validations pass | OK | Coordinator re-ran on 2026-07-02: `claude plugin validate ./techting --strict` → PASS and `claude plugin validate . --strict` → PASS. | OK | Validations re-run after each QA fix round (f476506, 23dff57) — both PASS on the final file (23dff57). |
| Dogfood (Level B): rebuilt document clears the floor; report shows substance first, then tells caught | OK | Run 2026-07-07 against the current SKILL.md, on a rough throwaway draft (`.rn/20260614-writ/dogfood/draft.md`, an unfocused pitch for `rn` full of padding/hedging/restatement) through two contrasting reader definitions. Reader 1 (developer new to `rn`, deciding whether to try it) → **Evaluation/survey** axis, neutral fact/judgment voice, no diagram (a short linear flow judged not to need one). Reader 2 (existing `rn` user wanting internals) → **Reference** axis, coverage-first voice with no closing, "Not specified in source" column instead of invented internals. Different axis, different voice, different form choices from the same source material — confirms derivation, not memorization. Both step-10 self-checks: all 9 items PASS, all seven tells named as caught in the what-changed note (padding, restatement, generalities, connectives, reflexive bulleting, wavering voice, hedging). A third, targeted check confirmed the mermaid half of Level B that the two full runs didn't exercise (neither test draft had genuine branching): fed a real three-branch triage rule (valid/invalid/escalation, with a capped retry loop) — correctly rendered as a mermaid flowchart with no prose duplication, judged "diagram warranted" because the loop cap is exactly the kind of detail prose buries. | OK | QA concurs: all four Level B sub-checks (voice/axis change, mermaid rendered when warranted, mermaid skipped when not, single axis per document, clean floor with tells-first-then-substance-report ordering) independently confirmed against live dogfood output, not simulated or assumed. |

## QA Expert Review

(Adversarial QA of 2026-07-02 against the build-fresh SKILL.md, covering #4 and #5 together — the
same rounds recorded in `4.md`. Round 1: PASS with 7 findings, 5 fixed in f476506, 1 rejected, 1
resolved by steering clarification 7bdba38. Round 2 re-review: PASS, 5/5 closed, 3 new low/info —
2 fixed in 23dff57, 1 rejected. Round 3 focused re-check on 23dff57: PASS, no new findings. The
earlier #5 round that failed on a 6-vs-7 floor mismatch targeted the superseded 1,900-word file and
is superseded by these rounds.)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Meaningful tests/verification | OK | QA verified the floor layer on the final file: seven tells consistent across step 9, the step-10 self-check, and §Floor checklist, each tell actionable (Spot-it + Fix); the net framing ("a net for stragglers, not the main tool") matches D-5; self-check gates delivery. Its fidelity pass surfaced real step-10 gaps (fixed in f476506), showing the review exercised the criteria rather than confirming them. |
| Edge case coverage | OK | Off-axis material and topic-only input — the two paths QA found underspecified — are now executable and reported in the what-changed note (f476506, 23dff57); headless/ask-or-infer covered; no contradiction between the two tiers and the five-axes / voice-table / diagram rules on the final file. |

## Expert Reviews (rn 0.8.0 migration — added 2026-07-07)

(Task #5 predates the 3-axis expert-review convention; QA above already ran. This section runs the
now-required Design/Craft/Verification axes, scoped to #5's own floor/ceiling material.)

### Design Expert

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Floor-as-final-net order is explicit and deliberate (D-5 reading) | OK | Order line (L20) and framing sentence (L11, "step 9 is a net for stragglers, not the main tool") confirm ceiling (step 8) precedes floor (step 9), cross-checked against `design.md` §4.2's guarantee text — matches almost verbatim. What-changed note orders substance → off-axis → tells (L85), matching the required reporting order. Both tiers stated in §The two tiers (L97-98); addressee sentence intact (L91). One cosmetic note (non-blocking): step-10 self-check lists the floor item (step 9) before the ceiling item (step 8), reversing procedure order — flagged, not fixed, since design.md doesn't mandate self-check ordering. |

### Craft Expert (writing)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Floor/ceiling material clean of the seven tells; floor-checklist table internally parallel; ceiling bullet's five qualities unambiguous | OK | Round 1: FAIL — floor-checklist row 7 (hedging) broke table parallelism (~3x sibling length); ceiling bullet's five qualities weren't cleanly delimited (ambiguous comma/semicolon grouping); step 9 vs the delivery paragraph disagreed on one-line-per-tell vs one aggregate line. First fix (commit 5354f7f) resolved the ceiling bullet and the note-wording mismatch, but left row 7 still an outlier plus introduced a grammar slip ("mark hypothesis" vs "state as decision"). Second fix (commit 451ded9) shortened row 7 to "State it as fact, hypothesis, or decision" — final re-check: PASS, all three findings closed, no new defect. |

### Verification Expert (fact-check)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Both strict validations pass; body <2,000 words; §4.2's claimed mechanism matches step 10; addressee sentence present once | OK | Live-ran both `claude plugin validate --strict` commands — both PASS. Body word count 1,790 (<2,000). design.md §4.2's claim ("step 10 re-verifies 'none of the seven tells remain'") matches SKILL.md L80 verbatim. Addressee sentence confirmed exactly once. One finding rejected as invalid: the criterion's literal phrase "floor→ceiling order" contradicts the file's actual (and design.md-confirmed) ceiling→floor implementation — but this is stale wording in task #5's frozen historical Completion-criteria text (kept verbatim per steering.md's historical-record exception), already explained by the D-5 supersession documented in this file's own header note and in `design.md` (D-5, §4.2). Not a SKILL.md defect; no fix applied. |

## Overall Verdict

- Self-check: OK on 5 of 5 criteria, judged against the D-5-revised reading (floor as final net, report = substance first then tells caught). Invariants verified 2026-07-02 on the post-fix file: mermaid = 0, 1,627 body words (<2,000), addressee sentence exactly once, two-layer split intact, seven tells consistent. Level B dogfood run 2026-07-07 (see Completion Criteria table above) — OK, all four sub-checks confirmed live.
- Design expert (2026-07-07): OK — floor-as-final-net order confirmed deliberate and correctly realized; 1 cosmetic note (self-check item ordering), non-blocking.
- Craft expert (2026-07-07): OK — 3 findings (table parallelism, ceiling-bullet grouping, note-wording mismatch), resolved across two fix rounds (5354f7f, 451ded9), final re-check PASS.
- Verification expert (2026-07-07): OK — all facts confirmed live (validations, word count, §4.2 mechanism, addressee sentence); 1 finding rejected as invalid (stale pre-D-5 criterion wording, not an artifact defect).
- Ready to check off (rn 0.8.0 gate): the 3 required expert-review steps are now satisfied; task #5's own Re-validate step stays open pending the session's Level B dogfood, per the criterion's own carve-out.
- QA: OK — PASS after two fix rounds (f476506, 23dff57); round-3 focused re-check clean.
- Ready for user review: Yes — deliverable pushed through 23dff57; awaiting user approval on PR #5 before the completion-marker check-off. Only the deferred Level B dogfood remains, by design.
- **Follow-up round (2026-07-02, user-approved during review)**: ba884d5 + f8136b3 — see `4.md` Overall Verdict for the full record. Relevant to #5's criteria: the seven-tells table's row-7 Fix cell now routes fact/hypothesis/decision (consistent with the extended §Throughout line); word count moved to 1,697 body words, still <2,000; addressee sentence intact; both strict validations PASS at f8136b3.
- **Round B (2026-07-02, user-requested — five axes re-sourced to researched best practice; steering D-6)**: a3f803c + c375c74, QA Low fixes in 9a43913 — see `4.md` Overall Verdict for the full record. Relevant to #5's criteria: the seven-tells floor checklist and the three-status rule are untouched across the round (QA re-check confirmed byte-identical); word count moved to 1,814 body words, still <2,000; addressee sentence exactly once; both strict validations PASS at 9a43913.
