# 6 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| `rn/docs/design.md` follows the new five-section, h3-question structure with no unanswered question and no undocumented decision | OK | `grep -n "^##" rn/docs/design.md` shows exactly the template's headings in order and numbering: `1. Background & Goals` (1.1-1.4), `2. Assumptions & Constraints` (2.1-2.2), `3. Design overview` (3.1-3.3), `4. Detailed design` (4.1-4.6), `5. Alternatives considered` (5.1-5.2) — matching `rn/references/design-template.md`'s template block verbatim on section order and heading text. Every h3 carries prose content (verified by reading the full file), none left as a bare heading. New mechanisms this session introduced each got their own `4.N`: 4.4 the question-driven design-doc contract, 4.5 the existing-design.md-update branch, 4.6 version-tracking + migration — each answers "what does it guarantee, and how is a breach caught" concretely (e.g. 4.6: "the check is placed as an unconditional early step in every one of the five skills' sequences — first in `ty`/`gm`, second in `on`/`dn`... seventh in `up`... none of the five branches around it"). Old-shape headings (Context & constraints / Approach / Structure / Flow / Open questions) are gone — the only remaining occurrence of that phrase is inside 4.4's prose, referencing the old shape by name to explain what it replaced, not a live heading. | | |
| the doc's own "Open questions" entry about "default home for a session's design.md" (existing line) is either resolved by #2's branch or explicitly carried forward with updated reasoning | OK | Resolved, not carried forward as open. §3.2's `design.md` row now carries a brief pointer, "(resolved in 4.5)", and §4.5 carries the resolving argument itself: "This same check also settles a question this document previously left open: whether `rn/docs/design.md` living at `rn/docs/` rather than under a per-session `.rn/` path is a special-cased exception to the per-session default. It is not — it is the ordinary outcome of this check: this canonical doc already covers `rn`'s own work area, so any `rn` session (including this one) whose work touches it is 'updating,' not authoring fresh. This resolves the prior open question on this point; there is nothing further to leave open here." The second old "Open questions" item (craft/verification specializations) was genuinely still undecided and out of this session's scope, so per the update procedure's step 5 it was removed from `design.md` entirely — the user explicitly decided against filing a GitHub issue for it, so it was instead given a destination inside this PR: recorded in `steering.md`'s State→Notes field as a concrete carried-forward item for a future session touching §3.1/§4, rather than left dangling. | | |
| `claude plugin validate rn --strict` and `claude plugin validate . --strict` both pass after this change | OK | Ran both commands after the rewrite: `claude plugin validate rn --strict` → "Validating plugin manifest: .../rn/.claude-plugin/plugin.json" → "✔ Validation passed". `claude plugin validate . --strict` → "Validating marketplace manifest: .../.claude-plugin/marketplace.json" → "✔ Validation passed". This task touched only `rn/docs/design.md` (a non-runtime reference doc), so no skill/plugin manifest was affected — confirmed by the passing validation. | | |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | | |

## Expert Reviews (axes the task needs)

### Design Expert (tasks that produce or revise structure/approach)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | | |
| System-wide integrity (interfaces, cross-doc consistency) | | |

### Craft Expert (coding / writing / visual — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | | |
| Consistency with existing style | | |

## Overall Verdict

- Self-check: OK
- QA:
- Design expert:
- Craft expert:
- Verification expert: N/A
- Ready to check off:
