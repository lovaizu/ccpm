# 5 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| all five SKILL.md files contain the version-check step, each citing migration-workflow.md rather than restating its procedure | OK | OK | `grep -n "migration-workflow\|Check version" rn/skills/{on,dn,up,ty,gm}/SKILL.md` confirms each of the 5 files has a "Check version" step that compares the session's `Rn version:` line against the installed plugin's version (read from `${CLAUDE_PLUGIN_ROOT}/.claude-plugin/plugin.json`'s `version` field) and, on a mismatch, runs `${CLAUDE_PLUGIN_ROOT}/references/migration-workflow.md` — none restates migration-workflow.md's own reconciliation steps (steering/design/tasks ordering, the carve-outs), it is only invoked by reference. Placement verified sensible per-skill: `on` step 2 (after planning stamps a fresh steering.md, before task #1 begins); `dn` step 2 (right after locating steering.md, all later steps and their internal step-number cross-references correctly renumbered 3-9); `up` step 7 (after task-sync/blocker-check/State-cleanup, before beginning the next task — so migration-workflow's own task reconciliation operates on the already-synced Tasks section); `ty` step 1 and `gm` step 1 (both before their existing logic, since neither skill otherwise names steering.md explicitly, with `gm`'s internal step references correctly renumbered). QA and Craft findings resolved through 2 fix-triage iterations, both re-verified PASS on final round (see QA/Craft columns). |
| a command run against a session with a matching recorded version takes no extra action (the check is a no-op on match) | OK | OK | Each of the 5 steps ends with the explicit clause "— on a match, do nothing," so a matching version never invokes `migration-workflow.md` or performs any other action; only a mismatch triggers it. Confirmed identical in all 5 files. |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | QA independently read all 5 files in full (not just the diff), verified every renumbered internal step cross-reference in `dn`/`gm` still resolves correctly, ran `claude plugin validate` itself rather than trusting prior claims, and found a real, substantive defect on its first pass (the `on`-skill dead-branch issue) rather than rubber-stamping. Final re-review after the fix: PASS, both criteria OK with quoted evidence. |

## Expert Reviews (axes the task needs)

### Craft Expert (coding / writing / visual — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | Went through 2 fix-triage iterations. Round 1: found `on`/SKILL.md's dead mismatch branch (same defect QA found, from the writing/consistency angle — reads as pasted-in boilerplate not adapted to context) — fixed in commit a9102a5. Round 2: found the fix's own wording ("structural parity") was single-use jargon absent elsewhere in the plugin's docs, plus a category mismatch (a control-flow branch can't "be" a relation) — fixed in commit 84c6c0a with plain, mechanical phrasing. Final re-review (this round, cap would be 3, resolved in 2): PASS — confirmed plain register, no category mismatch, and the "never fire" claim is consistent with how this plugin phrases similar unhedged claims elsewhere (e.g. `migration-workflow.md`'s own "never reads CHANGELOG.md"). |
| Consistency with existing style | OK | Verified the 5 instances' shared clause is verbatim-identical where behavior is identical, and deliberately differs only where each skill's context requires it (subject noun phrase: "the newly written," plain, "the active session's"). No terminology drift from `Rn version:` / `migration-workflow.md` / `steering.md` as used elsewhere. `on`'s extra trailing sentence (absent from the other 4) is justified asymmetry, not an unfinished patch — it's the only one of the 5 where the branch is provably inert. |

## Overall Verdict

- Self-check: OK — addressed a second-pass Craft finding ("structural parity" was single-use jargon plus a category mismatch) by rewording `on`/SKILL.md step 2's trailing sentence to plain, mechanical phrasing ("this branch can never actually fire here — it's kept only so `on`'s step matches `dn`/`up`/`ty`/`gm`'s..."), same length (see criterion 1 evidence); `claude plugin validate rn --strict` and `claude plugin validate . --strict` both pass; criteria still hold — step retained in all 5 files, match branch remains a no-op.
- QA: OK
- Craft expert: OK (PASS after 2 fix-triage iterations; both real findings resolved, not escalated)
- Design expert: N/A
- Verification expert: N/A
- Ready to check off: Yes
