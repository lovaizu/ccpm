# 3 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| steering-template.md's template block includes Rn version: and per-field guidance for it | OK | `rn/references/steering-template.md:21-27` adds prose explaining the field ("stamps the `rn` plugin version the session was authored under... written once, at creation... never user-edited afterward"); the fenced template block at line 32 now reads `Rn version: <the installed rn plugin's version, e.g. 0.7.0 — stamped at creation, not user-edited>`, placed above `Design:` per the real-instance ordering in `.rn/20260705-improve-design-template/steering.md` (`Rn version: 0.7.0` then `Design: rn/docs/design.md`). | OK | Confirmed the field and its dedicated guidance both exist; confirmed the `e.g. 0.7.0` example matches the real installed version in `rn/.claude-plugin/plugin.json` (not a fabricated placeholder); confirmed the pre-existing `Design:` paragraph was correctly updated from "the top `Design:` line" to "the `Design:` line below it" since `Rn version:` is now the new top line — no stale reference left. |
| planning-workflow.md's persist step names where the stamped value comes from (the installed plugin's version) | OK | `rn/references/planning-workflow.md:33` (Step 3, "Create steering.md" — the field-filling step that precedes the Step 5 disk-write) now instructs: "Stamp the template's top `Rn version:` line with the currently installed plugin's version — read from this plugin's own `${CLAUDE_PLUGIN_ROOT}/.claude-plugin/plugin.json` `version` field." This names the exact source field, confirmed against the real value read from `rn/.claude-plugin/plugin.json` (`"version": "0.7.0"`). | OK | QA's first pass flagged this as landing in Step 3 rather than the literally-named "Step 5" in this criterion and in steering.md's own task #3 Steps text. Re-traced against the actual file: Step 3 ("Create steering.md") is where every other top-of-block field is filled — `Design:` was already filled there before this task, unchanged by this task — while Step 5 ("Persist and open a draft PR") only writes the already-composed file to disk/git and opens the PR; it fills no fields. Placing `Rn version:` in Step 3 keeps it consistent with `Design:`, its direct analog. Steering.md's own task #3 Steps text (not the deliverable) was corrected in this session's coordination commit to say "Step 3, 'Create steering.md'" instead of the stale "Step 5" — the deliverable files needed no further change. |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | QA quoted the exact current prose for both criteria rather than confirming headings/presence, and independently checked the source-of-truth value (`plugin.json`'s real `version` field) rather than trusting the template's example text. Its "Step 5 vs Step 3" finding was a real, substantive check (not rubber-stamped) — triaged above as a wording correction in steering.md's own planning text, not a deliverable defect, since Step 3 is where `Design:`, the field's direct analog, is already filled. |

## Expert Reviews (axes the task needs)

### Craft Expert (coding / writing / visual — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | New prose is factually consistent with the real `plugin.json` version and correctly updates the adjacent, pre-existing `Design:` paragraph's stale "top line" reference rather than leaving it dangling. |
| Consistency with existing style | OK | New guidance mirrors the file's existing `Design:` explanation pattern exactly (short paragraph, backtick-quoted field name, same cadence); Step 3's inserted sentence in planning-workflow.md matches the verb-object-location shape of the sentence immediately after it, reading as a natural pair. Noted but non-blocking: the "never user-edited afterward" / "(see migration-workflow.md for how it gets updated on a mismatch)" pairing is a forward reference to task #4's not-yet-written file — expected given this session's task sequencing (task #3 has no prerequisite on #4; #4 depends on #3), resolved once #4 lands, not a defect in this task. |

## Overall Verdict

- Self-check: OK
- QA: OK (one finding triaged: steering.md's own task #3 Steps text had a stale "Step 5" reference, corrected to "Step 3" — not a deliverable defect)
- Craft expert: OK
- Design expert: N/A
- Verification expert: N/A
- Ready to check off: Yes
