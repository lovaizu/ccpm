# 1 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| design-template.md's five sections each carry h3 questions generalized from aiya's design.md, with no Conductor/CCS/Turn-specific wording remaining | OK | Template block has all 5 h2 sections (Background & Goals / Assumptions & Constraints / Design overview / Detailed design / Alternatives considered) with h3 questions matching aiya's shape (1.1–1.4, 2.1–2.2, 3.1–3.3, 4.1 repeatable, 5.1–5.2). `grep -nE 'Conductor\|CCS\|Turn\|verify-Turn' rn/references/design-template.md` returns no matches. | OK | QA re-derived independently from aiya's actual source and confirmed zero matches for all aiya-specific nouns; all 12 h3s generalized. |
| the per-section guidance states, for every question, that it must be answered with a decision and the intent behind it, and that "not applicable" is itself an answer requiring the same treatment | OK | Steps step 2 (now bold-only, no italics): "Answer every h3 question with a decision and the reasoning behind it ... If a question does not apply, say so **and say why** — 'not applicable' is itself an answer, and it gets the same decision-plus-reasoning treatment as any other." After the craft-review fix, the redundant closing-paragraph restatement under Per-section guidance was trimmed (it added no new content beyond step 2), and step 1 now cross-references step 2/3 directly; the rule itself still appears exactly once, in step 2, and applies file-wide — no section is exempted. | OK | QA confirmed the trim left the rule intact in step 2 plus a second anchor (the fence's top-of-template reminder), with no section left without access to it. |
| the old "drop an empty section" allowance is gone from the file (grep confirms no trace) | OK | `grep -n 'can be dropped' rn/references/design-template.md` returns no matches. Steps step 4 replaces it: "no section may be skipped for having 'nothing to record' — an empty-seeming section still owes step 2's decision-plus-reasoning answer, even if that answer is 'not applicable.'" The separate "if there is no design to record at all, write no design.md" guard is preserved in the same step. | OK | QA grepped independently for drop/skip/omit/equivalents; only legitimate uses (4.N add/drop-per-mechanism, and the explicit closure of the old allowance) remain. |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | Both QA rounds re-derived criteria from source (aiya's design.md, git history of the old file) rather than trusting the self-check's claims; second round specifically re-tested whether trimming duplicate sentences reopened a gap — it did not. |

## Expert Reviews (axes the task needs)

### Design Expert (tasks that produce or revise structure/approach)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | OK | Five-section shape coherent (why → ground → shape → mechanism detail → tradeoffs); intro framing, Steps, template block, and Per-section guidance all agree on names/numbering after the fix round. |
| System-wide integrity (interfaces, cross-doc consistency) | OK | No other repo file references the old section names or the removed "can be dropped" allowance; `rn/docs/design.md` still uses the old shape but that's task #6's explicit job, not a task-#1 regression. First round flagged two non-blocking nits (step 1 not flagging its `4.N` exception; contract/guarantee terminology drift) — both fixed and re-confirmed OK in round 2. |

### Craft Expert (coding / writing / visual — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | Round 1 found real duplication (Per-section guidance's closing paragraph and the Detailed-design bullet's tail both restated Steps rules verbatim), a mood break in step 4, and an italics/bold inconsistency. All fixed in round 2 and re-confirmed: no restatement remains, step 4 is imperative, no italics anywhere. |
| Consistency with existing style | OK | Bold-lead bullets, bracket-placeholder convention, and ending style all match `steering-template.md`; round 2 found no new issues introduced by the fix. |

## Overall Verdict

- Self-check: OK
- QA: OK
- Design expert: OK
- Craft expert: OK
- Verification expert: N/A
- Ready to check off: Yes — all completion criteria OK, all review experts OK across two rounds (round 1 found 6 valid findings, all fixed in commit `8baafe2` and re-confirmed clear in round 2).
