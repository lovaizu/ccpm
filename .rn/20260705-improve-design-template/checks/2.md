# 2 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| planning-workflow.md's design-location step explicitly instructs checking for a covering existing design.md before defaulting to a new path | OK | `rn/references/planning-workflow.md` Step 2, lines 21-30 (post-fix-round): bold-led "**Check for an existing design.md first.**", with a search hint ("check this repo/plugin's own canonical `design.md` … and any other active session's `design.md` whose `Design:` scope might overlap"), a partial-coverage clause ("Coverage need not be complete: if an existing design.md covers the core of the work's area, treat it as the update target …"), and unchanged fresh-path fallback. Terminology now says `design.md` (not `docs/design.md`) consistently with design-template.md. | | |
| design-template.md contains a clearly separate update procedure from the fresh-authoring one | OK | `rn/references/design-template.md`: "## Updating an existing design.md" moved to the end of the file (lines 98-135), after "## Per-section guidance" (79-96) — no longer wedged between the fenced template and Per-section guidance. File still has exactly two `---` (lines 30, 77), both bracketing only the fenced template block; the moved section separates purely by its own `##` heading, same as Per-section guidance does. "## Steps" (11-14) now carries a one-sentence forward-pointer marking it fresh-authoring-only and pointing to the Updating section. Own 6-step numbered procedure, distinct heading — unambiguous. | | |
| following the update procedure on rn/docs/design.md (task #6) does not require discarding and rewriting sections that did not change | OK | Step 2 (unchanged): "Revise only what changed … do not discard and rewrite sections that still hold." Re-traced against the actual `rn/docs/design.md` (147 lines, flat h2s, no h3s): step 4 (lines 112-125) now explicitly covers *relocation* ("trade-off/rejected-alternative reasoning that's currently embedded in an old 'Approach'-style section belongs under Alternatives considered, not Design overview") — this gives an executable instruction for `rn/docs/design.md`'s "Approach" section, whose "Over a fixed code-centric trio…" and "Over one agent that builds *and* reviews its own work…" prose is exactly this pattern, to move to §5 rather than stay mislabeled under §3. Step 5 (126-133) covers the "Open questions" section (its two bullets — craft/verification specializations, and design.md's default home) by requiring either a decision now or relocation outside `design.md`; since `rn/docs/design.md` is the plugin's own canonical doc with no session `steering.md`, the applicable branch is "track it outside `design.md` (e.g. a repo issue)" — no invented "open" placeholder inside the document, no full rewrite. Both gaps identified in the review (granularity mismatch requiring relocation, and no destination for open content) now have an executable, non-discard answer. | | |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | Each row cites the exact instructive sentence rather than just confirming a heading exists; the third criterion was checked by tracing the procedure's steps against the "no full rewrite" requirement, not just by confirming the words "only what changed" appear. |

## Expert Reviews (axes the task needs)

### Design Expert (tasks that produce or revise structure/approach)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | OK | The location check in planning-workflow.md Step 2 stays within Step 2 (does not touch Step 3's "force no empty design.md" rule, Step 4's design-sign-off task logic, or Step 5's design gate) — read back after edit, no other step needed changes. The update procedure in design-template.md sits after the template block and before Per-section guidance, so Per-section guidance (which describes what each h2/h3 should contain) still serves both the fresh-authoring Steps and the new update procedure without duplication. |
| System-wide integrity (interfaces, cross-doc consistency) | OK | planning-workflow.md's check names the exact target ("Updating an existing design.md" procedure in design-template.md) and design-template.md's new section names its exact trigger ("planning-workflow.md's location check (Step 2)") — cross-references point at real, now-existing headings/steps in both directions, no orphaned link. The new update procedure explicitly says the question+decision+intent contract "does not relax" for updates, so it does not contradict fresh-authoring's Steps 2/4 contract. |

### Craft Expert (coding / writing / visual — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | No italics introduced in either file (checked both full files post-edit). No new file created — both edits are additions inside the existing two files, scope-limited to Step 2 and a new `##` section respectively. |
| Consistency with existing style | OK | planning-workflow.md addition continues the existing bold-lead numbered-step prose voice inline within Step 2 (no new heading level introduced). design-template.md addition mirrors the file's own conventions: bold-lead numbered list like `## Steps`, `---` rules bracketing it like the template block, and a short lead-in paragraph like the file's opening paragraphs. |

## Overall Verdict

- Self-check: OK (re-verified after applying all 9 QA/Craft/Design review-finding fixes; all three completion criteria re-traced against the actual `rn/docs/design.md`)
- QA:
- Design expert:
- Craft expert:
- Verification expert: N/A
- Ready to check off:
