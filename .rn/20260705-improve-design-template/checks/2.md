# 2 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| planning-workflow.md's design-location step explicitly instructs checking for a covering existing design.md before defaulting to a new path | OK | `rn/references/planning-workflow.md` Step 2, lines 21-30 (post-fix-round): bold-led "**Check for an existing design.md first.**", with a search hint ("check this repo/plugin's own canonical `design.md` … and any other active session's `design.md` whose `Design:` scope might overlap"), a partial-coverage clause ("Coverage need not be complete: if an existing design.md covers the core of the work's area, treat it as the update target …"), and unchanged fresh-path fallback. Terminology now says `design.md` (not `docs/design.md`) consistently with design-template.md. | OK | Quoted the exact instructive sentence ("Check for an existing design.md first... Before defaulting to a new path...") rather than confirming a heading exists; order (check before default) is explicit in the prose itself. |
| design-template.md contains a clearly separate update procedure from the fresh-authoring one | OK | `rn/references/design-template.md`: "## Updating an existing design.md" moved to the end of the file (lines 98-135), after "## Per-section guidance" (79-96) — no longer wedged between the fenced template and Per-section guidance. File still has exactly two `---` (lines 30, 77), both bracketing only the fenced template block; the moved section separates purely by its own `##` heading, same as Per-section guidance does. "## Steps" (11-14) now carries a one-sentence forward-pointer marking it fresh-authoring-only and pointing to the Updating section. Own 6-step numbered procedure, distinct heading — unambiguous. | OK | Confirmed the update procedure is its own `##` heading with its own numbered steps 1-6, cross-referenced both ways with planning-workflow.md Step 2 — not a subsection or afterthought of the fresh-authoring Steps. |
| following the update procedure on rn/docs/design.md (task #6) does not require discarding and rewriting sections that did not change | OK | Step 2 (unchanged): "Revise only what changed … do not discard and rewrite sections that still hold." Re-traced against the actual `rn/docs/design.md` (147 lines, flat h2s, no h3s): step 4 (lines 112-125) now explicitly covers *relocation* ("trade-off/rejected-alternative reasoning that's currently embedded in an old 'Approach'-style section belongs under Alternatives considered, not Design overview") — this gives an executable instruction for `rn/docs/design.md`'s "Approach" section, whose "Over a fixed code-centric trio…" and "Over one agent that builds *and* reviews its own work…" prose is exactly this pattern, to move to §5 rather than stay mislabeled under §3. Step 5 (126-133) covers the "Open questions" section (its two bullets — craft/verification specializations, and design.md's default home) by requiring either a decision now or relocation outside `design.md`; since `rn/docs/design.md` is the plugin's own canonical doc, shared across sessions rather than scoped to this one, the applicable branch is "track it outside `design.md` (e.g. a repo issue)" — no invented "open" placeholder inside the document, no full rewrite. Both gaps identified in the review (granularity mismatch requiring relocation, and no destination for open content) now have an executable, non-discard answer.<br><br>**Terminology fix (commit f35e20a):** line 109 previously read "The question+decision+intent contract from fresh authoring still applies to whatever you touch" — a term used nowhere else in the file, sitting immediately before line 110's correct "decision-plus-reasoning answer," reading as a second, undefined contract. Reworded to "The decision-plus-reasoning contract from fresh authoring still applies to whatever you touch," matching the file's established term. `grep -n "question+decision+intent" rn/references/design-template.md` now returns no matches; `grep -n "decision-plus-reasoning" rn/references/design-template.md` returns 6 matches (lines 21, 28, 38, 106, 109, 129), consistent usage throughout. Re-read steps 2-4 (lines 106-126) together: the paragraph flows naturally, step 3 leads directly into line 110's "still owes the decision-plus-reasoning answer" without introducing an undefined second term. Scope-checked: only line 109 changed, nothing else in the file touched.<br><br>**This round's fixes (Craft/Design re-review, commit 82d2d17):** (1) step 4's closing sentence previously claimed the reconciliation "may already be current if `migration-workflow.md` has run since the document's last version-mismatch" — false, since `Rn version:` lives only on `steering.md` (confirmed against this session's own steering.md task #3/#4 and `rn/references/steering-template.md`) and `design.md` carries no version stamp of its own, so no "document's last version-mismatch" exists. Rewrote to state accurately that `migration-workflow.md` reconciles a session's own `design.md` automatically only while that session is active and its `steering.md` version is stale, and that this step remains the mechanism whenever the update procedure itself is invoked on a stale-shaped document — `grep -n "last version-mismatch" rn/references/design-template.md` returns no matches. (2) step 5's destination branch previously read "if there is no session `steering.md` (e.g. a plugin's own canonical design.md, not tied to a session)" — dead code, since this whole procedure is only ever entered from inside an active session's `planning-workflow.md` Step 2 (per this file's own opening line, line 100-102), so a session `steering.md` always exists when this step runs; verified against `rn/docs/design.md` task #6, which does invoke this exact procedure from within a session. Replaced with the real distinguishing question — session-scoped vs. canonical/cross-session — routing session-scoped items to that session's `steering.md` Notes and canonical/cross-session items (e.g. `rn/docs/design.md`) to an outside tracker; `grep -n "if there is no session" rn/references/design-template.md` returns no matches, and `grep -n "Session-scoped\|Canonical/cross-session" rn/references/design-template.md` confirms the new wording is present (lines 133, 135). Re-read the full "## Updating an existing design.md" section (lines 98-136) after both edits: reads as one coherent 6-step procedure, no leftover reference to the removed claims. | OK | Quoted the current prose of step 4 and step 5 directly (not paraphrased) to confirm both flagged issues are actually gone (`migration-workflow.md`'s scoping is now accurate; the destination branch is session-scoped-vs-canonical, both live/reachable cases) and re-traced against `rn/docs/design.md`'s real current shape (old five-heading, no-h3 structure, live "Open questions" section) — the procedure's steps map onto that real target, not a hypothetical, so no rubber-stamp risk. |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | Each row cites the exact instructive sentence rather than just confirming a heading exists; the third criterion was checked by tracing the procedure's steps against the "no full rewrite" requirement, not just by confirming the words "only what changed" appear, and re-verified against `rn/docs/design.md`'s actual current structure (old five-heading shape, live "Open questions" section) rather than a hypothetical target. |

## Expert Reviews (axes the task needs)

### Design Expert (tasks that produce or revise structure/approach)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | OK | The location check in planning-workflow.md Step 2 stays within Step 2 (does not touch Step 3's "force no empty design.md" rule, Step 4's design-sign-off task logic, or Step 5's design gate) — read back after edit, no other step needed changes. The update procedure in design-template.md sits after "## Per-section guidance" with an explicit redirect from fresh-authoring's Steps step 0, and its own 6 steps own read-first/revise-only/reconcile-shape/relocate-open-items/adjust-4.N with no overlap against fresh-authoring's Steps 1-4. |
| System-wide integrity (interfaces, cross-doc consistency) | OK | planning-workflow.md's check names the exact target ("Updating an existing design.md" procedure in design-template.md) and design-template.md's new section names its exact trigger ("planning-workflow.md's location check (Step 2)") — cross-references point at real, now-existing headings/steps in both directions, no orphaned link. Re-reviewed after this round's two fixes: step 4's `migration-workflow.md` scoping now matches task #3 (`Rn version:` on `steering.md` only) and task #4's not-yet-written scope (active-session-only reconciliation), no contradiction. Step 5's session-scoped-vs-canonical branch traced against a concrete case — `rn/docs/design.md` confirmed canonical (no owning session; both `.rn/20260625-rn-lean/` and this session's `steering.md` point `Design:` at it) — the branch is live and reachable, unlike the retired "no steering.md" condition which could never trigger. |

### Craft Expert (coding / writing / visual — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | No italics introduced in either file (checked both full files post-edit). No new file created — both edits are additions inside the existing two files, scope-limited to Step 2 and a new `##` section respectively. Round-2 re-review: flagged line 109's "question+decision+intent contract" as an undefined synonym for the file's established "decision-plus-reasoning" term used 5 times elsewhere — fixed (commit f35e20a) and re-confirmed: `grep` shows no remaining trace of the old phrase, 6 consistent uses of "decision-plus-reasoning" including the fixed line, paragraph (steps 2-4) reads naturally. |
| Consistency with existing style | OK | planning-workflow.md addition continues the existing bold-lead numbered-step prose voice inline within Step 2 (no new heading level introduced). design-template.md addition mirrors the file's own conventions: bold-lead numbered list like `## Steps`, `---` rules bracketing it like the template block, and a short lead-in paragraph like the file's opening paragraphs. Non-blocking note (out of this task's scope, pre-existing before this round's fixes): "Steps step N" phrasing at lines 110/138 reads slightly awkward ("Steps" the section name immediately followed by lowercase "step N") — internally consistent, not a defect introduced by this round, not tied to any completion criterion; left as-is. |

## Overall Verdict

- Self-check: OK (re-verified after applying all 9 QA/Craft/Design review-finding fixes; all three completion criteria re-traced against the actual `rn/docs/design.md`; further re-verified after this round's two narrow fixes to the "Updating an existing design.md" section — commit 82d2d17 — and again after this round's terminology-only fix to line 109 — commit f35e20a — per grep evidence in criterion 3's row above)
- QA: OK
- Design expert: OK
- Craft expert: OK
- Verification expert: N/A
- Ready to check off: Yes — all completion criteria OK per self-check and QA, Design and Craft both PASS after this round's fix-triage cycle (iterations 2 and 3 of the cap-3 cycle; both closed with no NG remaining)
