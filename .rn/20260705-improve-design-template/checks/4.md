# 4 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| migration-workflow.md names, in order, the three artifacts it reconciles (steering, design, remaining tasks) and what each is compared against | OK | OK | `## Steps` lists them in order: Step 1 — `steering.md` vs. `steering-template.md`; Step 2 — `design.md` (skipped if `Design:` line absent) vs. `design-template.md`'s "Updating an existing design.md" procedure; Step 3 — each remaining (unchecked) task vs. `steering-template.md`'s "Task definition requirements" table. Verified exact heading text against the current files: `design-template.md:98` reads `## Updating an existing design.md`; `steering-template.md:96` reads `## Task definition requirements`. Post-Design-review fix (migration-workflow.md:15-18): the ordering-rationale sentence previously claimed steering-before-tasks reasoning covered design's placement too, but step 3 never touches `design.md` — narrowed to the honest claim: steering must be reconciled first because design's location is read from its `Design:` line (step 2 depends on that), and both steering and design settle before tasks, which is where remaining forward-looking work is tracked. Also added a carve-out in step 3 (lines 39-43): a remaining task that already has in-progress work recorded (a `checks/{task-id}.md` file exists, or `steering.md`'s `State` section names it as current) is still reconciled, but the coordinator must flag that its already-recorded self-check/review evidence needs re-validation against the reconciled criteria before check-off — preventing silent verify-vs-spec desync. Post-QA/craft-finding fix (migration-workflow.md:40-44): "flag that..." was undefined — didn't say where the flag lives or how check-off would ever see it. Replaced with a concrete mechanism grounded in `task-verify-workflow.md`'s actual check file format and Phase: Complete step: the coordinator now records, in that task's `checks/{task-id}.md` Overall Verdict, a `Ready to check off: No — criteria reconciled, self-check/review must re-run` line. Verified against `task-verify-workflow.md`: the check file format block (lines 127-134) defines the exact `Ready to check off: Yes / No (reason)` field, and Phase: Complete step 1 (lines 208-209) states the coordinator checks off "With Verify cleared" — i.e. only once that field reads Yes — so the No line concretely blocks check-off on stale evidence rather than relying on an undefined "flag." Final fix-triage round (migration-workflow.md:39-52) closed two remaining gaps: (1) the OR-triggered carve-out ("a `checks/{task-id}.md` file exists ... or `State` names it as current") had no target to write into when only the `State` branch applied and no checks file existed yet — now explicit: if no `checks/{task-id}.md` exists yet, nothing is written, since there is no recorded evidence to invalidate and Execute will self-check against the reconciled criteria whenever it runs; (2) the `Ready to check off: No` write previously fired unconditionally — now gated on this task's own Completion criteria having actually changed during this reconciliation pass, with an explicit "no drift → no further action" branch when a task's Purpose/Prerequisites/Steps/Completion criteria already matched current convention. Re-read the full file once more after this edit; the three-sentence structure (unchanged-criteria branch, changed-criteria-with-existing-checks-file branch, changed-criteria-with-no-checks-file-yet branch) reads as one coherent paragraph, matches the file's plain sequential-prose style, and no other gap is visible. | | |
| the file states explicitly that reconciliation commits without a user gate and updates the recorded Rn version: line afterward | OK | OK | `## No user gate; then stamp the version` section states: "Apply the reconciling edits from all three steps and commit them directly. This is not one of the three scheduled gates... and it does not go through per-task QA/Craft/Design review... Once the reconciling edits are committed, update the session's `steering.md` `Rn version:` line to the installed plugin's version — the last step..." Confirmed the sentence order actually places the version-stamp update after the commit, matching the criterion's "afterward." | |
| the file does not rely on reading CHANGELOG.md version ranges — the comparison is always current-artifact-vs-current-template, regardless of which version the session started from | OK | OK | `grep -n "CHANGELOG" rn/references/migration-workflow.md` returns exactly one line (line 66), inside the final section `## Comparison is always current-vs-current`, which explicitly states the file "never reads `CHANGELOG.md` and never reasons about version ranges or deltas" — this is the required disclaiming statement, not a dependency on CHANGELOG. The section also states every comparison is "does this artifact match what the currently installed templates/workflows require, right now," independent of the session's starting version. Confirmed no other reference to CHANGELOG or version-delta logic exists anywhere else in the file. |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | QA quoted the exact current prose for all three criteria rather than confirming headings/presence alone, and independently verified line numbers and grep results against the actual committed file rather than trusting the self-check's citations. Confirmed the "no CHANGELOG" claim is structurally consistent throughout the file — every step (1, 2, 3) compares against "current" template/workflow content, never version-tagged history — so the disclaiming statement in `## Comparison is always current-vs-current` isn't a bolted-on claim contradicted elsewhere. Verified all cross-references (`design-template.md`'s "Updating an existing design.md" heading, `steering-template.md`'s "Task definition requirements" heading, `task-verify-workflow.md`'s `Ready to check off` field and Phase: Complete gate) resolve to real, verbatim-matching text in those files. |

## Expert Reviews (axes the task needs)

### Design Expert (tasks that produce or revise structure/approach)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | OK | Went through 3 fix-triage iterations before reaching PASS. Round 1 found: (a) the ordering rationale overclaimed that steering-before-tasks reasoning also justified design's placement, when step 3 never touches `design.md` — fixed by narrowing the rationale to what steps 2/3 actually support (`migration-workflow.md:15-18`); (b) step 3 had no carve-out for a remaining task with in-progress work, risking a silent Completion-criteria rewrite mid-flight — fixed by adding an in-progress carve-out. Round 2 found the carve-out's "flag that..." instruction was undefined (no artifact/mechanism named) — fixed by grounding it in `task-verify-workflow.md`'s actual `Ready to check off: Yes/No (reason)` check-file field and its Phase: Complete check-off gate (`migration-workflow.md:40-52`). Round 3 (final, cap reached) found two remaining gaps: the OR-triggered carve-out had no target to write into when only the `State`-names-it-current branch applied and no checks file existed yet, and the `Ready to check off: No` write fired unconditionally even without actual criteria drift — both closed in the same commit. Final re-review: PASS, no hard blockers. |
| System-wide integrity (interfaces, cross-doc consistency) | OK | Cross-doc references verified real and current: step 2 defers to `design-template.md`'s own update procedure rather than re-deriving it; step 3's carve-out is grounded in `task-verify-workflow.md`'s real check-file format and Phase: Complete's actual check-off condition, not an invented mechanism. One theoretical race condition remains (migration-workflow firing concurrently with Phase: Complete's check-off) — final Design re-review judged this outside this file's own invocation model ("Run once per invocation") and degrading safely if triggered; recorded as accepted risk, not a blocking gap. |

### Craft Expert (coding / writing / visual — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | Reference-doc prose is precise and unambiguous at each decision point (e.g. explicit "in this order" ordering claim, explicit skip condition for step 2, explicit three-way branch in step 3's final paragraph); appropriately defers to `design-template.md` and `task-verify-workflow.md` rather than restating their content, keeping this file proportionate to its own scope (reconciliation orchestration, not the mechanics it points to). |
| Consistency with existing style | OK | Matches sibling reference docs' established voice and structure: opening paragraph states what invokes the file and when, bold-lead numbered steps, backtick-quoted identifiers and file names, markdown cross-references via relative links (`[`task-execute-workflow.md`](./task-execute-workflow.md)` style). No terminology drift from established terms (`Rn version:`, `Design:`, `Ready to check off`). Noted, not a defect: the file isn't yet wired up by any `SKILL.md` — expected, since that wiring is task #5. |

## Overall Verdict

- Self-check: OK (updated after addressing Design-expert review: narrowed the ordering rationale to what steps 2/3 actually support, and added an in-progress-task carve-out to step 3 to prevent verify-vs-spec desync; updated again after a QA/craft finding that the carve-out's "flag" was undefined — grounded it in `task-verify-workflow.md`'s existing `Ready to check off: Yes/No (reason)` check-file field and its Phase: Complete check-off gate; final fix-triage round closed two remaining gaps in that same clause — handled the no-checks-file-yet branch explicitly (nothing to write; Execute self-checks later), and gated the `Ready to check off: No` write on this task's own Completion criteria having actually changed this reconciliation pass, not firing for a task left undrifted)
- QA: OK
- Design expert: OK (PASS after 3 fix-triage iterations; one theoretical race condition noted as accepted risk, not blocking)
- Craft expert: OK
- Verification expert: N/A
- Ready to check off: Yes
