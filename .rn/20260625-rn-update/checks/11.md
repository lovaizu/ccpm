# 11 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence |
|---|---|---|
| Running `/rn:on` behaves exactly as before the extraction (same prompts, same plan-gate sign-off via `/rn:ty`/`/rn:gm`, same `steering.md` produced) with all planning detail now living in `planning-workflow.md` and `on/SKILL.md` reduced to order-only orchestration (failure modes absent: a planning step lost in the move; `on/SKILL.md` still carrying step-level rationale or detail). | OK | Diffed every sentence: extracted `rn/skills/on/SKILL.md` original Steps 1-5 (the "Treat every user interaction as a proposal..." framing line plus all five numbered steps, lines 13-39 of the pre-edit file, captured via `git show HEAD:rn/skills/on/SKILL.md`) against the new `rn/references/planning-workflow.md` lines 10-36 with `diff` — output was empty (byte-identical), confirming no sentence was dropped, altered, or paraphrased. All 6 original numbered items now have a mapped destination: items 1 (Understand the goal), 2 (Propose the location), 3 (Create steering.md), 4 (Decompose tasks), 5 (Persist and open a draft PR — the plan gate, including the design-gate note and the sign-off-via-verdict-commands rule) → moved verbatim into `planning-workflow.md`; item 6 (Begin task #1) → stays in `on/SKILL.md` unchanged (still reads `task-execute-workflow.md` then `task-verify-workflow.md`). Post-rewrite `on/SKILL.md` Steps section is now only: Step 1 "Plan the session" (parses goal from `$ARGUMENTS`/message, hands off by reading `planning-workflow.md`, one-sentence summary of what that workflow does — no step-level detail, no slug logic, no PR/commit mechanics, no design-gate rationale) and Step 2 "Begin task #1" (verbatim carry-over). Grepped `rn/skills/up/SKILL.md` and all `rn/references/*.md` for cross-references to `on`/step numbers: `up/SKILL.md` only references `/rn:on` generically ("Run `/rn:on` to start") with no step-number dependency, so it still resolves; `planning-workflow.md`'s one internal "(Step 5)" reference points to its own Step 5 in the same file, not orphaned. No file outside the two touched paths was edited. |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | Round 1: found `on/SKILL.md` Step 1 duplicated `planning-workflow.md`'s own goal-taking clause and inline-summarized its 5 steps — a genuine order-only-orchestration violation, not rubber-stamped. Fixed in `81ec8f4`. Round 2: independently re-read both files, confirmed Step 1 now pure delegation symmetric with Step 2, all planning detail intact in `planning-workflow.md`. PASS. |

## Expert Reviews (axes the task needs)

### Design Expert (tasks that produce or revise structure/approach)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | OK | Round 1 found the same Step 1 duplication issue independently (contradicts design.md's own anti-duplication rationale); round 2 confirmed the fix restored genuine order-only orchestration, symmetric with Step 2, and consistent with `rn/docs/design.md`'s stated `planning-workflow.md`/`task-execute-workflow.md`/`task-verify-workflow.md` structure. PASS. |
| System-wide integrity (interfaces, cross-doc consistency) | OK | No stale references to old step numbers found anywhere in `rn/` (`up/SKILL.md`, `design.md` all consistent); `planning-workflow.md`'s intro matches `task-execute-workflow.md`'s established shape. PASS. |

### Craft Expert (coding / writing / visual — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | Round 1 independently found the same Step 1 duplication as a prose/redundancy defect. Fixed in `81ec8f4`; round 2 confirmed Step 1 now reads as a clean one-sentence delegation, no stranded information, "After approval" in Step 2 still resolves naturally via the frontmatter/intro. PASS. |
| Consistency with existing style | OK | Voice/terminology and the "Treat every user interaction as a proposal" framing correctly relocated intact; both rounds found the new file matches the established workflow-reference-doc shape. PASS. |

### Verification Expert (test / fact-check / dry-run — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | OK | Round 1 confirmed byte-identical content move via diff and grep; independently flagged the Step 1 duplication as a minor note even while giving an overall PASS. Round 2 independently re-ran the grep for duplicated clauses (zero hits), confirmed `planning-workflow.md` untouched by the fix (`git diff d559cfc..81ec8f4 -- rn/references/planning-workflow.md` empty), and confirmed the fix-round diff was scoped to exactly one line in one file. PASS. |
| Coverage (edge cases / claims / steps) | OK | Both rounds independently re-grepped for dangling references and duplicated content rather than trusting prior claims; no material claim left unchecked. |

## Overall Verdict

- Self-check: OK
- QA: OK
- Design expert: OK
- Craft expert: OK
- Verification expert: OK
- Ready to check off: Yes — self-check OK; round 1 found one real defect (Step 1 duplicating planning-workflow.md's goal-taking clause and step summary), unanimously flagged by all 4 axes; fixed in `81ec8f4` and re-verified PASS by all 4 axes in round 2; coordinator's own diff review (`git diff 56be739..81ec8f4`) confirms clean and scoped.
