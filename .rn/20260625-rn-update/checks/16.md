# 16 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| Every rn stop for user input while a session is active (its `steering.md` exists and is identified) opens with the status block per `status-display.md` — no in-session stop asks without it, the block alone conveys done / current / remaining plus the ask, and stops outside an active session (before steering exists, or none identified) are explicitly scoped out in the spec (failure modes absent: an in-session stop missing the block; a block that still requires opening `steering.md`; the ⬜ section rendered when nothing remains; a doc claiming coverage the wiring does not deliver) | OK | All fourteen in-session stop points carry the emit instruction — seven asks: plan gate on both branches, PR reported or console fallback (`rn/references/planning-workflow.md:35-38`), sign-off gate in both files' shared Process-selection bullet (`task-execute-workflow.md:68-69`, `task-verify-workflow.md:68-69`), escalation channel (`task-verify-workflow.md:199-200`, mirrored in the shared Review-gates line 48-49 of both), dn untracked-path ask (`rn/skills/dn/SKILL.md:38-41`), up dirty-tree ask (`rn/skills/up/SKILL.md:15`), up reconcile-phase asks via the step-2 blanket line "From step 3 on, any message stopping for user input opens with the session-status block" (`rn/skills/up/SKILL.md:22`, newly wired — covers e.g. a step-5 blocker), ty disambiguation ask (`rn/skills/ty/SKILL.md:13`); and seven flow-ending reports: pr-feedback no-PR abort (`rn/references/pr-feedback-workflow.md:28-31`), pr-feedback loop-completion report (`rn/references/pr-feedback-workflow.md:136-138`, newly wired — threads processed or none to process), task-verify session-close report when no unchecked tasks remain and the Evaluation sign-off was approved (`task-verify-workflow.md:217-219`, newly wired), ty flow-ending advance — evaluation gate closes the session or an accepted item has nothing following (`rn/skills/ty/SKILL.md:22`, newly wired), ty nothing-pending reply (`rn/skills/ty/SKILL.md:24`), gm post-revision report (`rn/skills/gm/SKILL.md:15`), dn suspend report (`rn/skills/dn/SKILL.md:54-57`; its closing "Tell the user to run /clear, then /rn:up" sentence dropped — the 👉 line now carries the next move per the spec's report rule). The report-stop class is settled intensionally: any message that halts the flow awaiting the user's next move, list e.g.-marked and extended (suspend, abort or loop-completion, nothing-pending, session-close), 👉 stating "where the session stands" (`status-display.md:13-16`); the report-variant grammar is in the Format section's 👉 bullet — `asking now:` replaced by where the session stands and the next move, with a concrete example line (`status-display.md:43-48`); a third example shows the report form, the /rn:dn suspend report (`status-display.md:75-83`), diff-verified byte-identical to the README dn block (`rn/README.md:64-68`). Out-of-session stops stay scoped out by the active-session boundary (`status-display.md:9-12`), and every conditional wiring now cites it in one normalized form "(subject to that spec's active-session boundary)" (`pr-feedback-workflow.md:30-31,138`, `ty/SKILL.md:24`, `gm/SKILL.md:15`, `up/SKILL.md:15`). ✅ membership's "explicit done annotation … in its heading" is now defined where tasks are defined: steering-template's Task-definition-requirements table gained a "Done annotation" row (done-but-awaiting an external gate, e.g. `— DONE through QA`, counts completed for the display) (`rn/references/steering-template.md:99`). Exhaustive halt audit found no in-class stop left unwired: planning's pre-steering asks, up's zero/multiple-candidate stops, and gm/ty with no session all fall under the spec's boundary; task-execute's body (lines 137-179) contains no user stop | OK | Round 4: QA's independent 14-entry ledger matched the wiring site for site; stop-language sweep over rn/ found no halt outside it; failure modes (a)–(d) each checked absent — the block is self-contained (status-display:29-52), empty ⬜ forbidden and respected in both no-remaining examples, coverage claims deliver exactly the wired class. |
| The record is consistent: CHANGELOG lists the feature, `design.md` records the structure, README's scenario shows it, and no rn doc contradicts another (failure mode absent: a stop-point instruction left contradicting `status-display.md`) | OK | All four docs state the same scoped claim, in the spec's own vocabulary: `rn/CHANGELOG.md:13` ("Once a session is underway, every message that stops for your input…" — with the four new wirings the universal claim is now delivered, so no CHANGELOG edit was needed), `rn/docs/design.md:86-93` (term unified to the spec's "flow-ending reports" — "flow-halting" is gone from rn/, grep zero — and the illustrative "among them" list now carries a report exemplar, `/rn:dn`'s suspend report, alongside the asks; wrap width kept), `rn/README.md:53` ("once a session is underway"; `steering.md` now backticked, matching its sibling CHANGELOG entry), `status-display.md:3-5`. README's `/rn:dn` scenario renders the block (`rn/README.md:64-68`) and its final "Run /clear, then start a fresh conversation with /rn:up." line is dropped — the 👉 line already states the next move, so the example no longer duplicates it (the standalone `> Run /clear yourself…` footnote at `rn/README.md:90` stays: it explains *why* the plugin cannot clear, not a repeat of the next move); the block was extracted and diff-compared byte-identical to the spec's new report-form example (`status-display.md:77-83`), and both trace true rule-by-rule: header = slug + goal one-liner, ✅ #1 labeled, exactly one 👉 in the report grammar (where the session stands + next move, no `asking now:`), ⬜ #3–#4 ranged with /-separated labels, closing parenthesized outlook. planning-workflow's preamble no longer uses the undefined "wired" vocabulary — now "At a stop that instructs opening with the session-status block, the block precedes the proposal" (`rn/references/planning-workflow.md:10`). Greps over rn/ confirm: zero hits for "flow-halting", "when a session is active", "At a stop wired", "Tell the user to run"; every status-display wiring remains a one-line citation, none restating the spec's rules. The two workflows' shared header stayed byte-identical after the task-verify edit — `diff <(sed -n '6,136p' task-execute-workflow.md) <(sed -n '6,136p' task-verify-workflow.md)` is empty (the session-close wiring sits in Phase: Complete, outside the shared span; task-execute-workflow.md untouched) | OK | Round 4: QA verified CHANGELOG:13 / design.md:86-93 / README:41-53,64-71 against the spec — no contradicting stop-point instruction remains; dn's removed handoff sentence is guaranteed by the report-form 👉 rule + its dn example; steering-template:99 agrees with the ✅-membership rule; planning-workflow:10 agrees with status-display:5. |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | Round 4: built a 14-entry stop-point ledger (every halt in rn/skills + rn/references) and checked each against wiring or the spec's boundary; verified the spec's e.g.-list members each map to a real wired stop; swept rn/ for stop-language to catch unenumerated halts — none found. Round 3 FAIL had caught the report-stop class gap (loop-completion + session-close unwired while docs claimed universal coverage), fixed in `cd47339`. |

## Expert Reviews (axes the task needs)

### Design Expert (tasks that produce or revise structure/approach)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | OK | Round 4: intensional class ("halts the flow awaiting the user's next move") + e.g. list judged the right settlement — decides future stops without list maintenance; single-spec/cite-only holds (every wiring a bare citation; the normalized boundary caveat names, not restates, the rule); up's step-2 blanket line coincides exactly with the spec boundary, sound not drift-prone. |
| System-wide integrity (interfaces, cross-doc consistency) | OK | Round 4: 👉 dual grammar coherent across Rules/Format/Examples; steering-template:99 Done-annotation row closes the ✅-membership contract with status-display:19-22 on both mechanism and location; no coverage claim exceeds the wiring. Round-2 3 Low findings (enumeration under-count, spec-opening width, up inline paraphrase) fixed in `bf66076`. |

### Craft Expert (writing)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | Round 4: single-source spec + citation-only wires judged the right doc shape; intensional definition with examples; normative rules separated from examples; both plan-gate branches specified. |
| Consistency with existing style | OK | Round 4: spec register in references, scenario register in README/CHANGELOG both held; README dn block byte-identical to the spec's report-form example; link conventions follow the pre-existing split (relative links in references, `${CLAUDE_PLUGIN_ROOT}` in skills). |

### Verification Expert (dry-run)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (flow traced) | OK | Round 4: all five skills + four workflow references traced end to end, including /rn:gm no-arg → queue-empty, evaluation approval → session close, up dirty-tree → hypothetical step-5 blocker; a session-close block constructed from the spec's rules alone with no improvisation. |
| Coverage (steps/branches) | OK | Round 4: five example blocks (3 spec + 2 README) confirmed mutually consistent as one payment-fix session at successive moments; stop-language grep surfaced no halt outside the ledger; all four criterion-1 failure modes checked absent. |

## Residual findings (accepted non-blocking; all four axes PASS despite them)

- ty/SKILL.md:13/:22 omit the boundary caveat that ty:24/gm:15/up:15/pr-feedback carry (flagged Low/Minor by all four axes) — not a contradiction: the boundary is Rule 1 of the cited spec and governs every citation; both stops presuppose an active session.
- status-display.md's Format code block shows only the ask-variant 👉 grammar; the report variant lives in the adjacent normative bullet + Example 3 (Craft Minor).
- status-display.md:46-48 un-numbered-stop examples name the plan gate/escalation but not session-close; the general clause covers it — Verification's constructed close block needed no improvisation (Verification Low).
- dn's "/clear, then /rn:up" next move is carried by the report-form 👉 rule + its dn example rather than a normative step-8 sentence (Craft Low).
- Cosmetic nits: design.md:91 compound subject with singular verb; "a stop that instructs" vs the spec's "a stop point instructs"; README:53 deictic gloss beside a no-✅ block; "abort report" naming inside the e.g. list.
- Rejected as Invalid across rounds: the two-register marker gloss (deliberate user-facing vs normative split, marker identity lives in the emoji); up dirty-tree × multiple-candidates ask stacking (pre-existing up flow design outside #16's criteria; block behavior deterministic per status-display:11).

## Overall Verdict

- Self-check: OK — round-4 fix (loop-completion and session-close wiring; report-stop class settled) re-traced end to end: the spec settles the class intensionally — any message that halts the flow awaiting the user's next move, e.g.-marked list extended with the loop-completion and session-close reports, 👉 stating "where the session stands" (`status-display.md:13-16`) — adds the report-variant 👉 grammar with a concrete example (`status-display.md:43-48`), and adds a third, report-form example diff-verified byte-identical to the README dn block (`status-display.md:75-83` = `README.md:64-68`); the two previously unwired report stops now reach emit instructions — pr-feedback loop-completion (`pr-feedback-workflow.md:136-138`) and task-verify session-close (`task-verify-workflow.md:217-219`, shared header 6-136 diff-verified still byte-identical, task-execute untouched) — plus ty's flow-ending advance (`ty/SKILL.md:22`) and up's reconcile-phase blanket line (`up/SKILL.md:22`), completing the fourteen-site wiring with no in-class halt left unwired (exhaustive audit: planning's pre-steering asks, up's zero/multiple-candidate stops, gm/ty with no session all fall under the boundary; task-execute body has no user stop); every conditional wiring uses the one normalized form "(subject to that spec's active-session boundary)" (`pr-feedback:30-31,138`, `ty:24`, `gm:15`, `up:15`); dn step 8's redundant closing sentence and the README example's duplicate next-move line are dropped (the 👉 line carries the next move); design.md unified to "flow-ending reports" with a report exemplar (`design.md:86-93`); planning's preamble drops the undefined "wired" (`planning-workflow.md:10`); steering-template defines the done annotation the spec's ✅ rule accepts (`steering-template.md:99`); greps for "flow-halting", "when a session is active", "At a stop wired", "Tell the user to run" hit zero across rn/ and every wiring is a one-line citation; CHANGELOG's Unreleased claim is now fully delivered — no CHANGELOG change.
- QA: OK — round 4 PASS (both criteria OK; 14-site ledger complete; findings Low/Info only). History: round 1 NG → `5fbeac0`; round 2 PASS with minor findings → `bf66076`; round 3 FAIL (report-stop class gap) → `cd47339`; round 4 PASS.
- QA (per criterion): criterion 1 OK — every in-session stop wired or blanket-covered, every unwired stop inside the spec's explicit boundary, all four failure modes absent; criterion 2 OK — CHANGELOG:13 / design.md:86-93 / README:41-53,64-71 consistent, no stop-point instruction contradicts the spec.
- Design expert: OK — round 2 PASS (3 Low, fixed `bf66076`); round 3 FAIL (class extension ambiguous, format grammar missing, two members unwired — fixed `cd47339`); round 4 PASS (structure, separation of concerns, system-wide integrity all OK).
- Craft expert: OK — round 4 PASS (prose clarity OK, voice/register consistency OK, doc-shape best practice OK; Minor/Nit polish only, recorded above).
- Verification expert: OK — round 4 PASS (all flows traced end to end; session-close block derivable from the spec without improvisation; five example blocks mutually consistent; Low/Info only).
- Ready to check off: Yes — self-check OK and all four review axes PASS on round 4; residual findings recorded above are non-blocking and accepted at the 3-iteration cap.
