# 4 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| Every contract in the approved design is present in what ships, and a third party can trace each one back to the design it came from. | OK | Design Contract Trace below covers every §2–§6 contract; each row names the design section and the shipped file/section carrying it. One patch round was needed (§5.4 symptom→remedy table, added in commit 2ee0d49) — after it, no §2–§6 contract is unshipped. | | |
| The plugin stands on its own: driving it end to end needs nothing it does not ship. | OK | Entry skills point only at `aiya:conductor`; conductor points only at `${CLAUDE_PLUGIN_ROOT}/references/*` (all seven ship) and the plugin's own three agent types; agents point only at `${CLAUDE_PLUGIN_ROOT}/references/*`. Verdict and CCS formats, which have no references/ file, ship inline in `turn-verify.md` / `turn-brief.md`. `claude plugin validate aiya --strict` → "Validation passed" (no warnings). Headless probe `claude -p "/aiya:dn" --plugin-dir aiya` executed the dn procedure end to end with no side effects (tree clean, reported resume point, handed off to `/clear` → `/aiya:up`). | | |
| No superseded decision survives anywhere in it. | OK | All shipped files are new in this task and written from the approved design/README only; the single pre-existing sentence contradicting the ratified conductor-as-sixth-skill decision (design §2 "exactly three kinds of part: five skills") was amended in commit f1d870f. `grep -ri "five skills" aiya/` now returns zero hits (the amended inventory reads "five command-word entry skills"; README's approved "Five command words" is untouched). `grep -rn smith` over the shipped plugin files returns zero hits — no legacy vocabulary survives. | | |

## Design Contract Trace

| Design contract | Shipped in |
|---|---|
| §2 two roles — Conductor holds purpose/never touches work; Turn touches work/never holds purpose; human only at gates | `aiya/skills/conductor/SKILL.md` §1 (preamble + do/never); all three `aiya/agents/turn-*.md` preambles |
| §2 wall 1 — Conductor reads only CCS/Verdicts/approved docs/paths, not even Report contents | conductor §1 (intake list, "carry Report paths, never Report content") |
| §2 wall 2 — Turns never nest; only the Conductor spawns; spawn tool omitted from Turn definitions | `tools:` allowlists in all three agents (no Task/Agent, no AskUserQuestion); conductor §1 ("only you spawn", direct children) |
| §2 part inventory — six skills (5 entry + Claude-only conductor) + 3 agent definitions + references/ | shipped tree: `aiya/skills/{on,ty,gm,dn,up,conductor}/SKILL.md`, `aiya/agents/turn-{generate,verify,brief}.md`, `aiya/references/*` (pre-existing); `aiya/docs/design.md` §2 amended to match (commit f1d870f) |
| §2 no controller program; rules default and observable | the whole loop is procedural prose in the conductor skill; no code shipped |
| §3.1 Phase → Plan → Step → Turn vocabulary; 1 Step = 1 Turn(generate) per attempt | conductor §3 opening |
| §3.1 one Turn kind, three work-order types; static definitions + parameters, never improvised | three static agent files; conductor §1 ("never improvise a work order outside these three definitions"), §5 dispatch parameters |
| §3.1 six gates; `ty`/`gm` two-word vocabulary; machines verify / humans review | conductor §3; `skills/ty`, `skills/gm` |
| §3.1 five command words on/dn/up/ty/gm | the five entry skills, meanings stated in each ("power on", "down — not quitting", "back up", "thank you", "gimme a change") |
| §3.1 investigation is first-class work | conductor §3 ("Investigation is first-class work in every phase"); turn-generate step 2 |
| §3.1 work too big → more flat Turns, never nested | turn-generate step 4 (surface, don't balloon); conductor §4 split rule |
| §3.2 immutable yardstick layer; change only via a gate | conductor §3 ("Yardsticks change only through a gate; you never edit one directly"); turn-verify (yardstick always a gate-approved immutable document) |
| §3.2 adaptive translation layer — Plan re-translated from zero on discoveries | conductor §6 |
| §3.3 run records CCS/Verdict/Report/Research and their writers; never yardsticks | turn-brief (CCS), turn-verify (Verdict), turn-generate (Report, Research); turn-verify refuses a moving state as yardstick |
| §3.4 ownership — Plan by Conductor, products by Turn(generate), verify per fixed/declared viewpoints, human owns approvals | conductor §4 ("You draft each phase's Plan…; Turns make every other product"), §3 gates; agents |
| §3.4 reverse side — verify never fixes/summarizes; brief never judges direction; generate never judges itself; Conductor not responsible for content quality | turn-verify preamble; turn-brief preamble; turn-generate return section; conductor §1 |
| §3.5 physical layout `.aiya/<issue>/` with 7 subdirs; file-naming (tNNN, step/viewpoint/attempt readable) | conductor §2 (layout verbatim); turn-generate (`s02-a1.md`), turn-verify (`s04-count-reconciliation-a2.yaml`), turn-brief (`tNNN.yaml`) |
| §3.5 formats in references/; Research deliberately format-free | conductor §3/§4 pointers to `${CLAUDE_PLUGIN_ROOT}/references/`; conductor §2 ("research/ … free-form"); turn-generate step 3 |
| §4.1 pipeline generate → verify ×viewpoint → mechanical AND → re-aim ≤3 → escalate with failure history | conductor §5 steps 1–5 |
| §4.1 verify never given Report or expected judgment | conductor §5.2; turn-verify ("ignore it and note the breach") |
| §4.1 missing return re-sent — accident, not an attempt | conductor §5.3 |
| §4.1 re-aim digs further on investigation Steps; attempt counter resets on settle | conductor §5.4/§5.5; turn-generate step 2 |
| §4.1 re-verify — new viewpoint without touching product or attempt count | conductor §5.4 |
| §4.2 waves derived from consumes, no wave file; same-file-write guard; platform cap 20 with slicing; per-Step pipeline, no lockstep | conductor §5 opening; `references/plan.md` (pre-existing, pointed at) |
| §4.2 one Turn(brief) per settled wave → one CCS regardless of width | conductor §6; turn-brief rule 5 |
| §4.3 keep planning — three questions in order; re-plan from zero, never diff; match ≠ "no change needed" | conductor §6 |
| §4.3 discretion line — Plan surgery yes, Purpose/Approach never; yardstick doubts go to a gate | conductor §4 condition 1, §1 |
| §4.3 finite steering verbs; investigating by oneself is a breach | conductor §1 |
| §4.3 doubt splits in two (viewpoint / investigation Step) | conductor §1 |
| §4.3 aggregation attribution — "does it require reading?" | conductor §1 |
| §4.4 three phases and their products; generation prerequisites (purpose/UX/design/plan), produce missing ones in aiya's formats + conversion Step | conductor §3 |
| §4.4 evidential soundness for Purpose and Research | conductor §3; turn-verify step 2; turn-generate steps 2–3 |
| §4.4 split drafting — Conductor elicits at chat surface, answers to research/ files on the spot, Turn(generate) drafts; user-question tool excluded from Turns | conductor §7; agents' `tools:` allowlists |
| §4.4 gates 3×{in,out}; G1–G3 name only outputs; bounded console summary + artifact on PR; `gm` no-arg takes PR review comments | conductor §3, §8; `skills/gm` |
| §4.4 dispatch rounds — self-run between stops, re-entry from file state | conductor §8, §9 |
| §4.4 progress board on console (README style ✅/👉/⬜, waves/items/attempts) | conductor §8 |
| §4.4 versioned approvals; Verdicts against old version invalidated, found by grep, re-verified; only new-yardstick fail re-aims | conductor §3; `skills/gm` step 2; turn-verify (`yardstick:` version pin) |
| §4.5 resume point = latest CCS + approved phase docs + current Plan, nothing extra saved | conductor §9; `skills/dn` step 2; `skills/up` step 2 |
| §4.5 `dn` commits and pushes; cycling dn → /clear → up; /clear is the user's | `skills/dn`; `skills/up`; conductor §9 |
| §5.1 Turn common contract rules 1–5; Conductor checks rule 4 on every return | agents' preambles and Return sections; conductor §5 ("Check every return for boundedness") |
| §5.2 Report — 3 fixed headings, ~20 lines, file not payload, only brief reads it | turn-generate (Report section, pointer to `references/report.md`); conductor §1 |
| §5.3 viewpoint = one concern; defaults = done when + phase criteria; extras from the Plan item; one flat verify per viewpoint | conductor §5.2; turn-verify |
| §5.3 Verdict YAML schema; written to disk by verify itself before returning; evidence mandatory; version pinned | turn-verify (schema ships inline — no references/verdict.md exists, by design §3.5 which lists no Verdict format) |
| §5.3 yardstick provenance fixed (never CCS/Plan) | turn-verify ("refuse in your Verdict's evidence") |
| §5.3 mechanical checks first, LLM thin last layer; runnable criteria | turn-verify step 1; turn-generate step 3 (criteria decidable as written); `references/purpose.md` |
| §5.3 product too large = scope signal, split the Step | turn-verify step 3 |
| §5.4 CCS post-verdict, third-person, replacement semantics; gap in CCS is the whole corrective instruction | turn-brief (work order + rule 2); conductor §5.4 (re-aim carries the gap) |
| §5.4 nine components with roles, sources, and starting `type` vocabularies; quote values with `:` | turn-brief (table + vocabulary + example — ships inline; no references/ccs.md exists, per design §3.5) |
| §5.4 product wins over Report; Report only for tried/unsure; lies caught at next verification | turn-brief rule 1 |
| §5.4 four boundedness rules (paths not inlined; 2,000-byte soft cap; Conductor intake = latest CCS + Verdicts only; one CCS per wave) | turn-brief rules 3–5; conductor §6 ("Your working state *is* that latest CCS — keep no growing summary of your own") |
| §5.4 breach symptoms → remedies (focal_entities/relational_map/uncertainty_signal) | conductor §6 (commit 2ee0d49) |
| §5.5 Plan item format + change log | `references/plan.md` (pre-existing), pointed at by conductor §4 ("follow it exactly") |
| §5.5 aiya-owned fixed viewpoints 1–4; Plan verified before gate and on every revision | conductor §4 (verbatim list; "not yours to write") |
| §5.5 three conditions for revision; split rule with fresh counters and partial-product consumes | conductor §4 |
| §5.6 Conductor does/never lists; doubt becomes a Step; complete intake list and the tell | conductor §1 |
| §6 three ratios, their definitions, where they are readable, 50% keep-rate heuristic | conductor §10 |
| README — banner style, dn/up wording, `/clear` note, six checkpoints, three-strikes escalation | conductor §8 (board style), §5.5 (escalation); `skills/dn`/`skills/up` (exact wording echoed); `skills/on` ("`on` happens once") |

Not shipped, deliberately: §7 (trade-offs — rationale, not contracts) and §1 (problem statement) define no runtime behavior; they remain in docs/design.md, which the plugin ships.

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | | |

## Expert Reviews (axes the task needs)

### Design Expert

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | | |
| System-wide integrity (interfaces, cross-doc consistency) | | |

### Craft Expert (writing)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | | |
| Consistency with existing style | | |

### Verification Expert (fact-check / dry-run)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (validate run / probe run / contracts traced) | | |
| Coverage (edge cases / claims / steps) | | |

## Overall Verdict

- Self-check: OK — all three criteria pass; `claude plugin validate aiya --strict` passes with no warnings (run 3×, after every artifact round); headless probe `claude -p "/aiya:dn" --plugin-dir aiya` responded correctly with no side effects; full §2–§6 contract trace recorded above.
- QA:
- Design expert:
- Craft expert:
- Verification expert:
- Ready to check off:
