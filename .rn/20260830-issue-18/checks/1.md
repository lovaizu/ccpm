# 1 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| The location and liveness facts are each recorded with the command run and the output observed, not asserted from reasoning. | OK | Fix round 2, all figures re-measured 2026-09-10 under 2.1.267. **Location**: `ls -d ~/.claude/projects/-Users-kiyo-work-lovaizu-ccpm*` (6 dirs); the `tr '/.' '--'` reproduction and the `/a/b.c` vs `/a/b-c` collision compressed to two lines but kept; the per-directory `relocated` loop (3/4 main checkout, 0/69 across five worktrees) with its command; `scan.py cwd $PROJ_MAIN/*.jsonl` showing 3 of 11-13 entries per stub carry a `cwd` — which is what the old "diverges on every entry" heading could not support and is now what the heading says; `relocated` at lines 7/10 of 11 and 7/12 of 13 (mid-file); `worktreeSession.originalCwd == preEnterOriginalCwd == relocatedCwd`. **Liveness**: `scan.py spans $PROJ_WT/*.jsonl` (five files, entries / with_ts / span); the 2026-09-06 newest-entry-vs-wall-clock block kept with the idleness confound named; and a new flush measurement — file mtime 0.102 s behind the newest entry, taken from inside a running Bash command, which also shows the in-flight call's own entry is not yet written. Append-only is no longer assumed: the same file hashed twice 105 s apart, 554,921 B / 126 lines -> 610,859 B / 150 lines with an identical first-40-line SHA-256. | | |
| At least one emission method is shown by recorded evidence to put an arbitrary string into the live JSONL, greppable before the conversation ends. | OK | Five channels land, each with its field path from `walk.py` (re-run 2026-09-10 against `$PROBE`, output byte-identical to the earlier round): `.message.content[0].text` (line 138), `.message.content[0].input.command` (139), `.message.content[0].content` + `.toolUseResult.stdout` (142), `.message.content[0].content` + `.toolUseResult.file.content` (144), and the subagent report at `queue-operation .content` + `user .message.content` (+119 ms / +66 ms, `scan.py handoff`). Greppable-before-the-end is now shown directly rather than inferred: a fresh token was emitted into Bash stdout at 11:07:47.607Z and grepped by the next tool call of the same turn at 11:07:59.006Z, hit count 1 (`grep -F -f`, so the token never touched the command line). The tightest cross-agent bound is re-derived from the recorded material at **11.1 s** (assistant text entry at line 159, 05:06:35.762Z; the concurrent subagent counted 160 lines at 05:06:46.849Z), replacing the 79.7 s figure; 82.1 s is recomputed from the correct first-probe anchor and marked as not load-bearing. Character-set scope restated, now including `<`, `>` and `&`. | | |
| Each reported hit is attributable to the emission itself — a hit that only matches the requesting instruction is not counted as a landing. | OK | The decorative guard is gone. The instruction that actually preceded the emissions is measured: the last `origin.kind: human` prompt before the probes is line 122 at 05:03:52.631Z, **3 characters** (the four human prompts in the file are 76 / 12 / 1 / 3 chars, listed with their command), and the tokens did not exist until `openssl rand -hex 8` ran in-conversation at line 127, 05:05:18.258Z — 86 s later. The work order is recorded as post-dating every hit (05:06:30.864Z vs 05:05:24-27Z), so counting tokens in it measures the wrong text. The emitter is named explicitly and consistently — the **coordinator** of `ef482a21`, `isSidechain: false` — and "probe" (an act) is separated from "token" (a string) in the Terms section. Line 134's extra p1/p2 hit is still identified as the coordinator's own `cat` and excluded. Re-runnability is stated rather than implied: a closing section names which blocks re-run and which are frozen on `$PROBE`. No raw token appears in the deliverable (`grep -c -F -f` returns 0 for all four). | | |
| Methods that landed and methods that did not are separately recorded; no method is reported as working without its observed evidence. | OK | The channel table now carries two **no** rows with their evidence: a subagent's own turns never reach the conversation file (subagent file grew 126 -> 150 lines while the conversation file stayed byte-identical at 578,631 B / 167 lines; the fresh probe returns 1 hit in the subagent file and 0 in the conversation file; `scan.py types` shows the isSidechain split, 48 of 167 entries carrying no such field), and a tool result over ~30 KB never enters the JSONL (`corpus.py persist-bracket`: largest inline 29,067 chars, smallest persisted 30,044 bytes, so the threshold is bracketed by the corpus; the entry carries `<persisted-output>` plus `toolUseResult.persistedOutputPath`/`persistedOutputSize` and the text goes to `tool-results/<id>.txt`). What crosses the boundary is stated: the final report only, and it is HTML-entity-escaped (`scan.py escaping`: `html.unescape(result) == report`, `&lt;`/`&gt;`/`&amp;` counted). Other measured facts, each with its command: compaction is 1 of 137 files, with P(reproduced in summary | appeared earlier) = 35/194 (~18%) measured in the decision-relevant direction; two continuation shapes — a new file replaying its predecessor's 229 uuids under a new `sessionId` with `sessionKind: bg` and a `continued-in` forward pointer (1 of 137 files), and a restart appending into the existing file across a version change (2 of 137 files span two versions, unbroken `parentUuid`, `bridge-session` ruled out as a seam record at 25 of 137 files); the file set moving from three to five files while the session is open, with one file on the machine carrying no timestamp anywhere; inversions split by entry type, running maximum 13,429.9 s at 2026-09-10T11:00Z and explicitly not a bound; 100 of 370 untimestamped entries; the full 19-type inventory including `cost-state`, `agent-name` and `continued-in`. Not-measured items stay in "What this still cannot answer", including the version stamp failing as a resume discriminator (`corpus.py version-range` shows overlapping version ranges). | | |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | NG (round 3) | Three scans are pointed at a narrower corpus than the prose generalises to; widening the same command to the named population produces a counterexample each time. Channel 5 is reported as landing a payload transformed only by HTML entities, while the same corpus shows the harness prepending a banner and rewriting `<` to `<\`. A whole class of emission methods — hook output, slash-command output, Write/Edit — is unexamined, and the omission is what makes the document's declared hardest open problem look unsolvable. See `1-review-qa-round3.md`. |

## Expert Reviews (axes the task needs)

### Craft Expert (writing)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | NG (round 3) | A headline claim (`:340`, `:24`) that the document's own output at `:367` refutes; a load-bearing transcript at `:301-304` the named tool cannot have produced; six location claims with no output and three with none at all, against the promise at `:5-6`. 335 of 872 lines inside fences, 11 blocks of inline Python belonging in the scripts the document itself introduces at `:81`. See `1-review-craft-round3.md`. |
| Consistency with existing style | NG (round 3) | A single-word discipline declared at `:51-52` and broken at seven sites; *session* defined at `:42` as the `rn` session then used for the Claude Code one; defensive draft archaeology at `:138-141` and `:275-276` against `output.md`'s ban. |

### Verification Expert (dry-run)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | OK (round 3) | 36 of 38 command blocks reproduce byte-identically or with upward-only drift; the scripts do what their docstrings say; the attribution argument holds under adversarial re-run. Two blocks are hand-assembled (`:302-304`, `:527`/`:531`). |
| Coverage (edge cases / claims / steps) | NG (round 3) | `corpus.py:58` filters on `stdout`, so the 30 KB threshold is a Bash property asserted of tool results — 280 non-stdout results exceed 30,000 chars inline, `Read` at 52,393. "No file on this machine carries real work and a `relocated` entry" is false; the document's own `relocated 14` tally at `:738` contradicts it. 183 of 254 hits sit at a snake_case `session_id` field the document says does not exist. See `1-review-verification-round3.md`. |

## Overall Verdict

- Self-check: OK
- QA: NG (round 3)
- Craft expert: NG (round 3)
- Verification expert: NG (round 3)
- Ready to check off: No — all three round-3 reviews returned Fail. Two fix iterations are spent of the three the workflow allows. Escalated to the user: whether #1's scope extends to probing the hook and slash-command channels, which the plan's four enumerated emission points do not cover.
