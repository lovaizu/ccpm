# 1 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| The location and liveness facts are each recorded with the command run and the output observed, not asserted from reasoning. | OK (round 5); OK (round-4-fix) | **Location, now machine-wide.** `corpus.py projdirs` over all 34 project directories and 155 cwd-recording files: 0 names outside `[A-Za-z0-9-]`; 32 of 57 (cwd, directory) pairs not reproduced by the `tr '/.' '--'` rule; **6 files filed under a directory no cwd of theirs reproduces**. `corpus.py relocated` over all 156 conversation files prints those same 6 with entry counts, assistant counts, relocated line positions, /clear positions and the `originalCwd==preEnterOriginalCwd==relocatedCwd` test. `scan.py cwd $PROJ_MAIN/*.jsonl` re-run (`04319f87` is gone from the corpus; the old `3 / 4` tally with it). **Liveness.** The 0.102 s mtime-vs-newest-entry block and the 2026-09-06 268-entry block are kept and now explicitly marked frozen with the reason; append-only is re-verified today — the same file is 1,466,701 B / 417 lines with the first-40-line SHA-256 still `809e0c5fd3437c3a`. The `<persisted-output>` notice text and the `ls` of the conversation directory, previously asserted or hand-formatted, are now command + real output. **round-4-fix:** the "seven files" sentence (file-set section) no longer contradicts the Placement section's own `5466c142` evidence — re-ran `scan.py cwd $PROJ_MAIN/5466c142*.jsonl` (cwd and gitBranch both name the issue-18 worktree, 3/13 entries) and reworded the sentence to "seven under `$PROJ_WT`" plus a new paragraph naming `5466c142` as an eighth, misfiled file the glob does not reach. The restart-seam `seam` block for `1b4dd5b8`/`f96e5843` was re-run verbatim (`scan.py seam ~/.claude/projects/.../1b4dd5b8-*.jsonl ~/.claude/projects/.../f96e5843-*.jsonl`); the quoted `## 1b4dd5b8` block was missing its first line (`assistant v=2.1.239 ts=2026-08-24T00:10:29.231Z uuid=271452ce parent=ac038e8d`), now added so both windows show 5 lines; `1f14e004`'s parent is still `c7b6b082`, `52192322`'s parent is still `2c62a778`, and both files still report "entries whose parentUuid is absent from the file: 0". | | |
| At least one emission method is shown by recorded evidence to put an arbitrary string into the live JSONL, greppable before the conversation ends. | OK (round 5) | Six channels land, each at its field path. Greppable-before-the-end is shown twice: the frozen same-turn probe (emitted 11:07:47.607Z, grepped back 11:07:59.006Z, hit 1 in the subagent file and 0 in the conversation file) and the live channel-6 probe (`grep -c` returned 2 at line 26 of `11b1c7b5`, twelve entries before that conversation closed). The cross-agent 11.1 s figure is no longer a hand-assembled `walk.py` fence: it is re-derived by a command that prints entry 16 of `$SUB` and cuts at `=== token counts ===`, output pasted verbatim. **"Arbitrary" is withdrawn** from summary row 2 — what was emitted was hex on channels 1–5 and `HOOKPROBE <event> <token>` on channel 6, and row 8 carries the scope. | | |
| Each reported hit is attributable to the emission itself — a hit that only matches the requesting instruction is not counted as a landing. | OK (round 5) | Attribution now rests on timing alone; the field-path argument is withdrawn, because the machine-wide `corpus.py needle` census shows quoted text reaching `.message.content[N].input.command` and `.toolUseResult.stdout` — the same paths channels 2 and 3 land at. The confounder that actually materialised is named as such: line 134's `cat` of the token files is a second instance of the Bash-output channel, 0.374 s after the `openssl` at line 127 (both timestamps re-read from the file). Human prompts before the probes re-verified at 76/12/1/3 chars, last one 86 s before the first token existed. Channel 6: `scan.py prompts` over all eight `user` entries of the four probe files, only run 2's deliberate `cat` holds a token. `grep -c -F -f` returns 0 for all three live tokens against the document and against every script. | | |
| Methods that landed and methods that did not are separately recorded; no method is reported as working without its observed evidence. | OK (round 5); OK (round-4-fix) | **round-4-fix:** the "Two hook placements..." subsection (row 6's negative finding) sat, as a nested `####`, physically before the "Two channels do not land" section (row 5's negative finding) even though the summary table lists row 5 first; moved the hook-placement subsection (promoted to a `###` sibling) to after "Two channels do not land," so section reading order now matches the table's numeric order — verified by `grep -n '^### '` showing "Two channels do not land" at line 808 and "Two hook placements..." at line 926, and confirmed no cross-reference inside the moved block depended on its old position (the one internal link, to the two-channels section, now points backward instead of forward, which still resolves). Also fixed the channel table's row-5 pointer at the summary table (row 2): "see row 5's neighbours in the channel table" → "see row 5's own cell in the channel table," since the "not reliably" explanation is already inline in that row's own cell. Three "landed" claims were overstated and are corrected against re-run output. **Channel 5** is now measured over all 18 handoff rows of `ef482a21`+`555280be` and all 316 task-notifications on this machine (`corpus.py handoffs`): 29 `failed` — where `<result>` carries the agent's last progress note, not a report, shown for line 305 — and 44 neutralised, where the harness prepends a 248–271-char banner and rewrites `<`→`<\` and `[harness:`→`[\harness:`. The neutralisation is shown **not invertible** (lines 233/244 quote `<\` themselves). `marker-prefix-forgery` is recorded as a named trigger. The wrapper's `tool-use-id` tag is absent on 6 of 316, and a fourth failure mode is now counted: **142 task ids in 33 files are enqueued by a `queue-operation` entry and removed again without ever becoming a `user` entry**, so channel 5's `user`-entry landing is not guaranteed at all. **The >30 KB negative** is corrected: `corpus.py persist-bracket` widened past `stdout` shows 157 non-stdout results over 30,000 bytes kept inline (largest 82,749), the bracket restated in bytes (29,829 → 30,044), and the kept text shown to be the **head** in 190 of 191 surviving side files and the tail in 0. **The subagent-turn negative** is strengthened machine-wide: `corpus.py sidechain` finds 0 of 156 conversation files holding an `isSidechain: true` entry. The continuation claim is corrected by `scan.py replay`: 136 of 141 uuid-less entries are dropped, `sessionKind: bg` on 256 of 296 not all, and a `queue-operation` copy does not survive. Compaction is re-stated at the conditioned rate. | | |
| The hook channel is measured on the same terms as the other four — whether it landed, in which fields, and with its probe text traceable to the emission rather than to the instruction. | OK (round 5, unchanged in substance and fully re-run) | Every channel-6 block was re-executed today and reproduces byte-identically: `scan.py spans`, `prompts`, `walk.py` on runs 1 and 2, `hookraw`, `hooks`, `hookids`, the h1/h2/h3 diagonal (5/0/0, 0/7/0, 0/0/5) and the `sessionend` zero-count. The two machine-wide censuses drifted as expected and are re-quoted at their new minute: `hook-entries` now reads 156+396 files with `hookEvent`-as-string at 65 (was 43) and as-key still 0; `attachment-types` now 531 `skill_listing`. `fired.log` is now marked frozen, since the throwaway plugin that wrote it is gone. | | |
| Whether a hook emission can be told apart from text that quotes the same string is settled by recorded evidence — either a discriminator shown to work, or a recorded failure to find one. | OK (round 5, re-verified) | The discriminator holds at the wider population: `corpus.py hook-entries` over 156 conversation + 396 subagent files still finds `hookEvent` as a JSON key only inside a hook attachment (0 elsewhere) while the substring count rose from 43 to 65 in fifteen minutes — the divergence between the two tests is now stated with both readings. Both limits are kept and re-checked: `attachment.command` is what pins a hit to one plugin, and `corpus.py attachment-types` still shows 3 of 32 types carrying `.attachment.content`. The self-reference section is amended to point at the timing-based attribution rather than at field paths. | | |
| Every figure in the document states the population its scan covered, and that scan covers the population the surrounding sentence generalises to. | OK (round 5); OK (round-4-fix); OK (round-6-fix) | **round-6-fix:** three findings fixed. (1) The channel-5 handoff table said "seven conversation files" but the shown command only globbed two (`ef482a21`, `555280be`); re-ran `scan.py handoff $PROJ_WT/*.jsonl` against all 7 and pasted the real 28-row output (20 distinct handoffs, not 10) — `17080efa`/`6ed86163`/`eded9b12` hold none, and the two files the old glob skipped, `c391e411` (6 handoffs) and `c763d0be` (4), are exactly where the counterexamples were: real single-notification delays now range 43 ms–37,372 ms (`a6e9b1e51c9ce499a`, `c391e411` line 575), traced to the coordinator being mid-turn when the notification was enqueued — delivery waits for the harness to have a gap between turns, so the delay is bounded by the coordinator's own turn length, not a fixed channel property; and one row (`a0b0b21899209706e`) shows −1,355,544 ms, investigated and explained as a script mismeasurement, not a real precedence violation: the task-id notified twice (a `failed` session-limit stop, then a resumption after `SendMessage to: a0b0b21899209706e` at 05:02:13.692Z), `scan.py`'s `_report()` takes the subagent file's unconditional last assistant text, so it paired the *first* (failed) notification with the *second* run's completion text; the resumed run's own `<task-notification>` was enqueued then removed (line 342/345) without ever landing, and what the coordinator actually received for it was a different wrapper, `<agent-message from="…"> [Subagent hand-back]` (line 335) — stated in the document as a real hazard (a resumed task-id's second completion can vanish from `<task-notification>` delivery and surface only as a hand-back, or not at all) rather than buried. The prose conclusion "14–119 ms on all ten" is replaced with the honest range and both explanations. (2) `worktree-ptr`'s description said "reads only the ccpm project directories," but reading `corpus.py`'s `worktree_ptr()` shows it iterates `CONV` (`pop()`, no `CCPM` restriction) — the same machine-wide default as the other unlisted scans, and the document's own quoted output for it already lists `dotfiles` rows. Reworded to state it reads every conversation file, not just ccpm's, matching the code; spot-checked every other named scan in that list (`versions`, `version-range`, `no-timestamp`, `entry-types`, `relocated`, `sidechain`, `continued-in`, `compaction`, `handoffs`, `inversions` — machine-wide-only, correctly unlisted; `persist-bracket`, `attachment-types`, `hook-entries`, `localcmd`, `needle` — `pop(subs=True)`, correctly listed as reading subagent files too) against its function body: no other mismatch. (3) The summary table's "830 places... as of 2026-09-20T06:00Z" inversions figure had no command anywhere in the document producing 830 — re-ran `corpus.py inversions` fresh now (935 pairs, 162 files, 2026-09-20T08:48Z) but the document's own body already shows a real, complete `corpus.py inversions` run (808 pairs, 156 files, 2026-09-20T05:37Z, lines ~1481–1502) that the surrounding prose treats as canonical — rather than add a third, unreferenced number, pointed the table row at that exact shown figure ("808 places... as of 2026-09-20T05:37Z — the `corpus.py inversions` run shown in this section's own body"), keeping one real number referenced from both places. **round-4-fix:** the Terms glossary said "nineteen types are in use on this machine" for a figure backed only by `corpus.py entry-types`, which scans the 156 conversation files, not the ~396 subagent files — reworded to "in use across conversation files on this machine," matching the population `entry-types` actually prints and the phrasing the Ordering section itself uses ("over every conversation file on this machine"). The summary table's inversions row said a frozen "805 places" while the body already showed a larger, later re-run figure and explicitly frames the count as a running maximum, not a bound — re-ran `corpus.py inversions` fresh (830 backward-running pairs, 158 conversation files scanned, 2026-09-20T06:00:58Z) and rewrote the table row as "830 places machine-wide as of 2026-09-20T06:00Z — a running maximum over a growing corpus, not a fixed count," matching the body's own established convention for a live-growing figure instead of swapping in one more number that will go stale. This was the failing criterion and the sweep was the task. **Every numeric claim in the document was re-derived, and every scan behind a machine-wide sentence is now machine-wide.** New or widened scans: `corpus.py relocated`, `sidechain`, `continued-in`, `compaction`, `projdirs`, `handoffs`, `inversions`, `needle`, `localcmd`; `persist-bracket` widened past `stdout`; `worktree-ptr` widened from the ccpm directories to all 156 files; `scan.py escaping`/`handoff` widened from one file to three and their `_reports` bug fixed (the subagent file is now found through the `<output-file>` symlink, which recovers the 8 handoffs the old path-building silently dropped, and the report is the last assistant *text* entry rather than the file's last entry, which was an error entry for the three agents that died). **Every `corpus.py` scan now prints its population as its first line**, so a figure quoted from it cannot lose it. Widening changed the finding in five places, and the document says what the widened scan shows: relocation (3 of 6 files hold real work; `/clear` not necessary), the 30 KB cap (Bash-stdout only, head-truncation), the replay (verbatim/`sessionKind` both false, `session_id` back-pointer found), the self-reference hits (194 of 254 structural, not 1), and compaction (4% for the once-occurring cohort, not 18%). Re-run diff: all 19 tool blocks re-executed and compared line-by-line against the document; the only differences are live drift in three censuses — `inversions` 805→809, `needle` 9→10 paths, `localcmd` — each of which now states in prose that it climbs while the document is edited, plus the blocks explicitly marked frozen. | | |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | NG (round 3) | Three scans are pointed at a narrower corpus than the prose generalises to; widening the same command to the named population produces a counterexample each time. Channel 5 is reported as landing a payload transformed only by HTML entities, while the same corpus shows the harness prepending a banner and rewriting `<` to `<\`. A whole class of emission methods — hook output, slash-command output, Write/Edit — is unexamined, and the omission is what makes the document's declared hardest open problem look unsolvable. See `1-review-qa-round3.md`. |

## Expert Reviews (axes the task needs)

### Craft Expert (writing)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | NG (round 3) | A headline claim (`:340`, `:24`) that the document's own output at `:367` refutes; a load-bearing transcript at `:301-304` the named tool cannot have produced; six location claims with no output and three with none at all, against the promise at `:5-6`. 335 of 872 lines inside fences, 11 blocks of inline Python belonging in the scripts the document itself introduces at `:81`. See `1-review-craft-round3.md`. |
| Consistency with existing style | NG (round 3) | A single-word discipline declared at `:51-52` and broken at seven sites; *session* defined at `:42` as the `rn` session then used for the Claude Code one; defensive draft archaeology at `:138-141` and `:275-276` against `output.md`'s ban. |

### Verification Expert (dry-run)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | OK (round 3) | 36 of 38 command blocks reproduce byte-identically or with upward-only drift; the scripts do what their docstrings say; the attribution argument holds under adversarial re-run. Two blocks are hand-assembled (`:302-304`, `:527`/`:531`). |
| Coverage (edge cases / claims / steps) | NG (round 3) | `corpus.py:58` filters on `stdout`, so the 30 KB threshold is a Bash property asserted of tool results — 280 non-stdout results exceed 30,000 chars inline, `Read` at 52,393. "No file on this machine carries real work and a `relocated` entry" is false; the document's own `relocated 14` tally at `:738` contradicts it. 183 of 254 hits sit at a snake_case `session_id` field the document says does not exist. See `1-review-verification-round3.md`. |

## Overall Verdict

- Self-check: OK (round 5 — every numeric claim re-derived at its own population; three scans widened to the machine produced counterexamples, and the document now says what the widened scan shows)
- Self-check (round-6-fix, against three findings on the channel-5 handoff table, the `worktree-ptr` description, and the "830" inversions figure): OK — (a) the handoff table is now a real 7-file, 28-row scan whose two large-delay and one negative counterexamples are explained rather than hidden (turn-length-gated delivery; a resumed task-id mismeasured by `_report()`'s unconditional last-assistant-text); (b) `worktree-ptr`'s description now matches `worktree_ptr()`'s actual machine-wide population, and every other scan in that list was spot-checked against its function body with no further mismatch found; (c) the "830" inversions figure is replaced with "808... as of 2026-09-20T05:37Z," pointing at the exact `corpus.py inversions` run already shown in the document's body, so the table and the body now cite one real, shared number.
- Self-check (round-4-fix, against the round-3 QA/Craft/Verification findings): OK — all six findings fixed and re-verified against re-run commands: (a) "seven files" no longer contradicts `5466c142`'s own cwd/gitBranch evidence; (b) the hook-placement-failures subsection now reads after the channels-1–5 negative section, matching the summary table's row order; (c) the row-2 cross-reference now correctly names row 5's own cell; (d) "nineteen types" now states the population `entry-types` actually scanned; (e) the inversions count is now a timestamped, explicitly-running figure instead of a frozen number that can silently diverge from the body; (f) the `seam` block's `1b4dd5b8` window is now complete and verbatim (5 lines, matching `f96e5843`'s 5), with the `1f14e004`/`c7b6b082`, `52192322`/`2c62a778` parent links and the zero-dangling-`parentUuid` claims re-confirmed unchanged in both files.
- QA: NG (round 3)
- Craft expert: NG (round 3)
- Verification expert: NG (round 3)
- Ready to check off: No — all three round-3 reviews returned Fail. Two fix iterations are spent of the three the workflow allows. Escalated to the user: whether #1's scope extends to probing the hook and slash-command channels, which the plan's four enumerated emission points do not cover.
