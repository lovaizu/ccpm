# 2 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| Every decision listed in Steps is present in `design.md` with its reasoning and the alternative it was chosen over; none is stated as a bare choice. | OK | Each Steps item maps to a reasoned decision: location + no session-identifying string (`design.md` 2.1 lines 94–127, 4.7 "Locating a session's own markers…" lines 434–448); marker form + emission method = plugin `PostToolUse` hook, channel 6 (4.7 lines 394–423, chosen over channels 1–5 per 5.1 lines 587–598); emission points = continuous restamping, not start/complete, with abandoned/re-done handling (4.7 "Continuous restamping…" lines 409–423, chosen over discrete events per 5.1 lines 599–607); marker naming (session slug + task description, not `#N`) (4.7 lines 425–432, 5.1 lines 618–622); friction store location + record shape (4.8 "Where facts are stored…" lines 490–502); friction bar (4.8 "The bar for a friction fact…" lines 479–488); stocktake command name + call sites, including the rejected `/rn:dn` call site (4.9 "Command and call sites" lines 517–528). Every one of these carries reasoning in the same paragraph as the decision, not as a separate bare statement. | | |
| The rejected alternatives include emitting a session-identifying string and reading task boundaries off the session-status block, each with the measured reason it was rejected. | OK | Session-identifying string rejected in 4.7 lines 445–448 and restated as an alternative in 5.1 lines 608–617, with the measured reason (working-directory glob already separates concurrent sessions — evidence doc's location census; relocation is measured to target only the parent checkout, so a bounded search suffices; an opaque ID would need a lookup table, which 2.2 rules out). Session-status block rejected in 5.1 lines 623–626, with the measured/stated reason carried from `steering.md`'s own Assumptions and `design.md` 2.2/4.2 (only three gates; ordinary tasks pass through without stopping, so the block never appears around most boundaries). | | |
| `design.md` still answers each of `design-template.md`'s h3 questions after the update, with no question left silently unanswered. | OK | Verified against `design-template.md`'s canonical list (1.1–1.4, 2.1–2.2, 3.1–3.3, 4.N, 5.1–5.2): grepped every `### ` heading in the updated `design.md` (see heading list captured during editing) and confirmed 1.1–1.4, 2.1–2.2, 3.1–3.3, 4.1–4.9, 5.1–5.2 are all present with body text under each, none blank or "TBD." 4.N is not capped at a fixed count (per the template's own instruction) — this update added 4.7–4.9 after the existing 4.1–4.6. | | |
| No statement in `design.md` contradicts #1's measured results. | OK | Cross-checked every measured figure the new text cites against `evidence/1-jsonl-behaviour.md`: channel-6 discriminator (0 false positives / 65 substring false positives, `hookEvent` key only inside a hook attachment across 552 files — matches "Six channels land" / "The hook entry tells an emission from a quotation"); `PostToolUse`/`SubagentStop`/`SessionEnd` landing behaviour (matches "Two hook placements fire without reaching the conversation file"); hook computing run-time content (matches "Channel 6" token-per-run table); relocation targeting only the parent checkout, 6/155 files, this session's own eighth file (matches "A file is placed by relocation target…" and "The file set changes while the session is open"); line-order/timestamp disagreement, 808 pairs, 13,429.9 s (matches "Line order and timestamp order disagree"); continuation replay by `uuid` (matches "A conversation can continue into a new file that replays the old one"); channel-5 unreliability figures, 14 ms–37.4 s, `marker-prefix-forgery` fired twice (matches "Channel 5: a subagent's final report…"). One drafting error was caught and fixed during this task: an earlier draft read "142 of 316 handoffs… never reach a `<task-notification>` entry," which misstated the evidence (142 is a *further*, separate count of dropped enqueues, not a subset of the 316 that landed); corrected in `design.md` 5.1 (the "on top of the 316 handoffs that do land…" sentence) to match the source's own wording. Untested items (character set beyond hex/space/uppercase, `Notification`/`PreCompact`, whether a broad no-matcher `PostToolUse` fires for every tool type) are stated as residual risk/extrapolation rather than as settled fact, per 2.1, 4.7, 5.2. | | |
| The document reads as one design, not as a new section bolted onto the old one — the existing sections that the new mechanism touches are updated rather than duplicated. | OK | No new h2/h3 section duplicates an existing one; every touched section (1.1–1.4, 2.1–2.2, 3.1–3.3) was extended in place with a new paragraph continuing the existing voice/citation style, not rewritten or given a parallel "session 2" subsection. 3.1 renumbers "Two organizing ideas" to "Three" and folds idea (C) into the same enumerated list as (A)/(B); the "Together these solve" synthesis paragraph was extended (not replaced) to add (C)'s contribution. 3.2's pieces table gained three new rows in the same table, not a second table. New mechanisms got new `4.N` subsections (4.7–4.9) rather than being folded awkwardly into 4.1–4.6 or left undocumented. 5.1/5.2 gained new bullets/paragraphs appended to the existing lists, not new subsections. Read the whole file end-to-end after editing to confirm continuity. | | |
| The marker form and the friction record shape both survive a revised task list: `design.md` states what each carries to stay self-describing, and why a bare task number was rejected. | OK | Marker: 4.7 "Naming the session and the task…" (lines 425–432) states it carries the session's slug and the task's *description text*, and gives the measured reason `#N` was rejected (task number not stable, `/rn:gm` rewrites/adds tasks, squashed PRs drop intermediate `steering.md` revisions — restated in 5.1 lines 618–622). Friction record: 4.8 "Where facts are stored, and their shape" (lines 490–502) states each entry carries the session's slug and the task's description text "as they read at the time (never `#N`, for the same reason as the marker in 4.7)" — explicitly cross-referencing the same reasoning rather than restating a different one. | | |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | | |

## Expert Reviews (axes the task needs)

### Design Expert (tasks that produce or revise structure/approach)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | | |
| System-wide integrity (interfaces, cross-doc consistency) | | |

### Craft Expert (coding / writing / visual — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | | |
| Consistency with existing style | | |

### Verification Expert (test / fact-check / dry-run — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | | |
| Coverage (edge cases / claims / steps) | | |

## Overall Verdict

- Self-check: OK

## Round 2 Self-check (implementation-expert fix round, four adversarial reviews)

| # | Finding | Self-check | Evidence |
|---|---|---|---|
| 1 (Must, Verification) | Continuation-replay claim overclaims scope | OK | `design.md` 2.1 lines 119–127 narrowed to name both measured shapes (`sessionKind: "bg"` replay vs. restart-append-in-place), corrected "verbatim" to "`.message` content byte-identical, `sessionId` restamped, `sessionKind` added," and stated the restart shape leaves uuid-dedup unaffected. 4.8 lines 488–492 carries the same correction where it repeats the claim. 5.2 lines 698–706 (marker residual-risk paragraph) gained the `--resume`/`--continue` attribution gap as a named, carried-forward residual risk, matching the evidence doc's own wording (`evidence/1-jsonl-behaviour.md` lines 1295–1310, 1406–1407, 1746–1752: "Replays every earlier entry verbatim" would be wrong on both halves…"; "Neither is attributable to a specific command-line flag"). |
| 2 (Must, Design) | Collection stage's steady-state cutoff undecided | OK | `design.md` 4.8: opening paragraph (lines 480–484) changed to state the collection stage always cuts the *whole* session from its own start, every run. New subsection "Finding where the previous run left off" (lines 510–525) decides idempotent re-scan (dedup by existing JSONL citation in `.rn/friction.md`) over an explicit watermark, with the watermark rejected against 2.2 (no new state store) and `steering.md`'s lean-contract rule (5.1 bullet), plus the watermark's own failure mode (silently absent after an uneventful stretch, per 4.8's empty-outcome rule). |
| 3 (Must, Design) | Stocktake grouping method a bare choice | OK | `design.md` 4.9 "Mechanism" (lines 570–575) states the coordinator, not a keyword/string match, judges which unfiled facts describe the same friction — named explicitly as a judgment call in 4.5's own sense — with exact/keyword matching named and rejected (near-duplicate wording would never cluster). "Breach detection" (lines 596–600) extended: a missed clustering is not lost, since the underlying friction persists and reappears as its own future fact, so a later run gets another chance — delay, not loss. |
| 4 (Must, Craft) | Session-status-block claim self-contradicts 3.2 | OK | `design.md` 4.7 (lines 401–404) and 5.1 (lines 677–683) both corrected from "only at the three sign-off gates" to the list 3.2 actually gives (sign-off gates, an escalation, `/rn:dn`'s untracked-path confirmation, its suspend report), with the argument reframed as "never during an ordinary task's build/verify work" — checked word-for-word against 3.2 lines 226–229, which is unchanged. |
| 5 (Should, Craft) | Register shift 4.7–4.9 vs. 4.1–4.6 | OK | All three "Breach detection" paragraphs in 4.7 (line 462), 4.8 (line 541), 4.9 (line 591) rewritten to open with 4.1–4.6's own "A breach (…) is caught/visible/checkable because…" inline clause pattern, replacing the prior "If X… Y" / bare-subject phrasing. Bold run-in sub-headers kept (per the instruction not to force full paragraph-merging). |
| 6 (Should, QA) | Session-status-block rejection not grounded in measurement | OK | `design.md` 5.1 bullet (lines 677–683) gained: "This alternative was never live to begin with — settled already by this design's own fixed three-gate rule (2.2) and 3.2's own list of stop points, not by anything task #1 measured." |
| 7 (Should, QA) | `/rn:sk` has no named rejected alternative | OK | `design.md` 4.9 "Command and call sites" (lines 555–558) gained: `/rn:stocktake`/`/rn:review` considered and rejected for the same two-letter-consistency reasoning already used for the existing five commands. |
| 8 (Should, Design) | Idea (C) narrower than (A)/(B), synthesis treats as equivalent | OK | `design.md` 3.1 (C) paragraph (lines 199–204) gained a "from this follows" consequence (the friction record in 4.8 is keyable off the same restamped value with no extra bookkeeping) and an explicit statement that (C) sits at a narrower altitude than (A)/(B) — decided as "strengthen + be honest," not inflate to false symmetry. |
| 9 (Should, Design) | Zero-coordinator-tool-call sign-off risk has no stated downstream effect | OK | `design.md` 4.8 (lines 492–497) gained a sentence stating the consequence: an unmarked sign-off task has no interval of its own, so any friction from it would be misattributed to a neighboring task's bracketing markers — named explicitly as a consequence of 4.7's already-flagged residual risk, not a new one. |
| 10 (Should, Design) | Stocktake approval channel unstated vs. `/rn:ty`/`/rn:gm` vocabulary | OK | `design.md` 4.9 "Mechanism" (lines 580–584) states explicitly that a stocktake approval is its own direct yes/no exchange, not through `/rn:ty`/`/rn:gm`'s shared verdict vocabulary, citing 3.1(A)'s own carve-out for escalation and weigh-in questions answered directly. |

- Self-check: OK

## Round 3 Self-check (Design re-review findings)

| # | Finding | Self-check | Evidence |
|---|---|---|---|
| 1 (Must, Design) | Stocktake's approval channel contradicts 3.1(A)'s own rule | OK | `design.md` 4.9 "Mechanism" (lines 600–631) rewritten: a stocktake proposal is now approved/declined through `/rn:ty` (approve → file the group as one GitHub issue) or `/rn:gm` (revise → the coordinator re-clusters differently or drops the proposal, per the user's feedback) — the same verdict vocabulary 3.1(A) gives every reviewed result, with the reasoning stated (a stocktake proposal is built, presented, and given an approve/decline verdict, which is structurally a reviewed result, not an escalation — tied to 5.1's "change in the agreed plan" — and not a weigh-in question, an ad hoc mid-task query). The old "its own direct yes/no exchange" / "neither one" text and its escalation/weigh-in carve-out claim are removed. Checked against 3.1(A) (lines 172–174, unchanged) and 5.1's escalation bullet (line 666) for no contradiction. Added, as this decision's own stated scope rather than an assumed given: this is `/rn:ty`/`/rn:gm`'s first call site with no active session's task loop underneath it — `/rn:sk` carries no version check of its own (4.6 names only `on`/`dn`/`up`/`ty`/`gm`), but `/rn:ty`/`/rn:gm`'s own version-check step (4.6) is written against "the active session's `Rn version:`," which a standalone stocktake run need not have, and their existing advance-step enumeration (4.2, 3.2) does not include "file an issue" or "return to re-cluster" — named as a gap task #6 (which implements `/rn:sk`) will need to close in `ty`'s and `gm`'s own skill steps, not claimed as already covered. |
| 2 (Must, Design) | 4.9's grouping "judgment call" borrows 4.5's precedent without an actual criterion | OK | `design.md` 4.9 "Mechanism" (lines 600–609) now states the actual equivalence test before calling it a judgment call: two facts belong to the same group when one could be rewritten as a restatement of the other without losing or adding information — concretely, whether merging the two into one sentence would drop any distinct information — targeting the friction itself, not the task or the wording. The judgment-call framing is kept (citing 4.5 as the same kind of call) but now reads as "guided by a stated test, not an unconstrained one." The previously-correct parts are unchanged and re-verified in place: groups of size 1 never surfaced (line 613), citations always attached (line 613), missed clustering is self-healing not silently lost (Breach detection, lines 649–654, untouched). |
| 3 (Must, Design) | 4.8's citation-equality dedup rule is undefined for fragment-only citations | OK | `design.md` 4.8: "Finding where the previous run left off" (lines 510–514) now states dedup equality is always the file-and-line pair, never a fragment-text compare. "Where facts are stored, and their shape" (lines 534–541) states file-and-line is available in literally every case the collection stage would ever cite, since every citable entry sits in an append-only file and so has a stable line position once written — reasoned from 2.1's own liveness/append-only measurement (evidence doc "The conversation file is live…" and "Every re-measured figure… rests on the log being append-only," lines 155–229) — and that a quoted fragment, when present, is only ever a readability supplement, never a dedup substitute. 5.2's friction-store paragraph (lines 759–765) names the one residual edge this doesn't close: the measured `sessionKind: "bg"` continuation shape copies a uuid-carrying entry into a second file at a different line (evidence doc lines 1276–1330), so the same underlying entry can carry two distinct file-and-line citations, one per copy — 4.8 already dedups this for task-boundary markers by `uuid` specifically, but not yet for an arbitrary friction citation, named as carried-forward risk rather than resolved. |
| 4 (Must, Design) | Cross-file interval ordering is never addressed | OK | `design.md` 4.8 gained a new paragraph "Ordering the files themselves, before cutting intervals inside any one of them" (lines 499–517), placed directly after the existing line-position/dedup ordering-rules paragraph it complements. States two cases: (a) a continuation pair is already known-adjacent and in order regardless of timestamp, ordered predecessor-before-successor by the `continued-in` entry itself (evidence doc lines 1313–1321, noted as n=1 machine-wide but directional by construction); (b) files with no continuation link are ordered by each file's own first *timestamped* entry — a coarser, whole-file comparison distinct from the per-entry inversions 2.1 already distrusts. Stated explicitly as a reasoned choice, not a measured one (task #1 measured cross-entry inversion within a file, not cross-file start-time reliability), per the fix instruction not to assert a new "measured" fact the evidence doc doesn't support. Checked the evidence doc's liveness/append-only and relocation sections for anything calling even the coarser comparison into question and found one: a file can carry zero timestamped entries for a stretch of its own early life (evidence doc lines 1260–1272: a 10-entry, no-timestamp file later grew into a 296-entry conversation; a separate single-entry file carries no timestamp at all) — named as a residual risk in the new paragraph rather than assumed away. |

- Self-check: OK

## Round 4 Self-check (final fix round — Design FAIL, 2 must-fix + 1 should-fix, plus a Craft wording note)

| # | Finding | Self-check | Evidence |
|---|---|---|---|
| 1 (Must, Design) | 4.8's "never a duplicate record" dedup guarantee is false in the continuation-replay case; self-contradicts 5.2's own admission | OK | `design.md` 4.8 "Finding where the previous run left off" (lines 538–544) changed the dedup key from "always the file-and-line pair" to "the source entry's `uuid` when it has one, the file-and-line pair otherwise." "Where facts are stored, and their shape" (lines 566–582) states the same mechanism, ties it explicitly to the fix 4.7 already applies to the task-boundary marker, and states why the file-and-line fallback is safe: every entry the citable-trace bar can point at (tool call, review verdict, user correction, workaround) is one of the message-type entries task #1 measured to carry a `uuid` (`evidence/1-jsonl-behaviour.md` lines 1298–1310), never one of the uuid-less bookkeeping kinds (`mode`, `cost-state`, etc., line 1530) a continuation drops — with the one measured exception, `file-history-snapshot` (line 1286: "5/141… copied byte-identically"), named explicitly as an entry kind this bar never cites either. 5.2's residual-risk paragraph (lines 778–787) rewritten from "does not itself close" / "carried forward as a residual risk" to a closure statement, with the remaining claim ("the bar's named entry kinds line up with the uuid-carrying set") marked as a reasoned conclusion, not a separate machine-wide measurement — matching the Method instruction not to introduce a new unverified claim. **Concrete-scenario trace-through (`ef482a21` → `555280be`, per `evidence/1-jsonl-behaviour.md` lines 1274–1330):** a friction fact extracted from a citable entry in `ef482a21` at some line N is, by the bar, one of the 229 `uuid`-carrying entries (a tool call, review verdict, correction, or workaround are never among the 141 uuid-less bookkeeping entries) — record it with `uuid=U`, citation file=`ef482a21`, line=N. The `sessionKind: "bg"` continuation replays that same entry into `555280be` at a different line M, with its original `uuid=U` unchanged and only `.message`/`sessionId` touched (line 1298: "the 229 entries that carry a `uuid`, with their original `uuid`… replayed"). A later `/rn:dn` re-scan, which walks the whole session from its own start (4.8's own rule) including `555280be`, extracts the same fact again from that replayed copy: citation file=`555280be`, line=M, `uuid=U`. Under the old file-and-line-only key, (`ef482a21`, N) ≠ (`555280be`, M) → not deduplicated → the genuine duplicate the finding named. Under the fixed key, `uuid` is present on the source entry for both extractions, so the key is `U` in both cases → equal → the second extraction is recognized as already recorded and no duplicate is appended. The fix closes exactly the scenario the finding traced through. |
| 2 (Must, Design) | 4.9's grouping-criterion "concretely" gloss doesn't match mutual-entailment and over-clusters on a literal reading | OK | `design.md` 4.9 "Mechanism" (lines 616–626): replaced "concretely, when merging the two into a single sentence would drop nothing either fact states on its own" (a test any two facts always pass, since a compound sentence never loses either original) with "concretely: group A and B only when telling the user about A alone, or B alone, would be an accurate but incomplete account of the same underlying friction, not when they are two distinct frictions worth reporting separately even though related" — option (b) from the fix instructions, chosen because it keeps a concrete, checkable test rather than falling back to bare judgment-call language, while now actually approximating mutual entailment (an incomplete-but-accurate account of the same fact, not an always-true compound-sentence test). The following judgment-call sentence was reworded from "decides whether merging would lose information" to "decides whether the two facts are actually restatements of one underlying friction or two separate ones," removing the same "merging" framing from that sentence too so the paragraph is internally consistent. Re-checked: does not touch 4.5's own wording (unchanged) or the surrounding "targets the friction itself" / rejected-exact-match / group-of-one / breach-detection text (unchanged). |
| 3 (Should, Design) | Cross-file ordering's named residual risk covers only the "no data yet" case, not the sharper "both files have timestamps but still misorder" case | OK | `design.md` 4.8 "Ordering the files themselves…" (lines 517–525): added a second failure mode after the existing "nothing yet to compare" sentence, without removing or rewording it — a file that truly starts first but has a long leading stretch of untimestamped bookkeeping entries (tying to the same measured stretch already named just above, evidence doc lines 1260–1272) before its own first timestamped entry, while a later-starting file reaches its first timestamped entry sooner, so first-timestamped-entry ordering ranks the later file ahead of the earlier one — stated explicitly as "a correctness risk rather than only a completeness one," distinct from the existing risk. Closing sentence changed from singular "carried forward as a residual risk" to "both are carried forward as residual risks alongside the others," confirming addition rather than replacement. |
| 4 (Minor, Craft) | 4.9's "first call site" framing overreaches against 3.3's plan-sign-off placement | OK | `design.md` 4.9 "Mechanism" topic sentence (lines 640–642) changed from "This is `/rn:ty`/`/rn:gm`'s first call site with no active session's task loop underneath it" to "Unlike the plan gate, which always has a `steering.md` just written underneath it (3.3), a standalone `/rn:sk` run may have no active session — no `steering.md` — at all" — the narrower, accurate point the paragraph's own follow-up sentences already relied on (the version-check step being written against "the active session's `Rn version:`," which a standalone run need not have identified). Checked against 3.3 (line 248: "the plan sign-off is planning's own closing hand-off," unchanged) — the plan gate does fire with no task-loop iteration yet run, so "first call site with no task loop underneath it" was inaccurate; the new wording does not depend on task-loop presence/absence at all, only on whether `steering.md` exists, so it no longer conflicts with 3.3. The rest of the paragraph (the two enumerated gaps, the task #6 scope-extension statement) is unchanged and still reads correctly against the new topic sentence. |

- Self-check: OK
