# 1 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence |
|---|---|---|
| 1. Execute has no delegate-or-act-directly decision for generation; deliverable work always dispatched to impl expert | OK | L58-62: "dispatches the implementation expert for the deliverable **unconditionally** — there is no 'delegate or act directly' decision, because the coordinator never produces the deliverable." The old decision paragraph is removed. |
| 2. Deliverable commit+push assigned to impl expert (each change pushed as made); single `complete task #{id}` marker on coordinator's post-approval steering check-off commit | OK | L79-83 work-order element 6: impl expert commits/pushes deliverable with plain message that must NOT contain `complete task #`, accumulating, pushed as made, never force-push. L185-192: coordinator commits the post-approval steering check-off with `{type}: complete task #{id} — {description}`, "the **single completion marker**". L5-8 intro states the same. |
| 3. Coordinator's direct writes limited to `steering.md` + review verdicts (into check file) plus their commits, stated explicitly | OK | L13-18 intro: "only direct-write carve-out is the coordinator's **own ledger**: `steering.md`, the review verdicts it already holds (recorded into the check file), and the commits of those … It never touches the deliverable or its git history." L38-41 "What the coordinator may write directly" restated, ending "**Nothing of the deliverable**". |
| 4. Verify/triage sends every deliverable-touching fix to impl expert; no coordinator-direct-fix path for deliverables | OK | L145-152: "**Every deliverable-touching fix goes to the implementation expert** — no matter its size". The old "if there isn't, the coordinator does it directly" branch and the "Fixing it directly" sub-paragraph are removed. Coordinator-direct remains only for recording verdicts (L150-151, L170-171). |
| 5. Intro, Process selection, Execute, Verify, Complete, Check file format mutually consistent; user-review gate matches push-and-review.md | OK | See consistency pass below. User review (L175-181) explicitly happens on the PR per `.claude/rules/push-and-review.md`, on the already-pushed deliverable. |

## Consistency pass

| Section | Consistent with new division? | Note |
|---|---|---|
| Intro | OK | L5-8 marker framing + L13-41 all-hands-on-work-delegated / ledger carve-out; coordinator role (L20-23) and impl-expert role (L27) updated to match. |
| Process selection | OK | L48-51: self-check always by impl expert; user review "on the pushed deliverable". "work-order element 5" still points at the Self-check element (unchanged number). |
| Execute | OK | Unconditional dispatch (L58-62); work-order gains element 6 Commit & push (L79-83), Return becomes element 7 (L84-86) returning SHAs + that it pushed. |
| Verify | OK | Triage routes all deliverable fixes to impl expert (L145-159); review machinery, 6-element neutral-framing prompt, adversarial safeguards, 3-iteration cap, verdict-recording-as-ledger all preserved. "element 6" at L105 refers to the review prompt's Neutral-framing element, not the work-order — no collision. |
| Complete | OK | User review on PR before check-off (L175-181); deliverable already pushed by expert; single `complete task #{id}` marker on coordinator's check-off commit (L183-192); `/rn:hi` substring note kept. |
| Check file format | OK | L201-203: impl expert always fills self-check, coordinator fills review verdicts; table structure unchanged. |

## QA Expert Review (subagent, adversarial)

| Aspect | Verdict | Evidence |
|---|---|---|
| Criterion 1 — no delegate/act-directly decision; deliverable always dispatched | OK | L60-62 unconditional dispatch; old decision paragraph removed |
| Criterion 2 — commit/push is the expert's; single marker on check-off commit | OK | L79-83 plain msg w/o `complete task #`; L188-192 single marker on check-off |
| Criterion 3 — coordinator direct writes limited to ledger, stated explicitly | OK | L38-41 "Nothing of the deliverable"; reinforced L13-18, L23 |
| Criterion 4 — every deliverable fix → impl expert; no direct-fix path | OK | L145-150; old "Fixing it directly" route removed; only ledger stays direct |
| Criterion 5 — six sections consistent; user-review gate matches push-and-review.md | OK | self-check ownership identical in L48-50/75-78/201-203; gate L177-181 |
| Numbered-list cross-refs resolve after renumbering | OK | element 5→Self-check; 6→Commit&push; L105 "element 6"→review-prompt Neutral framing |
| No stale "1 task = 1 commit" / "trivial single-step edit" | OK | full-file read: none surviving |
| `complete task #` substring / `/rn:hi` reconciliation (exactly one marker) | OK | deliverable commits forbidden the substring (L81-82); only check-off carries it |

**QA overall: PASS** — all 5 criteria OK, no defects, no contradictions.

## Fix pass (Verify diff / push fallback / check-file ownership)

| Fix | Verdict | New line ref | Evidence quote |
|---|---|---|---|
| 1. Verify "Read the diff" reads the committed change, not the working tree | OK | L101-108 | "the working tree is clean … The coordinator instead inspects the **committed** deliverable change: `git show <sha>` … or `git diff <task's starting commit>..HEAD`" |
| 2. Push fallback when expert cannot push (coordinator pushes the expert's commit, push-only) | OK | L84-88 | "**If the expert cannot push** … the coordinator then pushes that already-made commit — a push only, the commit stays the expert's … never the coordinator authoring or amending the deliverable." |
| 3. Check file is coordinator's ledger; expert writes self-check but does not commit it; element 6 commits deliverable only | OK | L78-79, L80, L209-214 | el.5: "write the self-check into it but **do not commit it**"; el.6: "stage the **deliverable change only** (not the check file)"; format intro: "committed by the coordinator." |

Full-file consistency re-read: PASS. All cross-references resolve after the fixes — work-order el.5/6/7, Verify reads committed commits (and L21 coordinator-role line tightened to "the committed diff" for consistency), triage routes deliverable fixes to the expert with the same push fallback, Complete keeps the single `complete task #{id}` marker on the check-off, and Check file format agrees on ledger ownership. "element 6 says what to withhold" (L113) still refers to the review-prompt list (Neutral framing), not the work-order — no collision.

## QA regression (fix pass) + L113 disambiguation

- QA regression on the 3 fixes: **PASS** (all OK, no new contradiction). Verdicts in `a40dc765`'s summary.
- QA flagged a pre-existing ambiguity: bare "element 6" at L113 (Verify section) could read as the
  work-order's element 6 (Commit & push) instead of the review-prompt's element 6 (Neutral framing).
  Triaged **valid** (genuine forward-reference ambiguity). Fixed coordinator-direct as a one-phrase
  disambiguation → "the review prompt's Neutral framing element below says what to withhold" (L113-114).
  No meaning change. (Done under 0.3.0's trivial-single-step-edit carve-out — the carve-out the new doc
  removes; noted for transparency.) Swept all other "element N" refs: the rest are qualified
  ("work-order element 5/6"); this was the only bare one.

## Overall
- Self-check: OK
- QA: OK (PASS, including fix-pass regression)
- Ready for user review: Yes
