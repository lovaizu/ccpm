# 3 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence |
|---|---|---|
| 1. Verify step no longer claims tree clean; states deliverable committed + only check-file uncommitted; directs coordinator to inspect committed change | OK | task-workflow.md:94-101 — "no uncommitted deliverable change in the tree … and left it uncommitted (Execute element 5) … `git status` showing only the check-file ledger is normal … inspects the **committed** deliverable change instead: `git show <sha>` … or `git diff <task's starting commit>..HEAD`" |
| 2. Execute element 6 has "cannot commit" fallback symmetric to "cannot push", preserving git-mechanics-only boundary | OK | task-workflow.md:80-82 — "**If the expert cannot commit at all** … the coordinator then runs the commit **mechanically** … git mechanics only, the content stays the expert's, and the coordinator never authors, edits, or regenerates the deliverable." "cannot push" fallback kept intact at :78-80 |
| 3. Element 6 forbids `git add -A`/`git add .` and requires explicit deliverable paths | OK | task-workflow.md:72-75 — "stage the deliverable **paths explicitly** (`git add <path>…`); **never `git add -A` or `git add .`**, which would sweep the check-file ledger … into a plain deliverable commit" |
| 4. Execute instructs coordinator to capture task's starting commit before dispatch | OK | task-workflow.md:85-87 — "Capture the task's **starting commit** — current `HEAD`, just before the expert's first deliverable commit — so Verify's `git diff <task's starting commit>..HEAD` is computable across feedback rounds." |
| 5. No behavioral invariant altered | OK | See Invariant preservation table below |
| 6. Both validations report success | OK | See Validation section |

## Invariant preservation

| Invariant | Still verbatim? | Evidence (path:line) |
|---|---|---|
| deliverable/ledger split | OK | task-workflow.md:6-12 (canonical statement block untouched) |
| one task = one completion marker | OK | task-workflow.md:18-20 ("One task = one completion marker." block untouched) |
| `complete task #{id}` substring rule | OK | task-workflow.md:196-199 ("only this check-off commit carries the `complete task #{id}` substring"); element-6 prohibition kept at :74 |
| neutral-framing withhold-list | OK | task-workflow.md:119-123 (Verify element 6 withhold-list untouched) |
| 3-iteration counting rule | OK | task-workflow.md:166-168 ("Cap this at 3 iterations, where one iteration is a single fix and all its re-reviews … count together as one") |

## Validation
- `claude plugin validate ./rn --strict`: ✔ Validation passed (exit 0)
- `claude plugin validate . --strict`: ✔ Validation passed (exit 0)

## QA Expert Review

| Aspect | Verdict | Evidence |
|---|---|---|
| Each of the 4 fixes actually closes its defect (not just appears to) | OK | All 6 criteria re-verified independently against the file on disk; new wording quoted per criterion |
| No NEW contradiction introduced (no residual "clean tree" / "deliverable change only" elsewhere) | OK | Full-file search: no residual stale phrasing; Fix 1 and Fix 3 reinforce (explicit staging is what keeps the check file uncommitted) |
| Fix 2 symmetry + no smuggled authoring | OK | "cannot commit" clause parallels "cannot push"; "git mechanics only … never authors, edits, or regenerates" preserves the boundary |
| Five invariants verbatim (split / one-marker / substring / withhold-list / 3-iteration) | OK | All five located verbatim; see self-check Invariant table |

## Empirical validation (dogfood)

- This task is the **first real run of the new model**: the implementation expert committed (`f612906`) and pushed the deliverable itself; the check file was left uncommitted for the coordinator. `git status --porcelain` after Execute showed exactly `?? checks/3.md` and nothing else — empirically confirming Fix 1's corrected wording.

## Overall Verdict
- Self-check: OK
- QA: OK (PASS — all 6 criteria OK, all 5 invariants verbatim, no additional defects)
- Ready for user review: Yes
