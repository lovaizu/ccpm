# 12 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| A coordinator runs one task end-to-end by reading `task-execute-workflow.md` then `task-verify-workflow.md` in sequence, with behavior identical to the pre-split `task-workflow.md` (same gates, same escalation channel, same check-off marker); no file in `rn/` still points at the deleted `task-workflow.md` (failure modes absent: a rule/step dropped at the seam; a dangling reference) | OK | Line-by-line diff of every original section against its new home (see Method below) shows zero content loss and zero accidental drift: Roles (orig L9-27 vs both copies) identical; Review gates (L29-43) identical; Process selection (L45-59) identical; Check file format (L182-240, 59 lines) byte-identical in both new files; Phase: Execute (orig L61-101, 41 lines) identical except one intentional cross-reference reword ("below"→"above", since Check file format now precedes Phase: Execute in `task-execute-workflow.md`); Phase: Verify (orig L103-164, 62 lines) byte-identical, including the escalation-channel paragraph and the 3-iteration triage cap; Phase: Complete (orig L166-180, 15 lines) identical content plus one added filename pointer (`Phase: Execute (task-execute-workflow.md)`) in the Advance step — same gates (plan/design/evaluation), same escalation channel, same check-off marker (`complete task #{id}` substring rule untouched). `grep -rn "task-workflow" rn/` → no output (zero remaining references under `rn/`; `.rn/**/steering.md` and `.rn/**/checks/*.md` historical records were left untouched per instruction). Updated the 4 confirmed referencing files: `rn/skills/on/SKILL.md:41`, `rn/skills/up/SKILL.md:30` (now read both new files in sequence), `rn/references/pr-feedback-workflow.md:5` (link now points to both), `rn/references/steering-template.md:97` (Process selection phrase now names both new files). `rn/references/planning-workflow.md` does not exist yet (task #11 not yet landed) so no reference existed there to update. |  |  |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | Independently re-diffed every section against the pre-split file and independently re-ran `grep -rn "task-workflow" rn/` (zero hits); confirmed both new files carry the full shared header rather than assuming the reader has the other open, so a reader following only one file is never stranded. Overall PASS. |

## Expert Reviews (axes the task needs)

### Design Expert (tasks that produce or revise structure/approach)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | OK | Split seam (Execute vs Verify+Complete) matches `rn/docs/design.md:76-77`'s stated intent verbatim. Interface between the two files is clean — each ends by naming the other by filename. |
| System-wide integrity (interfaces, cross-doc consistency) | OK | All 7 files that mention any of the three filenames (`grep -rln`) were checked; all correctly updated, no broken anchor. Flagged the ~110-line shared-header duplication as a standing drift risk against the repo's own anti-duplication principle — noted, not actioned: the task's own Purpose text explicitly chose duplication over a third shared file ("per the lean-steering precedent of never inventing accumulation machinery for a two-file split"), so this is an already-decided tradeoff, not a new defect. |

### Craft Expert (coding / writing / visual — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | Round 1 found 2 defects: `steering-template.md:97` lowercase "the" after a period (leftover from patching a sentence that used to start with a lowercase code-literal filename), and an unanchored "Execute element 5/6" cross-reference in `task-verify-workflow.md:125-126` that would silently go stale if the execute file's list is renumbered. Both fixed in `e710e9f` (capitalized; anchored with `` `task-execute-workflow.md` ``). Round 2 re-review: both confirmed fixed, no new defects, PASS. |
| Consistency with existing style | OK | Voice/terminology, heading structure, and the `{steering_dir}` placeholder convention consistent with the rest of `rn/references/` in both rounds. |

### Verification Expert (test / fact-check / dry-run — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | OK | Round 1: reconstructed the original file from `git show` and verified every rule/checklist/paragraph survived (escalation cap, check-off marker rule, never-force-push, expert checklists word-for-word); independently grepped for zero remaining references; checked all 4 referencing files read grammatically. Round 2: independently confirmed the "Execute element 5/6" renumbering claim by reading the actual numbered list in `task-execute-workflow.md` (5 = Self-check, 6 = Commit & push — mapping correct); confirmed the fix-round diff (`dae0caf..e710e9f`) touched only the two specified spots, nothing else. |
| Coverage (edge cases / claims / steps) | OK | Both rounds independently re-ran `grep -rn "task-workflow" rn/` (zero hits each time) rather than trusting the prior claim; no material claim left unchecked. |

## Overall Verdict

- Self-check: OK
- QA: OK
- Design expert: OK
- Craft expert: OK
- Verification expert: OK
- Ready to check off: Yes — self-check OK, all 4 review axes PASS (round 1 found 2 valid Craft findings, fixed and re-verified PASS in round 2 by both affected axes), coordinator's own diff review (`git diff 5a855af..e710e9f`, `grep -rn "task-workflow" rn/`) confirms clean and scoped exactly as expected.
