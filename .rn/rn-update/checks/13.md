# 13 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence |
|---|---|---|
| Every place `rn/` names a review expert uses the design/craft/verification+QA axes, with which axes spawn stated as a per-task judgment keyed to what the task produces (not a fixed code/non-code branch) | OK (within scope) | `rn/references/task-workflow.md` Roles (lines 9–23) now lists Coordinator, Implementation expert, QA expert, Design expert, Craft expert (per medium), Verification expert (per medium) — no fixed trio. Process selection (lines 45–59) states the axes-spawn rule as a per-task judgment ("Which axes spawn is a per-task judgment: the task states its medium … and whether it produces or revises structure/approach. QA always spawns. Craft and Verification spawn for the task's medium. Design spawns only when …") and gives code/docs/visual as three instances, not the only two branches, each ending "add Design when …". Scope limitation: per the assigned task's explicit Scope section ("Edit only `rn/references/task-workflow.md`. Do not touch other files"), I did not touch `rn/references/steering-template.md` or `rn/references/pr-feedback-workflow.md` — see next row. |
| No reference to "language expert" or "software-engineering expert" survives anywhere under `rn/` | OK | Expert flagged two out-of-scope leftovers (see original evidence below); coordinator fixed both directly: `rn/references/steering-template.md:62-64` now lists Craft/Verification/Design review bullets instead of the code-only language/software-engineering pair; `rn/references/pr-feedback-workflow.md:147` now reads "no Design/Craft/Verification experts …". `grep -rni "language expert\|software-engineering expert" rn/` → no match (exit 1) at whole-`rn/` scope. Original expert evidence: `grep -ni "language expert\|software-engineering expert" rn/references/task-workflow.md` → no match — that file was already clean. |
| The same axes that build a task also review it (already true structurally — naming carried through) | OK | Implementation expert (build) applies Craft/Design per Execute element 4 (lines 69–73: "apply the axes the task needs: **Craft** … and, when the task produces or revises structure/approach, **Design** …"); Verify dispatches the identical named axes — QA/Design/Craft/Verification (lines 107–134) — against the same artifact. Same axis names used on both the build side and the review side. |
| No axis is forced onto a task it doesn't fit; Craft/Verification's docs instances (writing/fact-check) are as real an option as their code instances (coding/test) | OK | Process selection's three bulleted instances (code/docs/visual, lines 51–56) are stated as parallel, equally-weighted examples of the same rule, not a primary code branch with docs as an afterthought. Expert checklists (lines 121–134) give coding/writing/visual as three coordinate items under Craft, and test/fact-check/dry-run as three coordinate items under Verification, each with its own concrete checklist content (writing: "prose clarity and correctness, consistency with the doc's existing voice/terminology"; fact-check: "every claim/reference verified against its source, no unverified assertion stated as fact") rather than a relabeled copy of the code checklist. Check file format (lines 206–226) keys each expert section to "coding / writing / visual" or "test / fact-check / dry-run" generically, not code-only. |

## Overall Verdict

- Self-check: OK (the expert's task-workflow.md edit was clean; the two out-of-scope leftovers it
  flagged in `steering-template.md` and `pr-feedback-workflow.md` were fixed by the coordinator
  directly, closing the whole-`rn/` scope of the criterion).

## Fix round

Four adversarial-review findings fixed after the original self-check above; each verified by grep
showing the old defect gone and the new text present.

| # | Finding | Verdict | Evidence |
|---|---|---|---|
| 1 | Build-time Verification axis was code-only (Execute elements 3 & 5 scoped `(code)` only) | OK | `rn/references/task-workflow.md:67-71` element 3 now gives coding/writing/visual their own build-time method ("apply the task's Verification method as you build, not only after: coding — write the test first … writing — verify each claim/reference against its source as you draft … visual — trace the diagram/flow step by step …"); element 5 (`:77-81`) now reads "plus confirm the Method above was actually applied: coding — measure coverage … writing — confirm every claim was checked … visual — confirm the flow was traced end to end …". `grep -n "(code) write the test first" rn/references/task-workflow.md` → no match. |
| 2 | Verify step 2 sub-item 1 ("Role") scoped QA/Design "per the task's medium", contradicting the lead sentence and Roles section | OK | `rn/references/task-workflow.md:115-116` now reads "the expert's domain (QA / Design / Craft / Verification — Craft and Verification scoped to the task's medium)". `grep -n "per the task's medium), told to" rn/references/task-workflow.md` → no match; the old ambiguous phrasing is gone. |
| 3 | `steering-template.md` "Criteria vs steps" row still called Process selection a "(non-code vs code)" binary | OK | `rn/references/steering-template.md:97` now reads "`task-workflow.md` Process selection — a per-task judgment of the task's medium (coding / writing / visual) and whether it produces or revises structure/approach — is the source of *which* reviews apply". `grep -n "non-code vs code" rn/references/steering-template.md` → no match. |
| 4 | QA and Verification checklists collided on "edge case coverage" (Verify-phase prose + Check-file table rows) | OK | Verify-phase: QA checklist (`task-workflow.md:127-128`) now reads "the verification approach is meaningful to the actual objective — does it check the right thing, not just 'it ran'/'it passed'"; edge-case/coverage language moved fully into the Verification checklist (`:136-140`, e.g. test — "cover the change's edge cases (boundary, error, empty, max, type conversion)"; fact-check — "completeness of claim coverage"; dry-run — "covering every step/branch"). Check-file table: QA table's "Edge case coverage" row removed, leaving only "Verification approach meaningful to the objective (checks the right thing, not just 'passed')" (`:200-204`); Verification table's existing "Coverage (edge cases / claims / steps)" row (`:227-231`) is now the sole owner of that wording. `grep -n "Edge case coverage" rn/references/task-workflow.md` → no match. |

Minor cleanup also applied: Roles section's QA entry reworded to "**QA expert** (every task) — subagent."
for parallelism with Design/Craft/Verification (`task-workflow.md:17`); `rn/docs/design.md` Open
questions gained one clause noting a task spanning more than one medium (e.g. code + diagram) is
unresolved re: one Craft/Verification instance per medium (`design.md:125-128`).
