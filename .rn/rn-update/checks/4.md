# 4 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| up reconcile retires shipped-Governs decisions, keeps live ones; rationale recoverable | OK | `rn/skills/up/SKILL.md:28` — "Keep a decision that still governs an unchecked task, and keep any decision whose `Governs` is `—`. A decision with no `Governs` field is treated as `Governs: —` (kept)." Missing-field back-compat now defined (safe default = keep); `rn/DESIGN.md:79-81` records the rationale. `steering-template.md:62-63` states rationale persists in the recording commit + PR, so retirement is recoverable. | OK | Re-QA: two-part "shipped" (checked off AND marker) consistent with Step 4; stale-linger, wrong-drop, and missing-field failure modes all unreachable. |
| steering-template Decisions carries Governs + retirement rule; State Notes = forward pointer | OK | `rn/references/steering-template.md:67` `- **Governs**: <#N, or — if cross-cutting>` (first bullet, before Issue); lines 62-64 state the live-working-set retirement rule; line 84 `- **Notes**: bounded forward pointer — branch/PR, next concrete action, open blockers, user-deferred paths, open questions / pending decisions not yet recorded as a `D-N`; not a re-narration of the session (that lives in `git log`)`. Forward pointer now restores pending decisions; still a pointer, not a re-narration invitation. Both edits inside the fenced markdown block; blank-line structure intact. | OK | Re-QA: rule documented where the writer reads (the copied template); field + State Notes form both present. |
| dn Step 3 writes Notes as bounded forward pointer, not re-narration | OK | `rn/skills/dn/SKILL.md:22-24` — "Cap `Notes` to a bounded forward pointer — branch/PR, next concrete action, open blockers, user-deferred paths, open questions / pending decisions not yet recorded as a `D-N` — not a multi-paragraph re-narration of the session (the narrative is in `git log`)." Pending-decisions item restored to the pointer (forward-looking resume context), kept terse and still a pointer. Rest of step (Status/Date/Last completed/Next, fields per template) intact. | OK | Re-QA: both failure modes closed — bloat forbidden, and no over-capping (pending decisions + deferred paths retained for resume). |
| rationale in DESIGN.md only; runtime files stay pure procedure; no cross-doc contradiction (grep) | OK | `rn/DESIGN.md` carries all three why-notes: up Step 6 retirement (74-79), dn Step 3 Notes cap (34-39, supersedes the old "anything not written there is lost"), steering-template live-working-set + forward-pointer (162-166). Runtime files (up/dn SKILL.md, steering-template.md) hold no "why" prose — only procedure/structure. Grep over `rn/` for verbatim/immutable/narrat/context/Governs found no contradiction: the surviving "verbatim" hits refer to copying when creating a steering.md / Completion criteria, not template immutability; the prior contradiction at DESIGN.md Step 3 was reconciled. | OK | Re-QA: rationale confined to DESIGN; template parentheticals are operational rule statements (what/when), not rationale; exhaustive re-grep found no contradiction. |

## Lever C — Completion Criteria

| Criterion | Self-check | Evidence |
|---|---|---|
| up Step 6 collapses a shipped task (box checked + `complete task #` marker) to the `### #N: <name> — SHIPPED (#N in <sha>)` one-liner, dropping the body; leaves unshipped/in-progress tasks intact | OK | `rn/skills/up/SKILL.md:30` — "for each shipped task — box checked in steering.md AND `complete task #{id}` marker in `git log` (the same trigger, per Step 4) — find the short SHA of its `complete task #{id}` commit and replace the whole task block (the `### #N: …` heading and everything under it, up to the next `### ` task heading) with the single line `### #N: <task name> — SHIPPED (#N in <sha>)`. Drop the body. Leave an in-progress or unshipped task fully intact." Block boundary is structural (heading-to-next-heading), not a field enumeration — robust to extra/absent fields and no longer names a spurious `Status` field (`Status` is a `# State` field, not a task field). Same two-part trigger as Lever A (decisions), same reconcile pass (`SKILL.md:28` lead). |
| up Step 7 next-task scan treats a `SHIPPED` task as done | OK | `rn/skills/up/SKILL.md:33` — "A task collapsed to its `### #N: … — SHIPPED (…)` one-liner counts as done — skip it when scanning for the next unchecked task." |
| steering-template records the shipped-task collapse on the `Tasks` section; fenced block's existing fields + blank-line structure otherwise unchanged | OK | `rn/references/steering-template.md:36-38` parenthetical on the `# Tasks` heading (inside the fenced block, parallel to the Decisions note at 62-64 and State note at 76-78): "a task is collapsed once shipped: /rn:up replaces a checked-off, shipped task's whole block with the one-line pointer `### #N: <task name> — SHIPPED (#N in <sha>)`, dropping its Steps / Completion criteria — the number stays so other tasks' `Prerequisites: #N` still resolve." Template fields (`### #1` heading, Purpose/Prerequisites/Steps/Completion criteria) and their blank lines unchanged; no table row added (kept minimal, parallel to Decisions/State which add none). |
| up/SKILL.md + steering-template.md stay pure procedure — no rationale prose | OK | SKILL.md Step 6/7 additions are imperative ("replace the whole task block … up to the next `### ` task heading", "Drop the body", "skip it when scanning"); the template note states what/when (the collapse rule + the number-preservation it must satisfy), an operational rule statement like the existing Decisions/State notes, not a "because"-style rationale. All "why" (collapse-vs-delete, largest-section motivation) lives only in DESIGN.md. |
| DESIGN.md carries the collapse rationale AND collapse-vs-delete distinction, under up + steering-template sections, in existing voice | OK | `rn/DESIGN.md:81-90` (under `## up`) bolded lead "**Step 6 collapses a shipped task to a one-line `SHIPPED` pointer, rather than deleting it**" then "— because …"-style body covering largest-section motivation, decision=deleted (pure rationale, recoverable) vs task=collapsed (map indexed by number, numbering must stay stable), and the shared two-part trigger. `rn/DESIGN.md:178-182` (under `## steering-template`) "**`Tasks` carries the same non-accumulation note …**" parallel to the Decisions/Notes note, restating the structural collapse-vs-delete reason. |
| No rn doc contradicts another (grep "SHIPPED"/"collapse") | OK | `grep -rn "SHIPPED\|collapse" rn/` — collapsed-form string `### #N: <task name> — SHIPPED (#N in <sha>)` identical in SKILL.md:30, steering-template.md:37, DESIGN.md:80/180. Only unrelated hit is DESIGN.md:143 ("Markdown collapses … onto one line"), a different sense. No contradiction. |

## Lever C — QA Expert Review

QA (adversarial subagent) on the Lever C change. Round 1 FAIL (one defect), round 2 PASS after fix `a1fb624`.

| Criterion | QA | QA Evidence |
|---|---|---|
| up Step 6 collapses a shipped task to the one-liner, drops body, leaves unshipped intact | OK | Trigger (box checked AND marker in git), short-SHA lookup, replacement string, and "leave unshipped/in-progress intact" all unambiguous; next-task scan skips the collapsed one-liner so a collapsed task is neither re-run nor mistaken for unchecked. |
| up Step 7 next-task scan treats `SHIPPED` as done | OK | `SKILL.md:33`; last-task case correctly falls through to "all done → propose Acceptance criteria". |
| steering-template records collapse on Tasks; fenced block fields/blank lines unchanged | OK | Parenthetical added above `### #1`, parallel to Decisions/State notes; task body and blank lines verified untouched in the diff. |
| runtime files stay pure procedure; rationale + collapse-vs-delete in DESIGN.md | OK | No "why"/Lever labels leaked into runtime files; collapse-vs-delete distinction confined to DESIGN.md:81-90/178-182. |

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Meaningful tests/verification | OK | Round 1 NG: Step 6 enumerated a phantom `Status` field on the task block (`Status` is a `# State` field) — a cross-doc contradiction with the template and an ambiguity for the executing agent. Fixed `a1fb624`: boundary is now structural ("the `### #N: …` heading and everything under it, up to the next `### ` task heading"), robust to absent/extra fields. Coordinator re-grep confirmed no remaining task-block `Status` reference and the collapsed-form string identical across all three files. |
| Edge case coverage | OK | Probed: in-progress task (no marker → intact), box-checked-but-marker-missing (AND trigger → not collapsed), last task collapsing (→ propose Acceptance criteria), `Prerequisites: #N` pointing at a collapsed task (number preserved → still resolves), string match across files. All handled. |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Meaningful tests/verification | OK | Round 1 (FAIL) found D1 (pending decisions dropped from the forward pointer) and D2 (missing-`Governs` undefined on pre-field steering files); both fixed in `87f63cf`. Round 2 re-review: PASS, all 4 criteria OK with file:line evidence. |
| Edge case coverage | OK | Probed: partial-ship (some `Governs` tasks shipped → kept), `Governs: —`, missing field (→ kept), marker/check-off divergence both directions, over-capping resume context. All handled. Only a cosmetic nit (bullet style) — no fix required. |

## Overall Verdict

- Self-check: OK (Levers A+B and Lever C)
- QA: OK — Levers A+B round-2 PASS (round-1 found D1/D2, fixed `87f63cf`); Lever C round-2 PASS
  (round-1 found the phantom-`Status` defect, fixed `a1fb624`)
- Ready for user review: Yes (whole of #4: Levers A+B+C)
