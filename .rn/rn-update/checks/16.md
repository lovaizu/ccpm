# 16 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| Every rn stop for user input while a session is active (its `steering.md` exists and is identified) opens with the status block per `status-display.md` — no in-session stop asks without it, the block alone conveys done / current / remaining plus the ask, and stops outside an active session (before steering exists, or none identified) are explicitly scoped out in the spec (failure modes absent: an in-session stop missing the block; a block that still requires opening `steering.md`; the ⬜ section rendered when nothing remains; a doc claiming coverage the wiring does not deliver) | OK | All seven in-session stop points carry the emit instruction: plan gate on both branches — PR reported or console fallback (`rn/references/planning-workflow.md:35-38`), sign-off gate in both files' shared Process-selection bullet (`task-execute-workflow.md:68-69`, `task-verify-workflow.md:68-69`), escalation channel (`task-verify-workflow.md:199-200`, mirrored in the shared Review-gates line 48-49 of both), dn untracked-path ask (`rn/skills/dn/SKILL.md:38-41`), up dirty-tree ask with read-only steering discovery first (`rn/skills/up/SKILL.md:15`), ty disambiguation ask (`rn/skills/ty/SKILL.md:13`), pr-feedback no-PR report when steering is active (`rn/references/pr-feedback-workflow.md:28-30`). Out-of-session stops are scoped out by the spec's active-session boundary rule (`status-display.md` Rules): planning's goal/slug/design-location asks before steering persists, `/rn:up` before a steering is identified (incl. the multiple-candidates proposal and zero-result stop), `/rn:gm` with no active session. The spec alone suffices for derivation: ✅ membership defined (checked off or explicit done annotation; done-but-awaiting counts ✅ with the pending item on the outlook line), consecutive ranges + comma-separated disjoint groups (`#1–#14, #16`), bold "No remaining tasks → omit the ⬜ lines entirely" — dry-run built the block from `.rn/rn-update/steering.md` for both the ⬜-present case (✅ #1–#14 incl. the DONE-through-QA-awaiting-review nuance, 👉 #16, ⬜ #15) and the empty-⬜ case at #15 (✅ #1–#14, #16, ⬜ omitted) with no information missing. Example 1's outlook now traces true against `dn/SKILL.md` (answer → suspend completes → #3 resumes on /rn:up) | | |
| The record is consistent: CHANGELOG lists the feature, `design.md` records the structure, README's scenario shows it, and no rn doc contradicts another (failure mode absent: a stop-point instruction left contradicting `status-display.md`) | OK | All four docs now state the same scoped claim — the block opens every stop while a session is active: `rn/CHANGELOG.md:13` ("Once a session is underway…"), `rn/docs/design.md:86-92` ("while a session is active (its `steering.md` exists and is identified)", boundary noted as living in status-display), `rn/README.md:53` ("once a session is underway"), `status-display.md` opening line. Exactly two gloss registers survive (grep-verified): user-facing "✅ done / 👉 now / ⬜ ahead" (README, CHANGELOG) and normative "✅ completed / 👉 current / ⬜ remaining" (spec, design.md) — no third variant. README's scenario now shows `⬜ #1–#4 … / evaluation sign-off`, matching planning-workflow's always-last Evaluation sign-off and the spec's own payment-fix examples, and its outlook no longer denies the final evaluation stop ("until #4 — the evaluation sign-off"). The sign-off bullet's rewording (commas replacing the paired dashes) left the two workflow files' shared header byte-identical — `diff <(sed -n '6,136p' …execute…) <(sed -n '6,136p' …verify…)` clean. Format itself lives only in `status-display.md`; every stop point remains a one-line citation | | |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | | |

## Expert Reviews (axes the task needs)

### Design Expert (tasks that produce or revise structure/approach)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | | |
| System-wide integrity (interfaces, cross-doc consistency) | | |

### Craft Expert (writing)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | | |
| Consistency with existing style | | |

### Verification Expert (dry-run)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (flow traced) | | |
| Coverage (steps/branches) | | |

## Overall Verdict

- Self-check: OK — fix round re-traced end to end: every ask site in rn/skills/*/SKILL.md and rn/references/*.md either reaches an emit instruction (plan gate both branches, sign-off gate, escalation, dn untracked-path, up dirty-tree, ty disambiguation, pr-feedback no-PR report) or falls under the spec's stated active-session boundary (planning's pre-steering asks; up before a steering is identified, incl. multiple-candidates and zero-result; gm with no session); the block was built from `.rn/rn-update/steering.md` for both the ⬜-present case (✅ #1–#14 with the done-but-awaiting-review nuance resolved by the new ✅-membership rule, 👉 #16, ⬜ #15) and the empty-⬜ case at #15 (✅ #1–#14, #16 — disjoint groups comma-separated, ⬜ omitted), from the spec alone; example 1 traced true against dn/SKILL.md (answer → suspend completes → #3 resumes on /rn:up); the two workflow files' shared header (lines 6–136) diffed byte-identical after the sign-off-bullet rewording; gloss grep confirmed exactly two marker registers.
- QA:
- Design expert:
- Craft expert:
- Verification expert:
- Ready to check off:
