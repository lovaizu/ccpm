# 1 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| Given a worktree whose only residue is recurring test/build artifacts, the flow ends with `git status --porcelain` empty (residue gitignored away) | OK | Step 4 first bullet appends a repo-root `.gitignore` rule for clearly-regenerable artifacts (`.pytest_cache/`, `.coverage`, `htmlcov/`, `coverage.xml`, `__pycache__/`, `dist/`, `node_modules/`, `.tox/`), hiding them from `git status`; Step 5 commits the rule; Step 6 then sees an **empty** porcelain → Step 7. Pure recurring residue reaches empty | OK | Round-4 QA traced the recurring-artifact happy path (incl. a mis-matched rule recovering via the bounded corrective pass) and confirmed it reaches empty porcelain |
| Instructions direct adding a committed repo-root `.gitignore` rule for recurring artifacts so residue does not re-dirty the tree later | OK | Step 4 first bullet directs appending the rule to the **repo-root `.gitignore`** (create if absent), no delete / no artifact-commit; Step 5 commits the `.gitignore` edit together with `State` in **one commit per pass** and pushes — so the committed rule prevents re-dirtying on a later run | OK | QA confirmed repo-root placement + committed-in-Step-5; rule persists across runs |
| The agent never deletes any untracked path on its own; a not-clearly-regenerable path is surfaced to the user, never auto-deleted or auto-gitignored | OK | Step 4 second bullet: any path not *sure* to be regenerable (and "any doubt → treat as the next bullet") → "never delete, never gitignore", surface to user to decide; explicit line "The agent never deletes a file on its own. Deletion is only ever the user's explicit choice." No auto-delete branch exists | OK | QA: three reinforcing statements; no reachable path auto-deletes or auto-gitignores an uncertain path. Strongest-held criterion |
| The suspend always completes — never loops/wedges; an unresolved path is recorded in `State → Notes` and the session suspends anyway | OK | Termination is **structural per-path**, not a prose "once": Step 6 caps the corrective attempt for any failing `.gitignore` rule at **most once for that path** — if the same porcelain path still appears after that one attempt, the agent stops fixing and **reclassifies it as user-deferred in `State → Notes`** (literal porcelain path, per Step 4). The stated invariant: every untracked path lands in exactly one of two terminal buckets — (a) gitignored away or (b) recorded user-deferred — after ≤1 corrective attempt, so with finitely many paths the flow provably terminates without any global loop counter. Crucially, the per-path correction marker is now **persisted in `State` → `Notes`** (Step 4 annotates the path on the corrective attempt; Step 6 reads the spent/unspent state from persisted `State`, not agent memory), so the bound survives a context compaction mid-suspend — the exact boundary `/rn:dn` exists to cross. Corrective re-entry to Step 4 is scoped to the returned path(s); already-deferred paths in Notes are settled and not re-surfaced. Step 6's match against Notes is exact because Step 4 records the literal `git status --porcelain` string. The suspend always completes | OK | QA round 4 PASS. Round-3 had flagged the bound as memory-held (asserted, not constructed); the persisted-marker fix closed it. Worst-case trace (multiple artifacts + a repeatedly-mis-matching rule + a deferred path) terminates with no undefined transition |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Meaningful tests/verification | OK | The verification (Step 6's `git status --porcelain` empty / accepted-terminal check) is meaningful to the purpose — a genuinely clean tree or an explicit, recorded handoff — not a nominal assertion. Confirmed by adversarial trace, not just by the steps being present |
| Edge case coverage | OK | Across four rounds, QA surfaced and the fixes closed: wedge with no terminal state; silent auto-delete; gitignore no-op on tracked-dirty; co-occurring gitignored + deferred paths; unbounded retry on a repeatedly-mis-matching rule; double-commit; prose-vs-literal Notes matching; stale push-status across two passes; correction-marker lost on context compaction. Final residual seams were Low and were fixed |

## Overall Verdict

- Self-check: OK
- QA: OK (round 4 PASS, after 3 fix iterations; all four criteria met, two Low seams fixed)
- Ready for user review: Yes
