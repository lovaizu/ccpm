# 9 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| Collects exactly the unresolved threads whose last comment is the author's | OK | FIX 1 (pagination + robust author): GraphQL now paginates — `reviewThreads(first:100, after:$cursor)` with `pageInfo{ hasNextPage endCursor }`; "**Repeat** the call while `reviewThreads.pageInfo.hasNextPage` is true, passing the previous response's `endCursor` as `-F cursor=<endCursor>`, and **accumulate** `nodes` across pages." First page invocation `-F cursor=null`. Author determination is >100-comment-safe: `firstComment: comments(first:1){ nodes{ databaseId author{login} path line } }` and `lastComment: comments(last:1){ nodes{ author{login} } }`; step 2 keeps a thread only when `isResolved == false` AND `lastComment.nodes[0].author.login` == `firstComment.nodes[0].author.login`. Bodies still fetched best-effort via `comments(first:100){ nodes{ body author{login} } }`. |  |  |
| Processes one at a time with a coordinator review between each | OK | "Process the queue **one thread at a time**. Never dispatch two threads in parallel — the coordinator reviews each subagent's result before dispatching the next." Phase: Coordinator review "between items": OK → dispatch the next; problem → re-instruct the same subagent on the same thread. |  |  |
| Each thread ends addressed-and-replied-with-its-commit-link OR answered with a question | OK | Outcome (a): change → `git add <path>…` → commit → push → `gh browse <sha> -n` → reply via `gh api .../replies -f body='Done: … <commit-url>'`. Outcome (b): no code change, reply with the question. FIX 2 (push-fail path): new step 5 "**If the commit or push fails, do not post a Done reply** (its permalink would point at an unpushed, dead commit). Return the failure in the summary so the coordinator resolves the git mechanics (stage the explicit paths, push); post the reply only once the commit is confirmed pushed." Reply step now reads "**only after the commit is pushed**". |  |  |
| Resolution is left to the author | OK | Phase: Resolve-by-author: "The loop **never** resolves a thread. Neither the coordinator nor the subagent calls `resolveReviewThread`…"; "Resolution is the **author's** act on GitHub"; loop treats unresolved state as its queue so re-runs skip resolved threads. Also restated in Dispatch step 6. |  |  |
| Verification is the single coordinator pass | OK | Verification section: "One coordinator pass per item (Phase: Coordinator review) is the whole of verification. No QA expert, no language or software-engineering experts, no multi-round iteration cap." |  |  |
| Failure mode absent: thread dispatched past the review gate in parallel | OK | "Never dispatch two threads in parallel — the coordinator reviews each subagent's result before dispatching the next." (sequential dispatch) |  |  |
| Failure mode absent: a thread changed without a reply | OK | Outcome (a) makes reply (step 6) a mandatory step after commit/push; outcome (b) replies with a question. Both outcomes end in a reply. |  |  |
| Failure mode absent: heavy QA/multi-round chain present | OK | No QA expert / language / software-engineering experts / iteration cap anywhere; Verification explicitly excludes them. Intro contrasts with task-workflow's heavy chain. |  |  |
| Failure mode absent: assistant resolving a thread | OK | Resolve-by-author phase forbids both coordinator and subagent from resolving; Dispatch step 6 "Never resolve the thread." |  |  |
| `/rn:gm` no-arg enters loop; `/rn:gm <text>` does not | OK | Top of doc: "`/rn:gm` with no argument enters this loop … `/rn:gm <text>` instead acts on `<text>` directly and does **not** enter this loop." |  |  |
| Style mirrors task-workflow.md (Roles / Phases / numbered steps, rationale-free) | OK | Uses `# Title` + intro, `## Roles`, `## Phase: …` headings, numbered executable steps, concrete `gh`/`gh api graphql` commands; no "why" prose. FIX 3 (no-PR handling): Phase: Identify the PR step 1 now reads "If this exits non-zero or reports \"no pull requests found\" for the branch, **stop and report** — do not fabricate a PR number or proceed into the GraphQL call." FIX 4 (owner/repo split): captured as two separate shell vars — `owner=$(gh repo view --json owner,name -q '.owner.login')` and `repo=$(gh repo view --json owner,name -q '.name')`; the GraphQL call passes `-f owner="$owner" -f repo="$repo"`, so `{owner}`/`{repo}` placeholders are now unambiguous. |  |  |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Meaningful tests/verification | OK | QA re-run PASS: all completion-criterion clauses hold; validated live against PR #14 — GraphQL alias coexistence, `-F cursor=null` null-typing, `-F pr=` Int-typing, and `gh browse <sha> -n` permalink. |
| Edge case coverage | OK | Round-1 NG findings fixed and re-verified: pagination over >100 threads; >100-comment-safe last-author via `comments(last:1)`; commit/push-fail path (no dead permalink); no-PR stop-and-report. Remaining nit cosmetic (reply snippet shows `{owner}` braces vs the captured `$owner`/`$repo` vars; in scope, non-blocking). |

## Overall Verdict

- Self-check: OK — all 4 QA fixes applied and verified against the on-disk file: (1) `reviewThreads` paginated via cursor loop + >100-comment-safe first/last author via `firstComment`/`lastComment` aliases; (2) Outcome (a) posts the Done reply only after the commit is pushed, returns the failure otherwise; (3) no-PR → stop and report; (4) owner/repo captured as two separate shell vars. Completion-criterion clauses still hold.
- QA: OK (re-run PASS after round-1 fixes)
- Ready to check off: Yes
