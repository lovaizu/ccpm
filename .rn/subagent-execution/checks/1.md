# 1 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence |
|---|---|---|
| Execute phase delegates the actual work to an implementation expert dispatched by the coordinator; the coordinator performs no domain work itself | OK | `## Phase: Execute — coordinator dispatches the implementation expert`; intro "The coordinator does no domain work itself — it routes work to the expert who owns that domain" |
| The implementation expert returns only a compact summary; the document instructs that the expert's full output and trial-and-error do not enter the coordinator's context | OK | Work-order element 6 "Return — compact summary only … Do not paste full file contents or the trial-and-error"; Dispatch step "only its summary enters the coordinator's context" |
| The Verify phase has the coordinator inspect `git diff` and dispatch the review experts, and re-instruct the implementation expert for fixes (the coordinator edits no artifact itself) | OK | `Step — Read the diff`; QA/language/software-engineering expert reviews; `Step — Triage and re-instruct`: "the coordinator writes improvement instructions and re-dispatches the implementation expert … does not edit the artifact itself" |
| The Complete phase keeps the user-review gate before commit, and performs commit and push via a subagent | OK | `Step — User review` ("DO NOT proceed without user approval"); `Step — Commit and push (subagent)` dispatches a subagent to commit + push |
| Each expert is instructed to apply its domain's best practices, and the cast reads uniformly as experts (QA / language / software-engineering) across table, prompts, checklists, and check format | OK | Work-order element 4 "Best practices — apply the domain's best practices"; review prompt element 1 "from its domain's best practices"; Process-selection table, checklists, and Check file headers all say "QA expert / Language Expert / Software-engineering Expert" |
| The review-expert independence and neutral-framing safeguards are present and unweakened | OK | All 6 prompt elements retained; independence note retained; element 6 withholds self-check file, the implementation expert's summary, and any verdict |
| `gm` and `hi` still reach task execution solely through `task-workflow.md`, with no workflow text duplicated into the skill files | OK | `gm` Step 6 and `hi` Step 7 unchanged (still "read … task-workflow.md and execute"); no workflow prose added to skill files |

## QA Expert Review

Round 1 (FAIL → fixed): found that `steering-template.md` task-Steps still said "QA engineer review" /
"software engineer review", which `gm` propagates into every generated steering.md — contradicting the
new expert cast. **Valid finding.** Also a non-blocking note: the Process-selection table listed
self-check as a Verify step though it is produced in Execute.

Triage & fix:
- Renamed `steering-template.md` Steps → "QA expert review" / "software-engineering expert review".
- Clarified the Process-selection table: self-check is produced by the implementation expert in
  Execute; reviews run by the coordinator in Verify; user review in Complete.
- Repo-wide grep confirms zero leftover "QA engineer / software engineer / member / leader" role
  names in `rn/` and `.rn/`.

Round 2 (PASS): independent re-review confirmed the cast reads uniformly as experts across
task-workflow.md, steering-template.md, gm, hi; template Steps and Process selection are in sync; the
self-check phase attribution is corrected.

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Meaningful tests/verification | OK | Followable end-to-end; work-order (6 elements), review prompt (6 elements), bounded re-instruct loop, check-file format all concrete |
| Edge case coverage | OK | Check-file write split (expert self-check cols / coordinator verdicts) disjoint; re-instruct bounded at 3 + escalation; coordinator/expert boundary consistent; no dangling refs; cross-file vocabulary in sync |

## Overall Verdict

- Self-check: OK
- QA: OK (PASS, after one fix round)
- Ready for user review: Yes
