# 2 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence |
|---|---|---|
| `aiya/skills/<verb>/SKILL.md` exists | OK | `aiya/skills/conduct/SKILL.md` exists (204 lines). Verb chosen: `conduct` — matches design.md's own name for the main agent ("the Conductor", §3.1/§3.2), is a plain English verb (not a reused noun), and is the plugin's one skill per steering.md's Level A criterion (`aiya/skills/<conductor>/SKILL.md exists`). Frontmatter is model-invocable (no `disable-model-invocation`), third-person ("Runs the aiya Conductor loop…"), states concrete trigger phrases ("run aiya on this goal", "conduct the build for \<issue\>", "drive this goal.md to delivery", "continue the aiya Conductor loop", "resume the Step loop"), and is version-agnostic (explicit "behavior does not depend on the installed aiya version" clause, and no version number is referenced anywhere in the body). Frontmatter shape checked against `~/.claude/plugins/cache/ccpm/rn/0.7.0/skills/{ty,gm,on,up,dn}/SKILL.md` and `plugin-dev`'s `skill-development/SKILL.md` guidance (third-person description, name+description only). |
| Skill is prompt-driven, no embedded JS/script loop | OK | `grep -nE '```(js\|javascript\|python\|bash\|sh)\|function(\|=>\|while\s*(\|for\s*(' aiya/skills/conduct/SKILL.md aiya/skills/conduct/references/viewpoints.md` returns no matches (grep exit 1) — no code fence of any language, no function/loop syntax anywhere in either file. The only fenced block in SKILL.md is the plain-text `.aiya/<issue>/` directory tree (L21–28). The loop is stated entirely as imperative Markdown the main agent (the Conductor) follows by reading and reasoning — "There is no controller script. This file *is* the loop" (L14–15) — matching design.md §5.1's first alternatives-considered entry ("Prompt-driven, no script holds the loop... carried by procedure prose") and steering.md's Rules ("no JS Workflow-tool script holds the iteration loop in the plugin"). Turns are dispatched via the Agent-tool-style "compose a fresh work order" mechanism (SKILL.md L36–41, L73–77), never a hardcoded script controlling iteration. |
| Encodes bounded-CCS / verify-Turn / re-aim / phase-gate cycle with a stated cap | OK | Checked item by item against `aiya/docs/design.md`, every pillar traced to a specific section, none sampled: **CCS contract** (§4.1) — file path `.aiya/<issue>/ccs/tNNN.yaml`, replacement semantics (SKILL.md L80–82), all 9 fixed components + starting `type` vocabulary table verbatim from §4.1 (L90–103), all 4 boundedness rules incl. the symptom→remedy mapping (L105–119), retrospective/`yq`/`wc -c` parseability note (§4.1 last section → SKILL.md L121–123), and the §4.4 "feeding the phase chain in" mapping (`goal.md`→`goal_orientation`, `approach.md` constraints→`constraints`, phase docs by path→`retrieved_artifacts`) (L85–88). **verify-Turn contract** (§4.2) — per-viewpoint dispatch, all flat direct children of the Conductor / not nesting (L134–135), fresh context + blind to the self-report (L135–143), true-goal-from-gate-approved-doc-never-running-CCS goal-provenance rule with its rationale (L137–139), 3-target priority order + mechanical-checks-first/LLM-as-thin-last-layer (L144–146), Acceptance-Scenario simulation / test-first (L146–148), mechanical aggregation = pass-only-if-all-pass / gap-is-union (L148–153), and the discarded-single-shot-Turn / artifact-too-large-splits-the-Step boundedness rule (L155–158) — this last item was missing from the first draft and added on the cross-check pass. **Re-aim path** (§4.3) — 3-attempt cap, corrective-instruction re-dispatch, escalation as an exception interrupt distinct from the 6 gates, failure-history (≤3 gaps) payload, counter kept in the Conductor's own per-Step working context so it resets and stays bounded (L160–171). **Phase gates** (§4.4) — 3 phases × {Planning Gate IN, Output Gate OUT} = 6 touchpoints table reproduced (L179–183), who-drafts-each-phase respected (goal.md precondition/human-authored L30–34, approach.md Conductor-drafted+G2 L43–52, delivery.md Conductor-drafted+planning-gate L54–60), storage layout tree matches §4.4 verbatim (L19–28), async-chat surface + `/ty`/`/gm` approve-or-redirect (human redirects, not merely approves) at every gate incl. G2/planning-IN/G3 (L50–52, L60, L185–189), sparse-by-design rationale (L173–177). **Invariants** — "1 Step = 1 Turn" stated with its re-aim-adds-work-Turns-within-the-cap clause (§3.2 → L68–71) and "Turns never nest" / dynamically-composed-work-order-not-a-static-agent-file (§3.2 → L36–41). Re-verified after two gaps found on the first cross-check pass and fixed: (1) verify-Turn boundedness/split-the-Step-not-the-verifier was absent, added at L155–158; (2) the §4.4 phase→CCS-component feed rule was absent, added at L85–88; (3) a factual slip where the Storage section's `delivery.md` comment conflated the (unnamed) planning-gate with G3 (the Output gate) was caught and corrected to distinguish them (L25, L56–57) — G3 is the Output gate that closes only after Step execution, not the Steps-review planning gate. |

## Overall Verdict

- Self-check: OK
