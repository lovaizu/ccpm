# 2 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence |
|---|---|---|
| (a) exact command to send ESC that stops an in-progress CC response, targeting a surface by id, with observed result | OK | `cmux send-key --surface surface:21 escape` interrupted a live-streaming 600-word essay. `read-screen` showed it cut off mid-sentence (`...Remington company, a firm`) with marker `⎿  Interrupted · What should Claude do instead?` and the `/effort` active-generation indicator gone. ESC key name = `escape`. |
| (b) exact command to type a slash command and submit it, targeting a surface by id, with observed result | OK | `cmux send --surface surface:21 '/help'` then `cmux send-key --surface surface:21 enter` ran the slash command — `read-screen` showed `Help  General   Commands   Custom commands`. Generic text+submit also proven: `send 'echo hello-cmux-7f3'` + `send-key enter` → `read-screen` showed `hello-cmux-7f3`. |
| (c) exact command to read a surface's screen, targeting a surface by id, with observed result | OK | `cmux read-screen --surface surface:21` returned the rendered pane text used to confirm every result above (shell prompt `❯`, echo output, streaming essay, interrupt marker). |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Meaningful verification (observed effect, not exit-0) | OK | (a) stop evidenced by `⎿ Interrupted` marker + `/effort` indicator gone, not just a keypress; (b) `/help` panel rendered, proving the command ran; (c) real pane text returned (used to confirm a/b). |
| Edge cases / fragility coverage | OK | PTY-attach gotcha, always-`--surface`, non-atomic submit race, surface-health unreliability all documented. |

## Triage of QA findings

- **[HIGH] `cmux tree --id-format both` would error (flag must precede subcommand)** — **REJECTED as Invalid.** Live test refutes it: `cmux tree --id-format both` returns exit 0 and prints UUIDs, identical to `cmux --id-format both tree`. cmux accepts the global flag in either position; the note is correct as written.
- **[MED] `escape` asserted, not shown as a valid key name** — **Accepted, fixed.** Added a line: the observed interrupt itself proves the key name is valid+effective (an unknown key would not stop generation).
- **[MED] single-call `send '...\n'` submit omitted** — **Accepted, fixed.** Documented the `\n`/`\r` atomic form (per `cmux send --help`) with the caveat it was confirmed for shell only and not the TUI prompt; two-call `send`+`send-key enter` stays the verified default.
- **[LOW] surface:21 unverifiable post-hoc; PTY guidance sound** — no action (acceptable for a spike / positive).

Re-review judged unnecessary: the two accepted fixes are the exact clarifying lines QA prescribed (verifiable by reading), and the HIGH was rejected on hard live evidence — no behavior changed that a re-run would re-assess.

## Overall Verdict
- Self-check: OK
- QA: OK (PASS; 1 finding rejected on evidence, 2 minor doc fixes applied)
- Ready for user review: Yes
