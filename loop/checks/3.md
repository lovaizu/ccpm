# 3 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence |
|---|---|---|
| bb_done = false before a real bb run (HEAD, Status not paused) | OK | `./loop/gate.sh bb_done HEAD` → exit 1. `status_at HEAD .rn/loop/steering.md` → `not suspended`. |
| bb_done = true after a real bb run (clean tree + Status: paused at the fixture commit) | OK | `./loop/gate.sh bb_done 668bbf6` → exit 0 (on clean tree). `status_at 668bbf6 .rn/loop/steering.md` → `paused`. |
| rearmed = true after a real /rn:hi reset | OK | `./loop/gate.sh rearmed HEAD` → exit 0 (HEAD = real /rn:hi reset, `not suspended`). Counter: `rearmed 668bbf6` → exit 1 (paused). |
| demonstrated against actual commits, results recorded | OK | Refs used: `668bbf6` (real /rn:bb, paused), `HEAD` (real /rn:hi reset, not suspended). Dirty-tree half exercised against the live tree via a temp untracked file in gate.test.sh. |

## Test Coverage
- Test file: loop/gate.test.sh — 7 cases, all PASS on a clean tree (`7 passed, 0 failed`, exit 0).
  Cases: bb_done@paused(clean)→true, bb_done@HEAD→false, bb_done@paused(dirty tree)→false,
  bb_done@paused(clean again)→true, rearmed@HEAD→true, rearmed@paused→false,
  steering_path→.rn/loop/steering.md.
- The two tree-cleanliness cases (dirty/clean-again) exercise the LIVE-tree half of bb_done
  independently of the ref (create+remove a temp untracked file). The Status-from-git half is
  exercised against the real historical fixture refs without moving HEAD.
- Coverage is by enumerated behavioral cases (no shell line-coverage tool used).
- Note: bb_done's clean-tree half is genuinely live — the suite only reaches 7/7 once the
  deliverables themselves are committed (tree clean), mirroring the real watcher scenario where
  /rn:bb has committed and pushed everything. While the gate files were untracked the clean-tree
  cases failed honestly, confirming the predicate is not faking cleanliness.

## Overall Verdict
- Self-check: OK
- Ready for user review: Yes — all completion criteria met against real commits; predicates and CLI
  dispatch (exit 0=true / 1=false) are watcher-ready. shellcheck not installed in this environment
  (could not lint); code follows the same conventions but was not statically verified.
